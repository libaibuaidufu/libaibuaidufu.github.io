<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Elasticsearch、Kibana 使用</title>
    <url>/post/Elasticsearch%E3%80%81Kibana-%E4%BD%BF%E7%94%A8.html</url>
    <content><![CDATA[<pre><code>通过最近一段时间对`ES`的基础使用，也算有了一定的了解，特在此记录一下，避免以后忘记。
</code></pre>
<span id="more"></span>

<h2 id="ES、Kibana、Elasticsearch-head安装"><a href="#ES、Kibana、Elasticsearch-head安装" class="headerlink" title="ES、Kibana、Elasticsearch-head安装"></a>ES、Kibana、Elasticsearch-head安装</h2><h3 id="docker一键部署"><a href="#docker一键部署" class="headerlink" title="docker一键部署"></a>docker一键部署</h3><p><code>docker-compose.yaml</code>文件</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">version:</span> <span class="string">&#x27;3&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="attr">services:</span></span><br><span class="line">  <span class="attr">es:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">elasticsearch:7.6.2</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">always</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;9200:9200&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;9300:9300&quot;</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;discovery.type=single-node&quot;</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;ES_JAVA_OPTS=-Xms64m -Xmx512m&quot;</span></span><br><span class="line">    <span class="attr">volumes:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">esdata:/usr/share/elasticsearch/data</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">esplugins:/usr/share/elasticsearch/plugins</span></span><br><span class="line">    <span class="attr">user:</span> <span class="string">root</span></span><br><span class="line">    <span class="attr">networks:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">es</span></span><br><span class="line">  <span class="attr">kibana:</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">kibana:7.6.2</span></span><br><span class="line">    <span class="attr">restart:</span> <span class="string">always</span></span><br><span class="line">    <span class="attr">environment:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">ELASTICSEARCH_HOSTS=http://elasticsearch:9200</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">I18N_LOCALE=zh-CN</span></span><br><span class="line"><span class="comment">#      - ELASTICSEARCH_USERNAME=elastic</span></span><br><span class="line"><span class="comment">#      - ELASTICSEARCH_PASSWORD=mypasword</span></span><br><span class="line">    <span class="attr">depends_on:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">es</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;5601:5601&quot;</span></span><br><span class="line">    <span class="attr">links:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">&quot;es:elasticsearch&quot;</span></span><br><span class="line">    <span class="attr">networks:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">es</span></span><br><span class="line"></span><br><span class="line"><span class="attr">volumes:</span></span><br><span class="line">  <span class="attr">esdata:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br><span class="line">  <span class="attr">esplugins:</span></span><br><span class="line">    <span class="attr">driver:</span> <span class="string">local</span></span><br><span class="line"><span class="attr">networks:</span></span><br><span class="line">  <span class="attr">es:</span></span><br></pre></td></tr></table></figure>

<h3 id="快速使用"><a href="#快速使用" class="headerlink" title="快速使用"></a>快速使用</h3><p>启动：<code>docker-compose up -d</code></p>
<p>删除：<code>docker-compose down</code></p>
<p>查看容器信息：<code>docker inspect container-name-xxx</code></p>
<h3 id="安装ik分词"><a href="#安装ik分词" class="headerlink" title="安装ik分词"></a>安装ik分词</h3><p><code>docker volume ls </code>查看所有挂载</p>
<p><code>docker volume inspect es_esplugins</code>查看挂载信息后，找到挂载地址<code>Mountpoint</code>对应地址。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">root@dns:~/downloads/es_search_fastapi# docker volume inspect es_esplugins</span><br><span class="line">[</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="string">&quot;CreatedAt&quot;</span>: <span class="string">&quot;2021-12-22T15:42:14+08:00&quot;</span>,</span><br><span class="line">        <span class="string">&quot;Driver&quot;</span>: <span class="string">&quot;local&quot;</span>,</span><br><span class="line">        <span class="string">&quot;Labels&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;com.docker.compose.project&quot;</span>: <span class="string">&quot;es&quot;</span>,</span><br><span class="line">            <span class="string">&quot;com.docker.compose.version&quot;</span>: <span class="string">&quot;1.29.2&quot;</span>,</span><br><span class="line">            <span class="string">&quot;com.docker.compose.volume&quot;</span>: <span class="string">&quot;esplugins&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;Mountpoint&quot;</span>: <span class="string">&quot;/var/lib/docker/volumes/es_esplugins/_data&quot;</span>,</span><br><span class="line">        <span class="string">&quot;Name&quot;</span>: <span class="string">&quot;es_esplugins&quot;</span>,</span><br><span class="line">        <span class="string">&quot;Options&quot;</span>: null,</span><br><span class="line">        <span class="string">&quot;Scope&quot;</span>: <span class="string">&quot;local&quot;</span></span><br><span class="line">    &#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<ol>
<li>在  <a href="https://github.com/medcl/elasticsearch-analysis-ik">https://github.com/medcl/elasticsearch-analysis-ik</a> 下载对应版本</li>
<li>解压到 <code>/var/lib/docker/volumes/es_esplugins/_data/ik</code>目录下(没有<code>ik</code>目录，就自己创建一个)</li>
<li>重启 ES容器 <code>docker restart es_es_1</code>，就可以了</li>
</ol>
<h3 id="Elasticsearch-head-插件"><a href="#Elasticsearch-head-插件" class="headerlink" title="Elasticsearch-head 插件"></a>Elasticsearch-head 插件</h3><pre><code>我使用的是[谷歌插件](https://chrome.google.com/webstore/detail/elasticsearch-head/ffmkiejjmecolpfloofpjologoblkegm?utm_source=chrome-ntp-icon),可以自行搜索下载。
</code></pre>
<h2 id="Kibana对ES操作使用"><a href="#Kibana对ES操作使用" class="headerlink" title="Kibana对ES操作使用"></a>Kibana对ES操作使用</h2><h3 id="操作"><a href="#操作" class="headerlink" title="操作"></a>操作</h3><p>本来想写的，但是觉得这样内容太冗余了。后面放一些我收藏的别人的写的吧，毕竟一直重复也没有任何意义。</p>
<h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ol>
<li><code>ik分词</code>会对内容分词，因此需要全匹配的不适用<code>ik</code>和<code>keyword</code></li>
<li><code>_reindex</code>时，内容过多会超时，因此加上<code>?wait_for_completion=false</code>，不用等待</li>
<li><code>ES</code>分页修改查询10000限制，<code>index.max_result_window</code> 下面有修改语句</li>
<li><code>ES7.0</code>版本以后，一个<code>index</code>对应一个<code>type</code>了，不再是以前的一个<code>index</code>对应多个<code>type</code>，因此<code>index</code>也相当于一张表了</li>
</ol>
<h3 id="代码记录"><a href="#代码记录" class="headerlink" title="代码记录"></a>代码记录</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">GET _search</span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;query&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;match_all&quot;</span>: &#123;&#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 修改查询10000条限制</span></span><br><span class="line">PUT data_item/_settings</span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;index.max_result_window&quot;</span>:1000000</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 获取最后一个id，保持增量更新</span></span><br><span class="line">GET data_item/_search</span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;_source&quot;</span>: <span class="string">&quot;item_id&quot;</span>, </span><br><span class="line">  <span class="string">&quot;sort&quot;</span>: [</span><br><span class="line">    &#123;</span><br><span class="line">      <span class="string">&quot;item_id&quot;</span>: &#123;</span><br><span class="line">        <span class="string">&quot;order&quot;</span>: <span class="string">&quot;desc&quot;</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  ]</span><br><span class="line">  , <span class="string">&quot;size&quot;</span>: 1</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 搜索推荐词 推荐</span></span><br><span class="line">GET data_item/_search</span><br><span class="line">&#123; </span><br><span class="line">  <span class="string">&quot;suggest&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;my-suggestion&quot;</span>: &#123;</span><br><span class="line">      <span class="string">&quot;text&quot;</span>: <span class="string">&quot;科技&quot;</span>,</span><br><span class="line">      <span class="string">&quot;term&quot;</span>: &#123;</span><br><span class="line">        <span class="string">&quot;suggest_mode&quot;</span>: <span class="string">&quot;popular&quot;</span>,</span><br><span class="line">        <span class="string">&quot;field&quot;</span>: <span class="string">&quot;item_title&quot;</span></span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 获取索引映射</span></span><br><span class="line">GET data_item/_mapping</span><br><span class="line"><span class="comment"># 删除索引</span></span><br><span class="line">DELETE data_item</span><br><span class="line"><span class="comment"># 新建索引</span></span><br><span class="line">PUT data_item</span><br><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;mappings&quot;</span> : &#123;</span><br><span class="line">      <span class="string">&quot;properties&quot;</span> : &#123;</span><br><span class="line">        <span class="string">&quot;area_code&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;integer&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;create_time&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;date&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;dept_id&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;long&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;dept_name&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;text&quot;</span>,</span><br><span class="line">          <span class="string">&quot;analyzer&quot;</span> : <span class="string">&quot;ik_max_word&quot;</span>,</span><br><span class="line">          <span class="string">&quot;search_analyzer&quot;</span> : <span class="string">&quot;ik_smart&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;item_content&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;text&quot;</span>,</span><br><span class="line">          <span class="string">&quot;analyzer&quot;</span> : <span class="string">&quot;ik_max_word&quot;</span>,</span><br><span class="line">          <span class="string">&quot;search_analyzer&quot;</span> : <span class="string">&quot;ik_smart&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;item_deadline&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;date&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;item_id&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;long&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;item_id_key&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;text&quot;</span>,</span><br><span class="line">          <span class="string">&quot;index&quot;</span> : <span class="literal">false</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;item_imgurl&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;text&quot;</span>,</span><br><span class="line">          <span class="string">&quot;index&quot;</span> : <span class="literal">false</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;item_pulishdate&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;date&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;item_title&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;text&quot;</span>,</span><br><span class="line">          <span class="string">&quot;analyzer&quot;</span> : <span class="string">&quot;ik_max_word&quot;</span>,</span><br><span class="line">          <span class="string">&quot;search_analyzer&quot;</span> : <span class="string">&quot;ik_smart&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;item_type&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;long&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;item_type_name&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;text&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;item_url&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;text&quot;</span>,</span><br><span class="line">          <span class="string">&quot;index&quot;</span> : <span class="literal">false</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;level_code&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;long&quot;</span></span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="string">&quot;level_code_name&quot;</span> : &#123;</span><br><span class="line">          <span class="string">&quot;type&quot;</span> : <span class="string">&quot;text&quot;</span></span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 索引内容转移到另一个索引</span></span><br><span class="line">POST _reindex?wait_for_completion=<span class="literal">false</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;source&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;index&quot;</span>: <span class="string">&quot;data_item&quot;</span>,</span><br><span class="line">    <span class="string">&quot;size&quot;</span>: 5000</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">&quot;dest&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;index&quot;</span>: <span class="string">&quot;data_item_project&quot;</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 目前我这里查询最复杂的一种</span></span><br><span class="line">GET data_item/_search</span><br><span class="line">&#123;</span><br><span class="line">  <span class="string">&quot;query&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;bool&quot;</span>: &#123;</span><br><span class="line">      <span class="string">&quot;must&quot;</span>: [</span><br><span class="line">        &#123;</span><br><span class="line">          <span class="string">&quot;term&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;dept_id&quot;</span>: 8</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">          <span class="string">&quot;term&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;item_type&quot;</span>: 1</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">          <span class="string">&quot;term&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;level_code&quot;</span>: 2</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">          <span class="string">&quot;match&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;item_title&quot;</span>: <span class="string">&quot;创新&quot;</span></span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      ],</span><br><span class="line">      <span class="string">&quot;filter&quot;</span>: &#123;</span><br><span class="line">        <span class="string">&quot;range&quot;</span>: &#123;</span><br><span class="line">          <span class="string">&quot;item_pulishdate&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;gte&quot;</span>: <span class="string">&quot;2021-01-11&quot;</span></span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;,</span><br><span class="line">      <span class="string">&quot;should&quot;</span>: [</span><br><span class="line">        &#123;</span><br><span class="line">          <span class="string">&quot;term&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;area_code&quot;</span>: <span class="string">&quot;510100&quot;</span></span><br><span class="line">          &#125;</span><br><span class="line">        &#125;,</span><br><span class="line">        &#123;</span><br><span class="line">          <span class="string">&quot;term&quot;</span>: &#123;</span><br><span class="line">            <span class="string">&quot;area_code&quot;</span>: <span class="string">&quot;510300&quot;</span></span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      ]</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">&quot;highlight&quot;</span>: &#123;</span><br><span class="line">    <span class="string">&quot;pre_tags&quot;</span>: [</span><br><span class="line">      <span class="string">&quot;&lt;em&gt;&quot;</span></span><br><span class="line">    ],</span><br><span class="line">    <span class="string">&quot;post_tags&quot;</span>: [</span><br><span class="line">      <span class="string">&quot;&lt;/em&gt;&quot;</span>,</span><br><span class="line">    ],</span><br><span class="line">    <span class="string">&quot;fields&quot;</span>: &#123;</span><br><span class="line">      <span class="string">&quot;item_title&quot;</span>: &#123;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">&quot;from&quot;</span>: 0,</span><br><span class="line">  <span class="string">&quot;size&quot;</span>: 10</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Elasticsearch</category>
      </categories>
  </entry>
  <entry>
    <title>next主题文章列表 显示摘要</title>
    <url>/post/My-New-Post.html</url>
    <content><![CDATA[<p>本文针对Next主题，不确保对于其它主题有效（但从修改模式来看，是有效的）</p>
<p>Next默认是会显示全文的，这样显然很不方便，因此需要一些方法去只显示前面一部分。</p>
<h1 id="修改配置"><a href="#修改配置" class="headerlink" title="修改配置"></a>修改配置</h1><p>首先需要在Next主题的_config.yml中把设置打开：(默认安装时就打开了)</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Automatically excerpt description in homepage as preamble text.</span></span><br><span class="line"><span class="attr">excerpt_description:</span> <span class="literal">true</span></span><br><span class="line"><span class="number">12</span></span><br></pre></td></tr></table></figure>

<p>之后有两种方法</p>
<h2 id="方法一：写概述"><a href="#方法一：写概述" class="headerlink" title="方法一：写概述"></a>方法一：写概述</h2><p>在文章的<code>front-matter</code>中添加<code>description</code>，其中description中的内容就会被显示在首页上，其余一律不显示。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 让首页显示部分内容</span><br><span class="line">date: 2020-02-23 22:55:10</span><br><span class="line">description: 这是显示在首页的概述，正文内容均会被隐藏。</span><br><span class="line">---</span><br></pre></td></tr></table></figure>

<p>比较不方便的是还得写一下概述，很多时候会懒得写概述，于是就需要第二种方法了。</p>
<h2 id="方法二：文章截断"><a href="#方法二：文章截断" class="headerlink" title="方法二：文章截断"></a>方法二：文章截断</h2><p>在需要截断的地方加入：</p>
<figure class="highlight markdown"><table><tr><td class="code"><pre><span class="line">&lt;!--more--&gt;</span><br></pre></td></tr></table></figure>

<p>首页就会显示这条以上的所有内容，隐藏接下来的所有内容。<br>例如本文会显示到<code>修改配置</code>上面。</p>
<p>这个明显就方便很多，但当然有利有弊，比如开头都是废话首页看着就不是很好看，因此我一般会先选择方法二，如果感觉文章前面的写的不太好再用方法一。</p>
]]></content>
      <categories>
        <category>博客搭建</category>
      </categories>
      <tags>
        <tag>hexo</tag>
        <tag>next</tag>
      </tags>
  </entry>
  <entry>
    <title>cloudflare 动态修改 dns</title>
    <url>/post/cloudflare-%E5%8A%A8%E6%80%81%E4%BF%AE%E6%94%B9-dns.html</url>
    <content><![CDATA[<h3 id="首先申请-api-token"><a href="#首先申请-api-token" class="headerlink" title="首先申请 api-token"></a>首先申请 api-token</h3><ol>
<li>登陆进 <a href="http://www.cloudflare.com/">www.cloudflare.com</a></li>
<li>点击进入自己的域名</li>
<li>中文 概述 ，下滑找到 区域id (zone_id)复制保存， 点击下面的获取api令牌</li>
<li>进去创建令牌 ，选择dns模板</li>
<li>可以保持默认 直接创建，也可以在区域选择上指定 可操作 域名</li>
<li>得到令牌，后有一个测试curl命令 去测试一下</li>
</ol>
<span id="more"></span>

<p>已获得参数：</p>
<p>API_KEY &#x3D;令牌token<br>ZONE_ID &#x3D; 区域id<br>email &#x3D; 自己的邮箱<br>-x 是 代理</p>
<h3 id="获取-指定-dns记录-id"><a href="#获取-指定-dns记录-id" class="headerlink" title="获取 指定 dns记录 id"></a>获取 指定 dns记录 id</h3><p><strong>注意替换下面的 参数 为自己的,&lt;&gt;也要去掉</strong> </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -X GET <span class="string">&quot;https://api.cloudflare.com/client/v4/zones/&lt;ZONE_ID&gt;/dns_records&quot;</span> \</span><br><span class="line">     -H <span class="string">&quot;X-Auth-Email:&lt;email&gt;&quot;</span> \</span><br><span class="line">     -H <span class="string">&quot;Authorization:Bearer &lt;API_KEY&gt;&quot;</span> \</span><br><span class="line">     -H <span class="string">&quot;Content-Type:application/json&quot;</span> </span><br></pre></td></tr></table></figure>

<p><strong>从上面返回的结果中找到你需要的域名的id 可通过 域名搜索</strong></p>
<p>获取到id：<br>dx &#x3D; xxxx_id</p>
<h3 id="更新执行dns记录"><a href="#更新执行dns记录" class="headerlink" title="更新执行dns记录"></a>更新执行dns记录</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl -X PUT <span class="string">&quot;https://api.cloudflare.com/client/v4/zones/&lt;ZONE_ID&gt;/dns_records/&lt;xxxx_id&gt;&quot;</span> \</span><br><span class="line">     -H <span class="string">&quot;X-Auth-Email:&lt;email&gt;&quot;</span> \</span><br><span class="line">     -H <span class="string">&quot;Authorization:Bearer &lt;API_KEY&gt;&quot;</span> \</span><br><span class="line">     -H <span class="string">&quot;Content-Type:application/json&quot;</span> </span><br><span class="line">     --data <span class="string">&#x27;&#123;&quot;type&quot;:&quot;A&quot;,&quot;name&quot;:&quot;dx.example.com&quot;,&quot;content&quot;:&quot;1.2.3.4&quot;,&quot;ttl&quot;:120,&quot;proxied&quot;:false&#125;&#x27;</span></span><br></pre></td></tr></table></figure>

<h3 id="脚本处理"><a href="#脚本处理" class="headerlink" title="脚本处理"></a>脚本处理</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># modify_dns.sh</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#!/bin/bash</span></span><br><span class="line">ip=<span class="string">&#x27;1.1.1.1&#x27;</span></span><br><span class="line">dns_id=<span class="string">&#x27;xxx_id&#x27;</span></span><br><span class="line">data=<span class="string">&#x27;&#123;&quot;type&quot;:&quot;A&quot;,&quot;name&quot;:&quot;dx.example.com&quot;,&quot;content&quot;:&quot;%s&quot;,&quot;ttl&quot;:120,&quot;proxied&quot;:false&#125;&#x27;</span></span><br><span class="line">str_data=$(<span class="built_in">printf</span> <span class="string">&quot;<span class="variable">$data</span>&quot;</span> <span class="string">&quot;<span class="variable">$ip</span>&quot;</span>)</span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$str_data</span></span><br><span class="line">url=$(<span class="built_in">printf</span> <span class="string">&#x27;https://api.cloudflare.com/client/v4/zones/&lt;zone_id&gt;/dns_records/&#x27;</span><span class="variable">$dns_id</span> )</span><br><span class="line"><span class="built_in">echo</span> <span class="variable">$url</span></span><br><span class="line">curl -X PUT <span class="variable">$url</span> \</span><br><span class="line">     -H <span class="string">&quot;X-Auth-Email:&lt;email&gt;&quot;</span> \</span><br><span class="line">     -H <span class="string">&quot;Authorization:Bearer &lt;API_KEY&gt;&quot;</span> \</span><br><span class="line">     -H <span class="string">&quot;Content-Type:application/json&quot;</span> </span><br><span class="line">     --data <span class="variable">$str_data</span></span><br></pre></td></tr></table></figure>

<h3 id="其他脚本-调用此脚本"><a href="#其他脚本-调用此脚本" class="headerlink" title="其他脚本 调用此脚本"></a>其他脚本 调用此脚本</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># test.sh</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#!/bin/bash</span></span><br><span class="line">ip=<span class="string">&quot;1.2.3.4&quot;</span></span><br><span class="line"><span class="built_in">source</span> ./modify_dns.sh <span class="variable">$ip</span></span><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>教程</category>
      </categories>
      <tags>
        <tag>cloudflare</tag>
        <tag>dns</tag>
      </tags>
  </entry>
  <entry>
    <title>flask rbac 设计</title>
    <url>/post/flask-rbac-%E8%AE%BE%E8%AE%A1.html</url>
    <content><![CDATA[<p>一直对rbac设计有点不确定，看了一些文章对他有一定的了解，因此也想动手写一写。</p>
<span id="more"></span>

<h3 id="flask-rbac-code"><a href="#flask-rbac-code" class="headerlink" title="flask rbac code"></a>flask rbac code</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> logging <span class="keyword">as</span> logger</span><br><span class="line"><span class="keyword">import</span> uuid</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, g</span><br><span class="line"><span class="keyword">from</span> flask_admin <span class="keyword">import</span> Admin</span><br><span class="line"><span class="keyword">from</span> flask_admin.contrib.sqla <span class="keyword">import</span> ModelView</span><br><span class="line"><span class="keyword">from</span> flask_curd_db <span class="keyword">import</span> FlaskCurdDb</span><br><span class="line"><span class="keyword">from</span> flask_sqlalchemy <span class="keyword">import</span> SQLAlchemy</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line">app.config[<span class="string">&quot;SECRET_KEY&quot;</span>] = <span class="built_in">str</span>(uuid.uuid4())</span><br><span class="line">app.config[<span class="string">&quot;SQLALCHEMY_DATABASE_URI&quot;</span>] = <span class="string">&quot;sqlite:///rbac.db&quot;</span></span><br><span class="line">app.config[<span class="string">&quot;SQLALCHEMY_TRACK_MODIFICATIONS&quot;</span>] = <span class="literal">True</span></span><br><span class="line">app.config[<span class="string">&#x27;FLASK_ADMIN_SWATCH&#x27;</span>] = <span class="string">&#x27;cerulean&#x27;</span></span><br><span class="line"></span><br><span class="line">db = SQLAlchemy(app)</span><br><span class="line">FlaskCurdDb(app, db, logger)</span><br><span class="line"></span><br><span class="line">admin = Admin(app, name=<span class="string">&#x27;flask-rbac&#x27;</span>, template_mode=<span class="string">&#x27;bootstrap3&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> flask_curd_db.db <span class="keyword">import</span> CRUDMixin</span><br><span class="line"></span><br><span class="line">tb_user_role = db.Table(</span><br><span class="line">    <span class="string">&#x27;tb_user_role&#x27;</span>,</span><br><span class="line">    db.Column(<span class="string">&quot;role_id&quot;</span>, db.Integer, db.ForeignKey(<span class="string">&quot;tb_role.id&quot;</span>), nullable=<span class="literal">False</span>),</span><br><span class="line">    db.Column(<span class="string">&quot;user_id&quot;</span>, db.Integer, db.ForeignKey(<span class="string">&quot;tb_user.id&quot;</span>), nullable=<span class="literal">False</span>)</span><br><span class="line">)</span><br><span class="line">tb_role_menu = db.Table(</span><br><span class="line">    <span class="string">&#x27;tb_role_menu&#x27;</span>,</span><br><span class="line">    db.Column(<span class="string">&quot;role_id&quot;</span>, db.Integer, db.ForeignKey(<span class="string">&quot;tb_role.id&quot;</span>), nullable=<span class="literal">False</span>),</span><br><span class="line">    db.Column(<span class="string">&quot;menu_id&quot;</span>, db.Integer, db.ForeignKey(<span class="string">&quot;tb_menu.id&quot;</span>), nullable=<span class="literal">False</span>)</span><br><span class="line">)</span><br><span class="line">tb_menu_pemission = db.Table(</span><br><span class="line">    <span class="string">&#x27;tb_menu_pemission&#x27;</span>,</span><br><span class="line">    db.Column(<span class="string">&quot;menu_id&quot;</span>, db.Integer, db.ForeignKey(<span class="string">&quot;tb_menu.id&quot;</span>), nullable=<span class="literal">False</span>),</span><br><span class="line">    db.Column(<span class="string">&quot;pemission_id&quot;</span>, db.Integer, db.ForeignKey(<span class="string">&quot;tb_pemission.id&quot;</span>), nullable=<span class="literal">False</span>)</span><br><span class="line">)</span><br><span class="line">tb_pemission_operation = db.Table(</span><br><span class="line">    <span class="string">&#x27;tb_pemission_operation&#x27;</span>,</span><br><span class="line">    db.Column(<span class="string">&quot;pemission_id&quot;</span>, db.Integer, db.ForeignKey(<span class="string">&quot;tb_pemission.id&quot;</span>), nullable=<span class="literal">False</span>),</span><br><span class="line">    db.Column(<span class="string">&quot;operation_id&quot;</span>, db.Integer, db.ForeignKey(<span class="string">&quot;tb_operation.id&quot;</span>), nullable=<span class="literal">False</span>),</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Operation</span>(db.Model, CRUDMixin):</span><br><span class="line">    __tablename__ = <span class="string">&quot;tb_operation&quot;</span></span><br><span class="line">    name = db.Column(db.String(<span class="number">50</span>))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__str__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.name</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Pemission</span>(db.Model, CRUDMixin):</span><br><span class="line">    __tablename__ = <span class="string">&quot;tb_pemission&quot;</span></span><br><span class="line">    name = db.Column(db.String(<span class="number">50</span>))</span><br><span class="line">    tb_pemission_operation = db.relationship(Operation, secondary=tb_pemission_operation,</span><br><span class="line">                                             backref=db.backref(<span class="string">&#x27;pemissions&#x27;</span>, lazy=<span class="string">&#x27;dynamic&#x27;</span>))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__str__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.name</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Menu</span>(db.Model, CRUDMixin):</span><br><span class="line">    __tablename__ = <span class="string">&#x27;tb_menu&#x27;</span></span><br><span class="line">    name = db.Column(db.String(<span class="number">50</span>))</span><br><span class="line">    tb_menu_pemission = db.relationship(Pemission, secondary=tb_menu_pemission,</span><br><span class="line">                                        backref=db.backref(<span class="string">&#x27;menus&#x27;</span>, lazy=<span class="string">&#x27;dynamic&#x27;</span>))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__str__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.name</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Role</span>(db.Model, CRUDMixin):</span><br><span class="line">    __tablename__ = <span class="string">&quot;tb_role&quot;</span></span><br><span class="line">    name = db.Column(db.String(<span class="number">50</span>))</span><br><span class="line">    tb_role_menu = db.relationship(Menu, secondary=tb_role_menu,</span><br><span class="line">                                   backref=db.backref(<span class="string">&#x27;roles&#x27;</span>, lazy=<span class="string">&#x27;dynamic&#x27;</span>))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__str__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.name</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">User</span>(db.Model, CRUDMixin):</span><br><span class="line">    __tablename__ = <span class="string">&quot;tb_user&quot;</span></span><br><span class="line">    name = db.Column(db.String(<span class="number">50</span>))</span><br><span class="line">    tb_user_role = db.relationship(Role, secondary=tb_user_role,</span><br><span class="line">                                   backref=db.backref(<span class="string">&#x27;users&#x27;</span>, lazy=<span class="string">&#x27;dynamic&#x27;</span>))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__str__</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.name</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">admin.add_view(ModelView(User, db.session))</span><br><span class="line">admin.add_view(ModelView(Role, db.session))</span><br><span class="line">admin.add_view(ModelView(Menu, db.session))</span><br><span class="line">admin.add_view(ModelView(Pemission, db.session))</span><br><span class="line">admin.add_view(ModelView(Operation, db.session))</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> functools <span class="keyword">import</span> wraps</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">UserLoginError</span>(<span class="params">BaseException</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, message, status</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__(message, status)</span><br><span class="line">        <span class="variable language_">self</span>.message = message</span><br><span class="line">        <span class="variable language_">self</span>.status = status</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.before_request</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">before_request</span>():</span><br><span class="line">    g.user = User.get_ins_by_id(<span class="number">1</span>)</span><br><span class="line">    <span class="built_in">print</span>(g.user.name)</span><br><span class="line">    g.user_roles = g.user.tb_user_role</span><br><span class="line">    <span class="built_in">print</span>(g.user_roles)</span><br><span class="line">    g.user_role_menus = []</span><br><span class="line">    <span class="keyword">for</span> role <span class="keyword">in</span> g.user_roles:</span><br><span class="line">        g.user_role_menus.extend(role.tb_role_menu)</span><br><span class="line">    g.user_role_menus = <span class="built_in">list</span>(<span class="built_in">set</span>(g.user_role_menus))</span><br><span class="line">    <span class="built_in">print</span>(g.user_role_menus)</span><br><span class="line">    <span class="comment"># g.user_role_menu_pemissions = []</span></span><br><span class="line">    <span class="comment"># for menu in g.user_role_menus:</span></span><br><span class="line">    <span class="comment">#     g.user_role_menu_pemissions.extend(menu.tb_menu_pemission)</span></span><br><span class="line">    <span class="comment"># g.user_role_menu_pemissions = list(set(g.user_role_menu_pemissions))</span></span><br><span class="line">    <span class="comment"># print(g.user_role_menu_pemissions)</span></span><br><span class="line">    <span class="comment"># g.user_role_menu_pemission_operation = &#123;&#125;</span></span><br><span class="line">    <span class="comment"># for pemission in g.user_role_menu_pemissions:</span></span><br><span class="line">    <span class="comment">#     g.user_role_menu_pemission_operation[pemission.name] = [operation.name for operation in</span></span><br><span class="line">    <span class="comment">#                                                             pemission.tb_pemission_operation]</span></span><br><span class="line">    <span class="comment"># print(g.user_role_menu_pemissions)</span></span><br><span class="line">    g.user_role_menu_pemissions = &#123;&#125;</span><br><span class="line">    <span class="keyword">for</span> menu <span class="keyword">in</span> g.user_role_menus:</span><br><span class="line">        <span class="keyword">for</span> pemission <span class="keyword">in</span> menu.tb_menu_pemission:</span><br><span class="line">            g.user_role_menu_pemissions[menu.name] = &#123;pemission.name: [operation.name <span class="keyword">for</span> operation <span class="keyword">in</span></span><br><span class="line">                                                                       pemission.tb_pemission_operation]&#125;</span><br><span class="line">    <span class="built_in">print</span>(g.user_role_menu_pemissions)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.after_request</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">after_request</span>(<span class="params">reponse</span>):</span><br><span class="line">    <span class="keyword">return</span> reponse</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">login_require</span>(<span class="params">fn</span>):</span><br><span class="line"><span class="meta">    @wraps(<span class="params">fn</span>)</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">_dec</span>(<span class="params">*args, **kwargs</span>):</span><br><span class="line">        <span class="keyword">if</span> g.user.name == <span class="string">&quot;小黄&quot;</span>:</span><br><span class="line">            <span class="keyword">return</span> fn(*args, **kwargs)</span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;滚回去登录！！！&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> _dec</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">user_operation_menu</span>(<span class="params">menu_name, pemission_name, operation_name</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">user_operation</span>(<span class="params">fn</span>):</span><br><span class="line"><span class="meta">        @wraps(<span class="params">fn</span>)</span></span><br><span class="line">        <span class="keyword">def</span> <span class="title function_">_dec</span>(<span class="params">*args, **kwargs</span>):</span><br><span class="line">            <span class="keyword">if</span> menu_name <span class="keyword">in</span> g.user_role_menu_pemissions:</span><br><span class="line">                <span class="keyword">if</span> pemission_name <span class="keyword">in</span> g.user_role_menu_pemissions[menu_name]:</span><br><span class="line">                    <span class="keyword">if</span> operation_name <span class="keyword">in</span> g.user_role_menu_pemissions[menu_name][pemission_name]:</span><br><span class="line">                        <span class="keyword">return</span> fn(*args, **kwargs)</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;没权限，玩**呢！！！&quot;</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> _dec</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> user_operation</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route(<span class="params"><span class="string">&#x27;/&#x27;</span></span>)</span></span><br><span class="line"><span class="meta">@login_require</span></span><br><span class="line"><span class="meta">@user_operation_menu(<span class="params"><span class="string">&quot;用户管理&quot;</span>, <span class="string">&quot;用户管理的权限&quot;</span>, <span class="string">&quot;查看&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">hello_world</span>():</span><br><span class="line">    <span class="keyword">return</span> <span class="string">&#x27;Hello World!&#x27;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    db.create_all()</span><br><span class="line">    app.run(debug=<span class="literal">True</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h3 id="require-文件"><a href="#require-文件" class="headerlink" title="require 文件"></a>require 文件</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Flask            1.1.2</span><br><span class="line">Flask-Admin      1.5.6</span><br><span class="line">Flask-Curd-Db    1.0.0</span><br><span class="line">Flask-SQLAlchemy 2.4.4</span><br></pre></td></tr></table></figure>

<h3 id="设计与理解"><a href="#设计与理解" class="headerlink" title="设计与理解"></a>设计与理解</h3><h4 id="本案例中"><a href="#本案例中" class="headerlink" title="本案例中"></a>本案例中</h4><p>到此 发现设计一个颗粒到按钮还是比较容易，但是如何使用才是关键，例如上面的这种使用方法就很繁琐。</p>
<p>如何验证到 【查看，删除，增加，修改】这一步的权限，需要进一步的思考更好的做法。</p>
<p>可以在operation表中加一个唯一key，或者 从菜单-权限-操作 这个 形成一个唯一key，或者在视图中来操作一个。</p>
<h4 id="以前看Lin-CMS-Flask学习到的"><a href="#以前看Lin-CMS-Flask学习到的" class="headerlink" title="以前看Lin-CMS-Flask学习到的"></a>以前看Lin-CMS-Flask学习到的</h4><p>对flask源码有一定了解的，可以知道 flask的 view_functions ，并且这些view_functions的function name是唯一的，然后利用一定的约束标准，对接口进行划分。直接在程序启动时，对函数名称进行收集，然后权限操作直接对应着函数名称。</p>
<h3 id="参考："><a href="#参考：" class="headerlink" title="参考："></a>参考：</h3><ol>
<li><a href="https://juejin.im/entry/6844903540884766733">https://juejin.im/entry/6844903540884766733</a></li>
<li><a href="https://github.com/TaleLin/lin-cms-flask">https://github.com/TaleLin/lin-cms-flask</a></li>
</ol>
]]></content>
      <categories>
        <category>代码来了</category>
      </categories>
      <tags>
        <tag>flask</tag>
        <tag>rbac</tag>
      </tags>
  </entry>
  <entry>
    <title>github actions 自动部署 hexo</title>
    <url>/post/github-actions-%E8%87%AA%E5%8A%A8%E9%83%A8%E7%BD%B2-hexo.html</url>
    <content><![CDATA[<p>github actions 自动部署 hexo,感觉还是有点问题。有很多方法可以解决，但是我这里就不折腾了。</p>
<span id="more"></span>

<p>我自己的提问：<a href="https://github.com/JamesIves/github-pages-deploy-action/issues/389">https://github.com/JamesIves/github-pages-deploy-action/issues/389</a></p>
<h3 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h3><p>hexo 安装 需要全局，但是没有权限无法安装。可以利用docker安装 ，解决权限问题。</p>
<p>但是新版本的 action，作者去掉了docker，鼓励本地直接构建，然后直接同步。</p>
<p><strong>而且同步到其他仓库的功能在新的版本里才可以使用</strong></p>
<p>最后我已使用了本地构建这个方案。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">name:</span> <span class="string">buildAndDeploy</span></span><br><span class="line"><span class="attr">on:</span> [<span class="string">push</span>]</span><br><span class="line"><span class="attr">jobs:</span></span><br><span class="line">  <span class="attr">build-and-deploy:</span></span><br><span class="line">    <span class="attr">runs-on:</span> <span class="string">ubuntu-latest</span></span><br><span class="line">    <span class="attr">steps:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">checkout</span></span><br><span class="line">      <span class="attr">uses:</span> <span class="string">actions/checkout@v2</span></span><br><span class="line">      <span class="attr">with:</span></span><br><span class="line">          <span class="attr">persist-credentials:</span> <span class="literal">false</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Deploy</span></span><br><span class="line">      <span class="attr">uses:</span> <span class="string">JamesIves/github-pages-deploy-action@3.5.9</span></span><br><span class="line">      <span class="attr">with:</span></span><br><span class="line">        <span class="attr">ACCESS_TOKEN:</span> <span class="string">$&#123;&#123;</span> <span class="string">secrets.ACCESS_TOKEN</span> <span class="string">&#125;&#125;</span></span><br><span class="line">        <span class="attr">REPOSITORY_NAME:</span> <span class="string">libaibuaidufu/libaibuaidufu.github.io</span></span><br><span class="line">        <span class="attr">BRANCH:</span> <span class="string">master</span> </span><br><span class="line">        <span class="attr">BASE_BRANCH:</span> <span class="string">master</span> </span><br><span class="line">        <span class="attr">FOLDER:</span> <span class="string">public</span> </span><br></pre></td></tr></table></figure>

<h3 id="骚操作"><a href="#骚操作" class="headerlink" title="骚操作"></a>骚操作</h3><p>中途我也想了骚操作，例如用以前的版本，来构建，再用新的版本来部署。最后出现了权限问题。就没有继续了。</p>
<p><strong>因为同步到其他仓库的功能在新的版本里才可以使用</strong></p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">name:</span> <span class="string">buildAndDeploy</span></span><br><span class="line"><span class="attr">on:</span> [<span class="string">push</span>]</span><br><span class="line"><span class="attr">jobs:</span></span><br><span class="line">  <span class="attr">build-and-deploy:</span></span><br><span class="line">    <span class="attr">runs-on:</span> <span class="string">ubuntu-latest</span></span><br><span class="line">    <span class="attr">steps:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">checkout</span></span><br><span class="line">      <span class="attr">uses:</span> <span class="string">actions/checkout@v2</span></span><br><span class="line">      <span class="attr">with:</span></span><br><span class="line">          <span class="attr">persist-credentials:</span> <span class="literal">false</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">build</span></span><br><span class="line">      <span class="attr">uses:</span> <span class="string">JamesIves/github-pages-deploy-action@releases/v2</span></span><br><span class="line">      <span class="attr">env:</span></span><br><span class="line">        <span class="attr">ACCESS_TOKEN:</span> <span class="string">$&#123;&#123;</span> <span class="string">secrets.ACCESS_TOKEN</span> <span class="string">&#125;&#125;</span></span><br><span class="line">        <span class="attr">REPOSITORY_NAME:</span> <span class="string">libaibuaidufu/libaibuaidufu/private-blog-deploy-actions</span></span><br><span class="line">        <span class="attr">BASE_BRANCH:</span> <span class="string">master</span></span><br><span class="line">        <span class="attr">BRANCH:</span> <span class="string">gh-pages</span></span><br><span class="line">        <span class="attr">FOLDER:</span> <span class="string">public</span></span><br><span class="line">        <span class="attr">BUILD_SCRIPT:</span>  <span class="string">npm</span> <span class="string">install</span> <span class="string">hexo-cli</span> <span class="string">-g</span> <span class="string">&amp;&amp;</span> <span class="string">npm</span> <span class="string">install</span> <span class="string">&amp;&amp;</span> <span class="string">hexo</span> <span class="string">clean</span> <span class="string">&amp;&amp;</span> <span class="string">hexo</span> <span class="string">g</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">Deploy</span></span><br><span class="line">      <span class="attr">uses:</span> <span class="string">JamesIves/github-pages-deploy-action@3.5.9</span></span><br><span class="line">      <span class="attr">with:</span></span><br><span class="line">        <span class="attr">ACCESS_TOKEN:</span> <span class="string">$&#123;&#123;</span> <span class="string">secrets.ACCESS_TOKEN</span> <span class="string">&#125;&#125;</span></span><br><span class="line">        <span class="attr">REPOSITORY_NAME:</span> <span class="string">libaibuaidufu/libaibuaidufu.github.io</span></span><br><span class="line">        <span class="attr">BRANCH:</span> <span class="string">master</span> </span><br><span class="line">        <span class="attr">BASE_BRANCH:</span> <span class="string">master</span> </span><br><span class="line">        <span class="attr">FOLDER:</span> <span class="string">public</span> </span><br></pre></td></tr></table></figure>

<h3 id="windows-bat-命令"><a href="#windows-bat-命令" class="headerlink" title="windows bat 命令"></a>windows bat 命令</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cls</span><br><span class="line">@<span class="built_in">echo</span> off</span><br><span class="line"><span class="built_in">echo</span> 清理中。。。</span><br><span class="line">call hexo clean </span><br><span class="line"><span class="built_in">echo</span> 重新构建。。。</span><br><span class="line">call hexo g </span><br><span class="line"><span class="built_in">echo</span> 添加新的修改。。。</span><br><span class="line">call git add . </span><br><span class="line"><span class="built_in">set</span> /p commit_message=提交内容：</span><br><span class="line"><span class="built_in">echo</span> 提交新的修改</span><br><span class="line">call git commit -m %commit_message%</span><br><span class="line"><span class="built_in">echo</span> 推送新的修改</span><br><span class="line">call git push -u origin master</span><br><span class="line"><span class="built_in">echo</span> 已完成</span><br><span class="line">pause</span><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>博客搭建</category>
      </categories>
      <tags>
        <tag>hexo</tag>
        <tag>github-action</tag>
      </tags>
  </entry>
  <entry>
    <title>flask源码分析(一)</title>
    <url>/post/flask%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90-%E4%B8%80.html</url>
    <content><![CDATA[<p>对于flask源码，已经看过几次，每次都是看了忘，忘了看，这次就好生记录一下。也算是认真学习。</p>
<span id="more"></span>

<h2 id="Flask-class"><a href="#Flask-class" class="headerlink" title="Flask class"></a>Flask class</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask</span><br><span class="line">app = Flask(__name__)</span><br></pre></td></tr></table></figure>

<p>一般 我们都是这样开头的，那我们就 从最开始的 <code>__name__</code> 开始介绍。</p>
<h3 id="Flask-的一个参数-import-name"><a href="#Flask-的一个参数-import-name" class="headerlink" title="Flask 的一个参数 import_name"></a>Flask 的一个参数 import_name</h3><p>通过进入源码了解到：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">admonition:: About the First Parameter</span><br><span class="line">        The idea of the first parameter is to give Flask an idea of what</span><br><span class="line">        belongs to your application.  This name is used to find resources</span><br><span class="line">        on the filesystem, can be used by extensions to improve debugging</span><br><span class="line">        information and a lot more.</span><br><span class="line"></span><br><span class="line">        So it&#x27;s important what you provide there.  If you are using a single</span><br><span class="line">        module, `__name__` is always the correct value.  If you however are</span><br><span class="line">        using a package, it&#x27;s usually recommended to hardcode the name of</span><br><span class="line">        your package there.</span><br><span class="line"></span><br><span class="line">        For example if your application is defined in :file:`yourapplication/app.py`</span><br><span class="line">        you should create it with one of the two versions below::</span><br><span class="line"></span><br><span class="line">            app = Flask(&#x27;yourapplication&#x27;)</span><br><span class="line">            app = Flask(__name__.split(&#x27;.&#x27;)[0])</span><br><span class="line"></span><br><span class="line">        Why is that?  The application will work even with `__name__`, thanks</span><br><span class="line">        to how resources are looked up.  However it will make debugging more</span><br><span class="line">        painful.  Certain extensions can make assumptions based on the</span><br><span class="line">        import name of your application.  For example the Flask-SQLAlchemy</span><br><span class="line">        extension will look for the code in your application that triggered</span><br><span class="line">        an SQL query in debug mode.  If the import name is not properly set</span><br><span class="line">        up, that debugging information is lost.  (For example it would only</span><br><span class="line">        pick up SQL queries in `yourapplication.app` and not</span><br><span class="line">        `yourapplication.views.frontend`)</span><br><span class="line">##### 谷歌翻译</span><br><span class="line">告诫：：关于第一个参数</span><br><span class="line"></span><br><span class="line">        第一个参数的想法是让Flask了解属于您的应用程序。此名称用于查找资源在文件系统上，扩展可以使用它来改进调试信息等等。</span><br><span class="line"></span><br><span class="line">        因此，您在此处提供的内容很重要。如果您使用单个在模块中，“ __ name__”始终是正确的值。如果你是使用软件包，通常建议对代码名称进行硬编码您的包裹在那里。</span><br><span class="line"></span><br><span class="line">        例如，如果您的应用程序是在：file：`yourapplication / app.py`中定义的</span><br><span class="line">		您应该使用以下两个版本之一创建它：：</span><br><span class="line">            app = Flask(&#x27;yourapplication&#x27;)</span><br><span class="line">            app = Flask(__name__.split(&#x27;.&#x27;)[0])</span><br><span class="line"></span><br><span class="line">        这是为什么？由于如何查找资源，该应用程序甚至可以与“ __name__”一起使用。但是，这会使调试更加痛苦。某些扩展可以根据您的应用程序的导入名称进行假设。例如，Flask-SQLAlchemy扩展将在您的应用程序中查找在调试模式下触发SQL查询的代码。如果导入名称设置不正确，则该调试信息将丢失。 （例如，它将仅在`yourapplication.app`中而不是`yourapplication.views.frontend`中拾取SQL查询。）</span><br></pre></td></tr></table></figure>

<h4 id="第一个参数-单模块"><a href="#第一个参数-单模块" class="headerlink" title="第一个参数 单模块"></a>第一个参数 单模块</h4><p>如果只是使用单模块，那么使用<code>__name__</code> 总是对的。</p>
<p>什么是单模块就是一个文件搞定所有。</p>
<h4 id="第一个参数-多内容"><a href="#第一个参数-多内容" class="headerlink" title="第一个参数 多内容"></a>第一个参数 多内容</h4><p>目录结构是：<br>├─.idea<br>│  └─inspectionProfiles<br>├─to_the_world_package<br>│  ├─static<br>│  └─templates<br>└─<strong>pycache</strong></p>
<p>file: <code>to_the_world_package/to-the-world.py</code></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask, render_template</span><br><span class="line"><span class="keyword">from</span> flask.helpers <span class="keyword">import</span> get_root_path</span><br><span class="line"><span class="keyword">from</span> .hello <span class="keyword">import</span> data</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(__name__)</span><br><span class="line">app = Flask(<span class="string">&quot;to_the_world_package&quot;</span>)</span><br><span class="line"><span class="comment"># app = Flask(__name__.split(&quot;.&quot;)[0])</span></span><br><span class="line"><span class="comment"># app = Flask(__name__)</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(app.import_name)</span><br><span class="line"><span class="built_in">print</span>(app.template_folder)</span><br><span class="line"><span class="built_in">print</span>(app.root_path)</span><br><span class="line">import_name = <span class="string">&quot;to_the_world_package&quot;</span></span><br><span class="line">mod = sys.modules.get(import_name)</span><br><span class="line"><span class="built_in">print</span>(mod.__file__)</span><br><span class="line"><span class="built_in">print</span>(os.path.abspath(mod.__file__))</span><br><span class="line"><span class="built_in">print</span>(os.path.dirname(os.path.abspath(mod.__file__)))</span><br><span class="line"></span><br><span class="line">import_name = <span class="string">&quot;to_the_world_package.to-the-world&quot;</span></span><br><span class="line">mod = sys.modules.get(import_name)</span><br><span class="line"><span class="built_in">print</span>(mod.__file__)</span><br><span class="line"><span class="built_in">print</span>(os.path.abspath(mod.__file__))</span><br><span class="line"><span class="built_in">print</span>(os.path.dirname(os.path.abspath(mod.__file__)))</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route(<span class="params"><span class="string">&quot;/&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">index</span>():</span><br><span class="line">    <span class="built_in">print</span>(data)</span><br><span class="line">    <span class="keyword">return</span> render_template(<span class="string">&quot;index.html&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    <span class="comment"># app.run(debug=True, host=&#x27;0.0.0.0&#x27;, port=8080)</span></span><br><span class="line">    app.run(debug=<span class="literal">True</span>, port=<span class="number">8080</span>)</span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">to_the_world_package.to-the-world</span></span><br><span class="line"><span class="string">to_the_world_package.to-the-world</span></span><br><span class="line"><span class="string">templates</span></span><br><span class="line"><span class="string">C:\Users\libai\dfk-common\日常\flask-rbac\to_the_world_package</span></span><br><span class="line"><span class="string"># import_name = &quot;to_the_world_package&quot;</span></span><br><span class="line"><span class="string">C:\Users\libai\dfk-common\日常\flask-rbac\to_the_world_package\__init__.py</span></span><br><span class="line"><span class="string">C:\Users\libai\dfk-common\日常\flask-rbac\to_the_world_package\__init__.py</span></span><br><span class="line"><span class="string">C:\Users\libai\dfk-common\日常\flask-rbac\to_the_world_package</span></span><br><span class="line"><span class="string"># import_name = &quot;to_the_world_package.to-the-world&quot;</span></span><br><span class="line"><span class="string">C:\Users\libai\dfk-common\日常\flask-rbac\to_the_world_package\to-the-world.py</span></span><br><span class="line"><span class="string">C:\Users\libai\dfk-common\日常\flask-rbac\to_the_world_package\to-the-world.py</span></span><br><span class="line"><span class="string">C:\Users\libai\dfk-common\日常\flask-rbac\to_the_world_package</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br></pre></td></tr></table></figure>

<p>从这里来看 无论使用  <code>__name__</code>  还是 packeage_name 还是 <code>__name__.split(&quot;.&quot;)[0]</code> 都可以，因为</p>
<p><code>get_root_path</code>方法 会把root_path给改回来。</p>
<p><strong>Flask 的第一个参数 主要是 为了 让Flask找文件位置，就是找到所谓的root_path，然后就可以找到root_path下的文件，例如 static 或者 templates,如果root_path位置不对，就会无法使用 static 或者 templates，因此可以自己设定位置,设定 static_folder，template_folder，或者直接设置root_path</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params"></span></span><br><span class="line"><span class="params">    self,</span></span><br><span class="line"><span class="params">    import_name,</span></span><br><span class="line"><span class="params">    static_url_path=<span class="literal">None</span>,</span></span><br><span class="line"><span class="params">    static_folder=<span class="string">&quot;static&quot;</span>,</span></span><br><span class="line"><span class="params">    static_host=<span class="literal">None</span>,</span></span><br><span class="line"><span class="params">    host_matching=<span class="literal">False</span>,</span></span><br><span class="line"><span class="params">    subdomain_matching=<span class="literal">False</span>,</span></span><br><span class="line"><span class="params">    template_folder=<span class="string">&quot;templates&quot;</span>,</span></span><br><span class="line"><span class="params">    instance_path=<span class="literal">None</span>,</span></span><br><span class="line"><span class="params">    instance_relative_config=<span class="literal">False</span>,</span></span><br><span class="line"><span class="params">    root_path=<span class="literal">None</span>,</span></span><br><span class="line"><span class="params"></span>):</span><br></pre></td></tr></table></figure>

<h4 id="这里写一个真实的"><a href="#这里写一个真实的" class="headerlink" title="这里写一个真实的"></a>这里写一个真实的</h4><p>目录精简下来：</p>
<p>app:</p>
<p>├─.idea<br>│  └─inspectionProfiles<br>├─config<br>│  └─config.py<br>├─controllers<br>│  ├─app<br>│  ├─impl<br>│  ├─web<br>│  └─wx<br>├─log<br>├─models<br>├─static<br>│  └─dept_img<br>└─Main.py</p>
<p>我们在config目录下的 config.py中进行了配置，但是在主目录下Main.py进行注册启动。</p>
<p>因此 我们config下的config.py中如何写 Flask呢？</p>
<p>我去看看了以前的</p>
<h5 id="以前我们是设定template-folder-和-static-folder"><a href="#以前我们是设定template-folder-和-static-folder" class="headerlink" title="以前我们是设定template_folder 和 static_folder"></a>以前我们是设定template_folder 和 static_folder</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">app = Flask(__name__, template_folder=<span class="string">&#x27;../templates&#x27;</span>, 和 static_folder=<span class="string">&quot;../static&quot;</span>, static_url_path=<span class="string">&quot;/static&quot;</span>)</span><br></pre></td></tr></table></figure>

<h5 id="现在我们知道第一个参数的意义，那我们可以直接这样写就可以了"><a href="#现在我们知道第一个参数的意义，那我们可以直接这样写就可以了" class="headerlink" title="现在我们知道第一个参数的意义，那我们可以直接这样写就可以了"></a>现在我们知道第一个参数的意义，那我们可以直接这样写就可以了</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(__name__)</span><br><span class="line">app = Flask(<span class="string">&quot;app&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(app.root_path)</span><br><span class="line"></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">config.config</span></span><br><span class="line"><span class="string">I:\libaibuaidufu\app</span></span><br><span class="line"><span class="string">此时的app，root_path 就是对的。</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>flask</category>
      </categories>
      <tags>
        <tag>flask</tag>
      </tags>
  </entry>
  <entry>
    <title>github、gitlab、ssh密钥登录</title>
    <url>/post/github%E3%80%81gitlab%E3%80%81ssh%E5%AF%86%E9%92%A5%E7%99%BB%E5%BD%95.html</url>
    <content><![CDATA[<p>一直对ssh密钥登录半知半解，以前都是跟着教程自己走一遍，每次都去查看，这次就认真记录一下，可能遇到的情况。github多账号登录问题，github、gitlab多网站账户密钥问题以及ssh密钥登录问题。</p>
<span id="more"></span>

<h2 id="gitlab、gitlab"><a href="#gitlab、gitlab" class="headerlink" title="gitlab、gitlab"></a>gitlab、gitlab</h2><h3 id="生成密钥"><a href="#生成密钥" class="headerlink" title="生成密钥"></a>生成密钥</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t ed25519 -C <span class="string">&quot;your_email@example.com&quot;</span></span><br><span class="line">or</span><br><span class="line">ssh-keygen -t rsa -b 4096 -C <span class="string">&quot;your_email@example.com&quot;</span></span><br><span class="line"><span class="comment">#####输出 </span></span><br><span class="line">Generating public/private ed25519 key pair.</span><br><span class="line">Enter file <span class="keyword">in</span> <span class="built_in">which</span> to save the key (/c/Users/libai/.ssh/id_ed25519): <span class="comment"># 文件存储位置</span></span><br><span class="line">Enter passphrase (empty <span class="keyword">for</span> no passphrase): <span class="comment"># 输入密钥 密码 可留空</span></span><br><span class="line">Enter same passphrase again: <span class="comment"># 确认密码</span></span><br><span class="line">Your identification has been saved <span class="keyword">in</span> /c/Users/libai/.ssh/id_ed25519 <span class="comment"># 储存位置 私钥</span></span><br><span class="line">Your public key has been saved <span class="keyword">in</span> /c/Users/libai/.ssh/id_ed25519.pub <span class="comment"># 存储位置 公钥</span></span><br><span class="line">The key fingerprint is:</span><br><span class="line">SHA256:JiEaUc9F3/yd6WsU2nAnl5PHTcIXyS5K/4RnIV+Cx2Y dufukai@zhengzihui.com</span><br><span class="line">The key<span class="string">&#x27;s randomart image is:</span></span><br><span class="line"><span class="string">+--[ED25519 256]--+</span></span><br><span class="line"><span class="string">|  ...  .o    ...o|</span></span><br><span class="line"><span class="string">|   . o . . o  ooo|</span></span><br><span class="line"><span class="string">|  . . +   . oo.=+|</span></span><br><span class="line"><span class="string">|   o . .   ..=EXX|</span></span><br><span class="line"><span class="string">|  .   . S . o+@*O|</span></span><br><span class="line"><span class="string">|       o   . +.B |</span></span><br><span class="line"><span class="string">|              *. |</span></span><br><span class="line"><span class="string">|               o.|</span></span><br><span class="line"><span class="string">|              .. |</span></span><br><span class="line"><span class="string">+----[SHA256]-----+</span></span><br><span class="line"><span class="string"></span></span><br></pre></td></tr></table></figure>

<h3 id="复制公钥到-github、gitlab"><a href="#复制公钥到-github、gitlab" class="headerlink" title="复制公钥到 github、gitlab"></a>复制公钥到 github、gitlab</h3><blockquote>
<p><strong>id_ed25519</strong> 为私钥</p>
<p>id_ed25519.pub 为公钥</p>
</blockquote>
<p>打开公钥id_ed25519.pub，复制内容 到github或者 gitlab中。</p>
<h3 id="测试ssh连接"><a href="#测试ssh连接" class="headerlink" title="测试ssh连接"></a>测试ssh连接</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh -T git@github.com</span><br><span class="line">or</span><br><span class="line">ssh -T git@192.168.5.130 <span class="comment"># 自建gitlab地址</span></span><br><span class="line"></span><br><span class="line">您可能会看到类似如下的警告：</span><br><span class="line"></span><br><span class="line">&gt; The authenticity of host <span class="string">&#x27;github.com (IP ADDRESS)&#x27;</span> can<span class="string">&#x27;t be established.</span></span><br><span class="line"><span class="string">&gt; RSA key fingerprint is SHA256:nThbg6kXUpJWGl7E1IGOCspRomTxdCARLviKw6E5SY8.</span></span><br><span class="line"><span class="string">&gt; Are you sure you want to continue connecting (yes/no)?</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">输入 yes</span></span><br><span class="line"><span class="string">就可以了</span></span><br></pre></td></tr></table></figure>

<h3 id="github、gitlab不同邮箱（同样适用多个github不同邮箱）"><a href="#github、gitlab不同邮箱（同样适用多个github不同邮箱）" class="headerlink" title="github、gitlab不同邮箱（同样适用多个github不同邮箱）"></a>github、gitlab不同邮箱（同样适用多个github不同邮箱）</h3><p>上面生成密钥 -C 指定邮箱 ，如果你有多个github或者gitlab帐号，且使用不同邮箱。需要修改公钥和私钥名称配置<code>config</code>。</p>
<p>当创建第一个github的密钥过后，修改公钥和私钥为<code>github_id_ed25519</code>、公钥为<code>github_id_ed25519.pub</code>，当然加上邮箱更好。<code>xx@gmail_github_id_ed25519</code></p>
<p>在创建第二个gitlab。</p>
<p>然后你就有了</p>
<ol>
<li><code>github_id_ed25519</code></li>
<li><code>github_id_ed25519.pub</code></li>
<li><code>gitlab_id_ed25519</code></li>
<li><code>gitlab_id_ed25519.pub</code></li>
</ol>
<p>然后windows在<code>C:\Users\用户名\.ssh</code>，linux在 <code>~/.ssh</code>下。</p>
<p>配置<code>config</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 添加config配置文件</span><br><span class="line"># vi ~/.ssh/config</span><br><span class="line"></span><br><span class="line"># 文件内容如下：</span><br><span class="line"># gitlab</span><br><span class="line"># github</span><br><span class="line">Host github-libaibuaidufu</span><br><span class="line">    HostName github.com</span><br><span class="line">    User libaibuaidufu</span><br><span class="line">    PreferredAuthentications publickey</span><br><span class="line">    IdentityFile ~/.ssh/github_id_ed25519</span><br><span class="line"></span><br><span class="line">Host github-dfkai</span><br><span class="line">    HostName github.com</span><br><span class="line">    User dfkai</span><br><span class="line">    PreferredAuthentications publickey</span><br><span class="line">    IdentityFile ~/.ssh/xxx@qq.com_github_id_ed25519</span><br><span class="line"></span><br><span class="line">Host 192.168.5.130</span><br><span class="line">    HostName 192.168.5.130</span><br><span class="line">    PreferredAuthentications publickey</span><br><span class="line">    IdentityFile ~/.ssh/gitlab_id_ed25519</span><br><span class="line"></span><br><span class="line"># 配置文件参数</span><br><span class="line"># Host : Host可以看作是一个你要识别的模式，对识别的模式，进行配置对应的的主机名和ssh文件</span><br><span class="line"># HostName : 要登录主机的主机名</span><br><span class="line"># User : 登录名</span><br><span class="line"># IdentityFile : 指明上面User对应的identityFile路径  私钥位置</span><br><span class="line"># Port : 指定端口 默认 22</span><br></pre></td></tr></table></figure>

<p>这样就可以在不同的账户之间都是用到ssh了。</p>
<p>测试连接：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh -T git@host 上面的host</span><br><span class="line"></span><br><span class="line">ssh -T git@github-libaibuaidufu</span><br><span class="line"></span><br><span class="line">ssh -T git@github-dfkai</span><br><span class="line"></span><br><span class="line">ssh -T git@192.168.5.130</span><br></pre></td></tr></table></figure>

<h2 id="服务器SSH连接"><a href="#服务器SSH连接" class="headerlink" title="服务器SSH连接"></a>服务器SSH连接</h2><h3 id="客户端创建密钥"><a href="#客户端创建密钥" class="headerlink" title="客户端创建密钥"></a>客户端创建密钥</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa </span><br><span class="line">一顿回车，如果需要修改配置请看上面的介绍</span><br><span class="line"></span><br><span class="line">生成id_rsa私钥，id_rsa.pub 公钥</span><br></pre></td></tr></table></figure>

<h3 id="复制公钥内容到-服务器"><a href="#复制公钥内容到-服务器" class="headerlink" title="复制公钥内容到 服务器"></a>复制公钥内容到 服务器</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> ~/.ssh</span><br><span class="line"></span><br><span class="line">vi authorized_keys</span><br><span class="line"></span><br><span class="line">粘贴公钥内容到里面去</span><br><span class="line"></span><br><span class="line">或者</span><br><span class="line"></span><br><span class="line">复制公钥文件到 服务器 ~/.ssh</span><br><span class="line"><span class="built_in">cd</span> ~/.ssh</span><br><span class="line"></span><br><span class="line"><span class="built_in">cat</span> id_rsa.pub &gt;&gt; authorized_keys</span><br><span class="line"></span><br><span class="line">反正最后一定要复制进去，不管用什么方法</span><br><span class="line"></span><br><span class="line">设置权限 1可执行 2 可写  4 可读</span><br><span class="line"><span class="built_in">chmod</span> 600 authorized_keys <span class="comment"># 可读可写</span></span><br><span class="line"><span class="built_in">chmod</span> 700 ~/.ssh <span class="comment"># 可读可写可执行</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h3 id="配置ssh-打开密钥登录"><a href="#配置ssh-打开密钥登录" class="headerlink" title="配置ssh,打开密钥登录"></a>配置ssh,打开密钥登录</h3><p>编辑<code>/etc/ssh/sshd_config </code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">RSAAuthentication yes</span><br><span class="line">PubkeyAuthentication yes</span><br></pre></td></tr></table></figure>

<p>另外，请留意 root 用户能否通过 SSH 登录：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PermitRootLogin yes</span><br></pre></td></tr></table></figure>

<p><strong>注意：当你完成全部设置，并以密钥方式登录成功后，再禁用密码登录：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PasswordAuthentication no</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>最后，重启 SSH 服务：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@host .ssh]$ service sshd restart</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h3 id="连接服务器"><a href="#连接服务器" class="headerlink" title="连接服务器"></a>连接服务器</h3><p>使用xshell，输入服务器，选择密钥登录，选择你客户机上的私钥<code>id_rsa</code>,如果设置了私钥密码(看上面介绍),请输入私钥密码，没有就留空。则完成登录。</p>
<p>或者 </p>
<p>输入命令连接</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh 192.168.5.106</span><br><span class="line">xshell会让你输入用户名</span><br><span class="line">然后让你输入密码 或者选择密钥</span><br><span class="line">则完成登录 </span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>能不能跟简单一点呢。</p>
<p>当然可以，例如上面我们配置的<code>config</code>一样</p>
<p>在客户端加上以上配置。<code>~/.ssh/config</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Host 192.168.5.106</span><br><span class="line">    HostName 192.168.5.106</span><br><span class="line">    PreferredAuthentications publickey</span><br><span class="line">    User root</span><br><span class="line">    IdentityFile ~/.ssh/id_rsa</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>然后测试</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh 192.168.5.106 </span><br><span class="line">就可以直接连接了</span><br><span class="line">使用xshell 依然会让你输入用户名和选择密钥位置</span><br><span class="line"></span><br><span class="line">使用cmd 或者 bash 则没有了 可能是xshell限制</span><br><span class="line"></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>linux</category>
      </categories>
      <tags>
        <tag>github</tag>
        <tag>ssh</tag>
        <tag>密钥登录</tag>
      </tags>
  </entry>
  <entry>
    <title>gitlab 生产安全事故记录</title>
    <url>/post/gitlab-%E7%94%9F%E4%BA%A7%E5%AE%89%E5%85%A8%E4%BA%8B%E6%95%85%E8%AE%B0%E5%BD%95.html</url>
    <content><![CDATA[<p>问题：公司自建gitlab突然卡死 ，无法启动，启动后直接卡死虚拟机。</p>
<p>公司使用的是一个20c16g 的pc来做服务器。</p>
<p>安装了一个 windows，使用vm开linux虚拟机的方式来分配。</p>
<p>突然在没有断电的情况下，安装了gitlab虚拟机无法启动，直接卡死。</p>
<p>因为一直没有问题，也没有创建自动快照，因此没有备份，只能强行进去操作。</p>
<p>虚拟机能开，但是过一段时间会卡死。</p>
<span id="more"></span>

<p><strong>这里就直接说原因了： gitlab使用的是docker安装，只要docker启动gitlab，启动后几分钟，就会卡死，ssh断开连接。vm软件直接卡死。</strong></p>
<p><strong>因此 首先开启虚拟机，关闭开机启动docker，停止gitlab容器。</strong></p>
<p><strong>说明 我们的gitlab文件是外挂出来了的</strong></p>
<p>我用过的解决方法：</p>
<ol>
<li>我想复制gitlab文件出来，结果复制中途，直接卡死，估计文件损坏之类。</li>
<li>是有修复软件，修复结果最后卡死</li>
<li>利用软件，把vmdk镜像copy出来，结果vmdk连文件都无法查看</li>
<li>快照镜像，然后试图启动，卡死</li>
<li>复制vm的文件，中途一个文件损坏。估计就是这个文件损坏导致无法启动gitlab，</li>
<li>因此选择其他的vmdk文件启动，可以启动，但是gitlab的代码是一年前的，不是最新的，因此只是恢复了部分。</li>
<li>新建虚拟机，选择报错的vmdk文件启动，然后进行 快照<ol>
<li>企图启动gitlab 卡死</li>
<li>建立新的 gitlab容器 外挂地址选择 以前的地址，结果卡死（建立新的gitlab，外挂干净地址，是可以启动的）</li>
<li>拉取新版本的镜像，进行gitlab升级，同样挂在以前的地址，依然卡死。但是有所进步。</li>
</ol>
</li>
<li>企图升级内核，来解决 依然卡死</li>
</ol>
<p><strong>最终解决方法 把外挂地址的 git-data 文件 和 postgresql文件 复制出来，新建一个虚拟机，把文件复制进去，新建一个gitlab容器，外挂地址选择这两个文件 ，内容恢复，就此完结。</strong></p>
<p><strong>这之前：</strong>复制这两个文件出来的时候没有卡死，说明不是他们两的问题，是其他gitlab文件的问题，其实当时我也删除所有gitlab-*的文件夹，但是还是卡死，所以才选择复制出来新建一个虚拟机测试。</p>
<p>中途我也看了很多关于 gitlab出错如何修复的解决方法。但是都没有用。毕竟别人只是出问题，而我的是卡死。估计是硬盘出问题了。中途还发生了硬盘无法识别，真是吓死人。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> gitlab/data</span><br><span class="line"><span class="comment"># 删除 pid</span></span><br><span class="line"><span class="built_in">rm</span> -rf `find . -name *.pid`</span><br></pre></td></tr></table></figure>

<p>后续：</p>
<ol>
<li><p>建立定时快照</p>
</li>
<li><p>虚拟机 每周定时gitlab备份</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">crontab -e</span><br><span class="line">0 0 * * 0 docker <span class="built_in">exec</span> -it gitlab gitlab-rake gitlab:backup:create</span><br></pre></td></tr></table></figure></li>
</ol>
<p>真的是安全第一啊。。。。</p>
<p>windows 自动重启和关机命令  别问我如何知道的（不知道电脑重启了几百次）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">shutdown -r -t 5</span><br><span class="line">shutdown -s -t 0</span><br></pre></td></tr></table></figure>

<p><strong>2020-9-8更新</strong></p>
<p>用了一段时间发现居然不能webhook，ci等功能，然后经过一堆google，发现问题了。</p>
<p>（其实新的项目可以webhook，但是旧的以前用过webhook，就不能了因为key不一样了，因此理论你可以把所有项目重新创建一个新的，就可以了，但是这样太麻烦了）</p>
<p>正常的是gitlab-secrets.json 文件丢失，而我只是没有copy，所以我就把以前的gitlab-secrets.json复制过来了，就可以了。</p>
<p>如果真的丢失了。</p>
<p>官方回答：<a href="https://gitlab.com/gitlab-org/gitlab-foss/-/issues/57690">https://gitlab.com/gitlab-org/gitlab-foss/-/issues/57690</a></p>
<p>您是否进行了备份和还原？机密文件似乎不正确，并且加密的URL字段无法解密：</p>
<ul>
<li><a href="https://docs.gitlab.com/ee/raketasks/backup_restore.html#storing-configuration-files">https://docs.gitlab.com/ee/raketasks/backup_restore.html#storing-configuration-files</a></li>
<li><a href="https://docs.gitlab.com/ee/raketasks/backup_restore.html#when-the-secrets-file-is-lost">https://docs.gitlab.com/ee/raketasks/backup_restore.html#when-the-secrets-file-is-lost</a></li>
</ul>
]]></content>
      <categories>
        <category>生产安全记录</category>
      </categories>
      <tags>
        <tag>gitlab</tag>
      </tags>
  </entry>
  <entry>
    <title>hexo 的基础使用</title>
    <url>/post/hello-world.html</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<span id="more"></span>

<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
      <categories>
        <category>博客搭建</category>
      </categories>
  </entry>
  <entry>
    <title>grpc 之 word2pdf使用</title>
    <url>/post/grpc-%E4%B9%8B-word2pdf%E4%BD%BF%E7%94%A8.html</url>
    <content><![CDATA[<blockquote>
<pre><code>    做一个word转pdf的服务，采用grpc，使用libreoffice命令。
</code></pre>
</blockquote>
<span id="more"></span>

<h2 id="构建libreoffice镜像"><a href="#构建libreoffice镜像" class="headerlink" title="构建libreoffice镜像"></a>构建libreoffice镜像</h2><ol>
<li>采用python3.6镜像 </li>
<li>使用阿里源</li>
<li>安装libreoffice</li>
<li>解决中文乱码 加入中文字体</li>
</ol>
<h3 id="dockerfile"><a href="#dockerfile" class="headerlink" title="dockerfile"></a>dockerfile</h3><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">FROM</span> python:<span class="number">3.6</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">ENV</span> TZ=Asia/Shanghai</span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> <span class="built_in">ln</span> -snf /usr/share/zoneinfo/<span class="variable">$TZ</span> /etc/localtime &amp;&amp; <span class="built_in">echo</span> <span class="variable">$TZ</span> &gt; /etc/timezone</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> <span class="built_in">cd</span> /etc/apt \</span></span><br><span class="line"><span class="language-bash">    &amp;&amp; <span class="built_in">mv</span> sources.list sources.list.bak \</span></span><br><span class="line"><span class="language-bash">    &amp;&amp; <span class="built_in">echo</span> <span class="string">&quot;deb http://mirrors.aliyun.com/debian/ stretch main non-free contrib \</span></span></span><br><span class="line"><span class="string"><span class="language-bash">deb-src http://mirrors.aliyun.com/debian/ stretch main non-free contrib \</span></span></span><br><span class="line"><span class="string"><span class="language-bash">deb http://mirrors.aliyun.com/debian-security stretch/updates main \</span></span></span><br><span class="line"><span class="string"><span class="language-bash">deb-src http://mirrors.aliyun.com/debian-security stretch/updates main \</span></span></span><br><span class="line"><span class="string"><span class="language-bash">deb http://mirrors.aliyun.com/debian/ stretch-updates main non-free contrib \</span></span></span><br><span class="line"><span class="string"><span class="language-bash">deb-src http://mirrors.aliyun.com/debian/ stretch-updates main non-free contrib \</span></span></span><br><span class="line"><span class="string"><span class="language-bash">deb http://mirrors.aliyun.com/debian/ stretch-backports main non-free contrib \</span></span></span><br><span class="line"><span class="string"><span class="language-bash">deb-src http://mirrors.aliyun.com/debian/ stretch-backports main non-free contrib&quot;</span> &gt; sources.list</span></span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> apt-get update</span></span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> apt-get install -y libreoffice</span></span><br><span class="line"><span class="keyword">COPY</span><span class="language-bash"> ./ /root/</span></span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> <span class="built_in">mv</span> /root/simsun.ttc /usr/share/fonts &amp;&amp; <span class="built_in">mv</span> /root/simhei.ttf /usr/share/fonts &amp;&amp; <span class="built_in">cd</span> /usr/share/fonts &amp;&amp; fc-cache -fv</span></span><br><span class="line"><span class="comment"># docker build -t libreoffice .</span></span><br></pre></td></tr></table></figure>

<h2 id="grpc服务端、客户端"><a href="#grpc服务端、客户端" class="headerlink" title="grpc服务端、客户端"></a>grpc服务端、客户端</h2><ol>
<li><p>创建proto配置文件 然后编译</p>
</li>
<li><p>服务端与客户端 采用二进制  数据进行传输</p>
</li>
</ol>
<h3 id="服务端"><a href="#服务端" class="headerlink" title="服务端"></a>服务端</h3>  <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python</span></span><br><span class="line"><span class="comment"># -*- coding: utf-8 -*-</span></span><br><span class="line"><span class="comment"># @Time    : 2019/7/9 0009 16:41</span></span><br><span class="line"><span class="comment"># @File    : word2pdf_server_main.py</span></span><br><span class="line"><span class="comment"># @author  : dfkai</span></span><br><span class="line"><span class="comment"># @Software: PyCharm</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># python -m grpc_tools.protoc -I. --python_out=. --grpc_python_out=. ./data.proto</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> pathlib</span><br><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="keyword">import</span> traceback</span><br><span class="line"><span class="keyword">import</span> uuid</span><br><span class="line"><span class="keyword">from</span> concurrent <span class="keyword">import</span> futures</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> grpc</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> proto_py <span class="keyword">import</span> word2pdf_pb2, word2pdf_pb2_grpc</span><br><span class="line"></span><br><span class="line">_ONE_DAY_IN_SECONDS = <span class="number">60</span> * <span class="number">60</span> * <span class="number">24</span></span><br><span class="line">_HOST = os.environ.get(<span class="string">&quot;HOSTNAME&quot;</span>, <span class="string">&quot;localhost&quot;</span>)</span><br><span class="line">_PORT = <span class="string">&#x27;8080&#x27;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">FormatData</span>(word2pdf_pb2_grpc.FormatDataServicer):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">DoFormat</span>(<span class="params">self, request, context</span>):</span><br><span class="line">        <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">        proto 定义方法</span></span><br><span class="line"><span class="string">        :param request:</span></span><br><span class="line"><span class="string">        :param context:</span></span><br><span class="line"><span class="string">        :return:</span></span><br><span class="line"><span class="string">        &quot;&quot;&quot;</span></span><br><span class="line">        data = request.text</span><br><span class="line">        doc_path, pdf_path, pdf_file_path = <span class="variable language_">self</span>.get_doc_pdf_path()</span><br><span class="line">        <span class="keyword">with</span> <span class="built_in">open</span>(doc_path, <span class="string">&quot;wb&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line">            f.write(data)</span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>.word2pdf_linux(doc_path, pdf_path):</span><br><span class="line">            <span class="keyword">try</span>:</span><br><span class="line">                <span class="keyword">with</span> <span class="built_in">open</span>(pdf_file_path, <span class="string">&quot;rb&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line">                    pdf_data = f.read()</span><br><span class="line">            <span class="keyword">except</span>:</span><br><span class="line">                traceback.format_exc()</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            pdf_data = <span class="string">b&quot;fail&quot;</span></span><br><span class="line">        <span class="keyword">return</span> word2pdf_pb2.Data(text=pdf_data)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">get_doc_pdf_path</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">        获取文件路径</span></span><br><span class="line"><span class="string">        :return:</span></span><br><span class="line"><span class="string">        &quot;&quot;&quot;</span></span><br><span class="line">        baseDir = os.getcwd()</span><br><span class="line">        p = pathlib.Path(baseDir)</span><br><span class="line">        u_name = <span class="built_in">str</span>(uuid.uuid4()).replace(<span class="string">&quot;-&quot;</span>, <span class="string">&quot;&quot;</span>)</span><br><span class="line">        doc_name = u_name + <span class="string">&quot;.docx&quot;</span></span><br><span class="line">        pdf_name = u_name + <span class="string">&quot;.pdf&quot;</span></span><br><span class="line">        pdf_path = p / <span class="string">f&quot;filepath/pdf/&quot;</span></span><br><span class="line">        doc_path = p / <span class="string">f&quot;filepath/doc/<span class="subst">&#123;doc_name&#125;</span>&quot;</span></span><br><span class="line">        pdf_file_path = p / <span class="string">f&quot;filepath/pdf/<span class="subst">&#123;pdf_name&#125;</span>&quot;</span></span><br><span class="line">        <span class="built_in">print</span>(doc_path, pdf_path, pdf_file_path)</span><br><span class="line">        <span class="keyword">return</span> <span class="string">rf&quot;<span class="subst">&#123;doc_path&#125;</span>&quot;</span>, <span class="string">rf&quot;<span class="subst">&#123;pdf_path&#125;</span>&quot;</span>, <span class="string">rf&quot;<span class="subst">&#123;pdf_file_path&#125;</span>&quot;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">word2pdf_win</span>(<span class="params">self, doc_path, pdf_path</span>):</span><br><span class="line">        <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">        windows 生成</span></span><br><span class="line"><span class="string">        :param doc_path:</span></span><br><span class="line"><span class="string">        :param pdf_path:</span></span><br><span class="line"><span class="string">        :return:</span></span><br><span class="line"><span class="string">        &quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">from</span> win32com <span class="keyword">import</span> client</span><br><span class="line">        <span class="keyword">import</span> pythoncom</span><br><span class="line">        pythoncom.CoInitialize()</span><br><span class="line">        <span class="comment"># word = client.Dispatch(&quot;Word.Application&quot;)</span></span><br><span class="line">        word = client.DispatchEx(<span class="string">&quot;Word.Application&quot;</span>)</span><br><span class="line">        worddoc = word.Documents.Open(doc_path)</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            worddoc.SaveAs(pdf_path, FileFormat=<span class="number">17</span>)</span><br><span class="line">        <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">            <span class="built_in">print</span>(e)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">        <span class="keyword">finally</span>:</span><br><span class="line">            worddoc.Close()</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">word2pdf_linux</span>(<span class="params">self, doc_path, pdf_path</span>):</span><br><span class="line">        <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">        linux 生成 pdf ,利用 libreoffice 命令</span></span><br><span class="line"><span class="string">        :param doc_path:</span></span><br><span class="line"><span class="string">        :param pdf_path:</span></span><br><span class="line"><span class="string">        :return:</span></span><br><span class="line"><span class="string">        &quot;&quot;&quot;</span></span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            os.system(<span class="string">f&quot;soffice --headless --invisible --convert-to pdf <span class="subst">&#123;doc_path&#125;</span> --outdir <span class="subst">&#123;pdf_path&#125;</span> &quot;</span>)</span><br><span class="line">        <span class="keyword">except</span>:</span><br><span class="line">            traceback.format_exc()</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">serve</span>():</span><br><span class="line">    <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    rpc 服务</span></span><br><span class="line"><span class="string">    :return:</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    grpcServer = grpc.server(futures.ThreadPoolExecutor(max_workers=<span class="number">4</span>))</span><br><span class="line">    word2pdf_pb2_grpc.add_FormatDataServicer_to_server(FormatData(), grpcServer)</span><br><span class="line">    grpcServer.add_insecure_port(_HOST + <span class="string">&#x27;:&#x27;</span> + _PORT)</span><br><span class="line">    grpcServer.start()</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            time.sleep(_ONE_DAY_IN_SECONDS)</span><br><span class="line">    <span class="keyword">except</span> KeyboardInterrupt:</span><br><span class="line">        grpcServer.stop(<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    serve()</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h3 id="客户端"><a href="#客户端" class="headerlink" title="客户端"></a>客户端</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python</span></span><br><span class="line"><span class="comment"># -*- coding: utf-8 -*-</span></span><br><span class="line"><span class="comment"># @Time    : 2019/7/9 0009 16:40</span></span><br><span class="line"><span class="comment"># @File    : word2pdf_client_main.py</span></span><br><span class="line"><span class="comment"># @author  : dfkai</span></span><br><span class="line"><span class="comment"># @Software: PyCharm</span></span><br><span class="line"><span class="keyword">import</span> traceback</span><br><span class="line"><span class="keyword">import</span> grpc</span><br><span class="line"><span class="keyword">from</span> proto_py <span class="keyword">import</span> word2pdf_pb2, word2pdf_pb2_grpc</span><br><span class="line"></span><br><span class="line">_HOST = <span class="string">&#x27;localhost&#x27;</span></span><br><span class="line">_PORT = <span class="string">&#x27;8080&#x27;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">run</span>():</span><br><span class="line">    file_name = <span class="string">&quot;test&quot;</span></span><br><span class="line">    doc_name = file_name + <span class="string">&#x27;.doc&#x27;</span></span><br><span class="line">    conn = grpc.insecure_channel(_HOST + <span class="string">&#x27;:&#x27;</span> + _PORT)</span><br><span class="line">    client = word2pdf_pb2_grpc.FormatDataStub(channel=conn)</span><br><span class="line">    <span class="keyword">with</span> <span class="built_in">open</span>(doc_name, <span class="string">&quot;rb&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line">        data = f.read()</span><br><span class="line">    response = client.DoFormat(word2pdf_pb2.Data(text=data))</span><br><span class="line">    <span class="keyword">if</span> response.text == <span class="string">b&quot;fail&quot;</span>:</span><br><span class="line">        <span class="comment"># 发送消息 生成失败</span></span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        pdf_name = file_name + <span class="string">f&#x27;.pdf&#x27;</span></span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            <span class="keyword">with</span> <span class="built_in">open</span>(pdf_name, <span class="string">&quot;wb&quot;</span>) <span class="keyword">as</span> f:</span><br><span class="line">                f.write(response.text)</span><br><span class="line">        <span class="keyword">except</span>:</span><br><span class="line">            traceback.format_exc()</span><br><span class="line">            <span class="comment"># 发送消息 生成失败</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="comment"># 发送消息 生成成功</span></span><br><span class="line">            <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    <span class="keyword">import</span> time</span><br><span class="line">    beg = time.time()</span><br><span class="line">    run()</span><br><span class="line">    end = time.time()</span><br><span class="line">    <span class="built_in">print</span>(end - beg)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h3 id="proto配置文件"><a href="#proto配置文件" class="headerlink" title="proto配置文件"></a>proto配置文件</h3><figure class="highlight protobuf"><table><tr><td class="code"><pre><span class="line">syntax = <span class="string">&quot;proto3&quot;</span>;</span><br><span class="line"><span class="keyword">package</span> example;</span><br><span class="line"><span class="keyword">service </span><span class="title class_">FormatData</span> &#123;</span><br><span class="line">  <span class="function"><span class="keyword">rpc</span> DoFormat(Data) <span class="keyword">returns</span> (Data)</span>&#123;&#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">message </span><span class="title class_">Data</span> &#123;</span><br><span class="line">  <span class="type">bytes</span> text = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>进入文件目录，构建命令：python -m grpc_tools.protoc -I. –python_out&#x3D;.&#x2F;proto_py&#x2F; –grpc_python_out&#x3D;.&#x2F;proto_py&#x2F; .&#x2F;proto&#x2F;word2pdf.proto</p>
<h2 id="构建rpc服务端镜像"><a href="#构建rpc服务端镜像" class="headerlink" title="构建rpc服务端镜像"></a>构建rpc服务端镜像</h2><h3 id="dockerfile-1"><a href="#dockerfile-1" class="headerlink" title="dockerfile"></a>dockerfile</h3><figure class="highlight dockerfile"><table><tr><td class="code"><pre><span class="line"><span class="keyword">FROM</span> libreoffice</span><br><span class="line"></span><br><span class="line"><span class="keyword">ENV</span> TZ=Asia/Shanghai</span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> <span class="built_in">ln</span> -snf /usr/share/zoneinfo/<span class="variable">$TZ</span> /etc/localtime &amp;&amp; <span class="built_in">echo</span> <span class="variable">$TZ</span> &gt; /etc/timezone</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">COPY</span><span class="language-bash"> ./ /root/</span></span><br><span class="line"><span class="keyword">WORKDIR</span><span class="language-bash"> /root/word2pdfRPC</span></span><br><span class="line"><span class="keyword">RUN</span><span class="language-bash"> pip3 install -i https://mirrors.aliyun.com/pypi/simple/ -r requirments.txt</span></span><br><span class="line"><span class="keyword">EXPOSE</span> <span class="number">8080</span></span><br><span class="line"><span class="keyword">CMD</span><span class="language-bash"> python server_main.py</span></span><br><span class="line"><span class="comment"># docker build -t word2pdf .</span></span><br><span class="line"><span class="comment"># docker run -d -p 8080:8080 -v /root/data/word2pdf/:/root/word2pdfRPC/filepath/ --name word2pdf word2pdf</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h3 id="reuirements-txt"><a href="#reuirements-txt" class="headerlink" title="reuirements.txt"></a>reuirements.txt</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">futures==3.1.1</span><br><span class="line">grpcio==1.22.0</span><br><span class="line">grpcio-tools==1.22.0</span><br><span class="line">protobuf==3.8.0</span><br></pre></td></tr></table></figure>



<p>参考、推荐:</p>
<ol>
<li><a href="https://blog.csdn.net/xqy1522/article/details/6942344">protobuf和thrift对比</a></li>
<li><a href="https://developers.google.com/protocol-buffers/docs/proto3">Language Guide (proto3)</a></li>
<li><a href="https://developers.google.com/protocol-buffers/docs/encoding">Encoding</a></li>
</ol>
]]></content>
      <categories>
        <category>代码来了</category>
      </categories>
      <tags>
        <tag>grpc</tag>
      </tags>
  </entry>
  <entry>
    <title>hexo next主题 安装记录</title>
    <url>/post/install-next-all.html</url>
    <content><![CDATA[<p>最近无聊有想整点东西，想这写blog，就心血来潮又搞了hexo，想着这次一定要静下心来。</p>
<span id="more"></span>

<h3 id="安装-hexo"><a href="#安装-hexo" class="headerlink" title="安装 hexo"></a>安装 hexo</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-cli -g --registry=https://registry.npm.taobao.org</span><br><span class="line">hexo init blog</span><br><span class="line"><span class="built_in">cd</span> blog</span><br><span class="line">npm install</span><br><span class="line">hexo server</span><br></pre></td></tr></table></figure>

<h3 id="根据配置-修改自己的信息"><a href="#根据配置-修改自己的信息" class="headerlink" title="根据配置 修改自己的信息"></a>根据配置 修改自己的信息</h3><p>官网：<a href="https://hexo.io/zh-cn/docs/configuration">https://hexo.io/zh-cn/docs/configuration</a></p>
<h3 id="安装主题-next"><a href="#安装主题-next" class="headerlink" title="安装主题 next"></a>安装主题 next</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> blog</span><br><span class="line">git <span class="built_in">clone</span> https://github.com/theme-next/hexo-theme-next themes/next</span><br></pre></td></tr></table></figure>

<h3 id="修改-主题配置"><a href="#修改-主题配置" class="headerlink" title="修改 主题配置"></a>修改 主题配置</h3><p>官网：<a href="https://theme-next.iissnan.com/getting-started.html">https://theme-next.iissnan.com/getting-started.html</a></p>
<h3 id="集成第三方插件"><a href="#集成第三方插件" class="headerlink" title="集成第三方插件"></a>集成第三方插件</h3><h4 id="1-搜索"><a href="#1-搜索" class="headerlink" title="1.搜索"></a>1.搜索</h4><h5 id="安装插件"><a href="#安装插件" class="headerlink" title="安装插件"></a>安装插件</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-searchdb --save --registry=https://registry.npm.taobao.org</span><br></pre></td></tr></table></figure>

<h5 id="配置博客"><a href="#配置博客" class="headerlink" title="配置博客"></a>配置博客</h5><p>安装完成，编辑博客配置文件：<code>_config.yml</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">search:</span><br><span class="line">  path: search.xml</span><br><span class="line">  field: post</span><br><span class="line">  format: html</span><br><span class="line">  limit: 10000</span><br></pre></td></tr></table></figure>

<h5 id="配置主题"><a href="#配置主题" class="headerlink" title="配置主题"></a>配置主题</h5><p>Next 主题自带搜索设置，编辑主题配置文件：<code>_config.yml</code></p>
<p>找到文件中 Local search 的相关配置，设为 <code>true</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># Local search</span><br><span class="line">local_search:</span><br><span class="line">  enable: true</span><br></pre></td></tr></table></figure>

<h4 id="2-评论和阅读次数"><a href="#2-评论和阅读次数" class="headerlink" title="2.评论和阅读次数"></a>2.评论和阅读次数</h4><p>注册 learncloud 进行部署</p>
<h4 id="3-seo优化"><a href="#3-seo优化" class="headerlink" title="3.seo优化"></a>3.seo优化</h4><p>参考：[<a href="https://hoxis.github.io/Hexo+Next%20SEO%E4%BC%98%E5%8C%96.html]">https://hoxis.github.io/Hexo+Next%20SEO%E4%BC%98%E5%8C%96.html]</a>(<a href="https://hoxis.github.io/Hexo+Next">https://hoxis.github.io/Hexo+Next</a> SEO优化.html)</p>
<h5 id="安装插件-1"><a href="#安装插件-1" class="headerlink" title="安装插件"></a>安装插件</h5><p>需要安装两个插件来生成 sitemap 文件，前一个是传统的 sitemap，后一个是百度的 sitemap。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-sitemap --save</span><br><span class="line">npm install hexo-generator-baidu-sitemap --save</span><br></pre></td></tr></table></figure>

<h4 id="修改站点配置文件"><a href="#修改站点配置文件" class="headerlink" title="修改站点配置文件"></a>修改站点配置文件</h4><p>将<code>sitemap</code>文件添加到站点配置文件<code>_config.yml</code>中，并修改<code>url</code>字段的值，其值默认为<code>http://yoursite.com</code>。</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">sitemap:</span> </span><br><span class="line">  <span class="attr">path:</span> <span class="string">sitemap.xml</span></span><br><span class="line"><span class="attr">baidusitemap:</span></span><br><span class="line">  <span class="attr">path:</span> <span class="string">baidusitemap.xml</span></span><br><span class="line"></span><br><span class="line"><span class="attr">url:</span> <span class="string">https://libaibuaidufu.club</span> <span class="comment"># 这个在最开始的地方有 注意重复</span></span><br></pre></td></tr></table></figure>

<h4 id="rabots-协议"><a href="#rabots-协议" class="headerlink" title="rabots 协议"></a>rabots 协议</h4><p>在站点<code>source</code>文件夹下新建<code>robots.txt</code>文件，文件内容如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">User-agent: *</span><br><span class="line">Allow: /</span><br><span class="line">Allow: /archives/</span><br><span class="line">Allow: /categories/</span><br><span class="line">Allow: /tags/ </span><br><span class="line">Allow: /resources/ </span><br><span class="line">Disallow: /vendors/</span><br><span class="line">Disallow: /js/</span><br><span class="line">Disallow: /css/</span><br><span class="line">Disallow: /fonts/</span><br><span class="line">Disallow: /vendors/</span><br><span class="line">Disallow: /fancybox/</span><br><span class="line"></span><br><span class="line">Sitemap: https://libaibuaidufu.club/sitemap.xml</span><br><span class="line">Sitemap: https://libaibuaidufu.club/baidusitemap.xml</span><br><span class="line"></span><br></pre></td></tr></table></figure>



<p>安装完成后执行<code>hexo g</code>即会在站点<code>public</code>目录下生成<code>sitemap.xml</code>和<code>baidusitemap.xml</code>。</p>
<p><code>Allow</code>字段的值即为允许搜索引擎爬区的内容，可以对应到主题配置文件中的menu目录配置，如果菜单栏还有其他选项都可以按照格式自行添加。</p>
<p>需要将<code>https://hoxis.github.io</code>改成自己的域名。</p>
<h3 id="github部署"><a href="#github部署" class="headerlink" title="github部署"></a>github部署</h3><p>github：<a href="https://hexo.io/zh-cn/docs/github-pages">https://hexo.io/zh-cn/docs/github-pages</a></p>
<p>一键部署：<a href="https://hexo.io/zh-cn/docs/one-command-deployment">https://hexo.io/zh-cn/docs/one-command-deployment</a></p>
<p>安装插件：<code>npm install hexo-deployer-git --save --registry=https://registry.npm.taobao.org</code></p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">deploy:</span></span><br><span class="line">  <span class="attr">type:</span> <span class="string">git</span></span><br><span class="line">  <span class="attr">repo:</span> <span class="string">https://github.com/&lt;username&gt;/&lt;project&gt;</span></span><br><span class="line">  <span class="comment"># example, https://github.com/hexojs/hexojs.github.io</span></span><br><span class="line">  <span class="attr">branch:</span> <span class="string">master</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>博客搭建</category>
      </categories>
      <tags>
        <tag>hexo</tag>
        <tag>next</tag>
      </tags>
  </entry>
  <entry>
    <title>ipv6-DDNS-CloudFlare-家庭内网穿透</title>
    <url>/post/ipv6-DDNS-CloudFlare-%E5%AE%B6%E5%BA%AD%E5%86%85%E7%BD%91%E7%A9%BF%E9%80%8F.html</url>
    <content><![CDATA[<h4 id="获取ipv6"><a href="#获取ipv6" class="headerlink" title="获取ipv6"></a>获取ipv6</h4><p>一般都是在路由器开启ipv6功能，然后路由器在分发ipv6,然后就是开启电脑的ipv6功能，电脑开启ipv6功能就直接搜索一下就知道。</p>
<p>通过win+r，输入cmd运行，弹出cmd命令框，输入ipconfig查看自己的ipv6。</p>
<h4 id="域名绑定ipv6"><a href="#域名绑定ipv6" class="headerlink" title="域名绑定ipv6"></a>域名绑定ipv6</h4><p>域名的作用：ipv6可能会变化，通过一个工具在ipv6变化的时候，自动更新到域名上就可以，你就只需要访问域名，而不需要去记ipv6地址。</p>
<p>工具：<a href="https://github.com/jeessy2/ddns-go%EF%BC%8C%E4%B8%8B%E8%BD%BD%E5%B7%A5%E5%85%B7">https://github.com/jeessy2/ddns-go，下载工具</a></p>
<p>直接运行，然后本地访问 <a href="http://127.0.0.1:9876/">http://127.0.0.1:9876</a> 去配置。</p>
<p>token：直接点击对应的服务商，工具已经做了很好的引导，直接配置搞定。</p>
<p>ipv6绑定域名，通过上面ipconfig命令可以看到多个ipv6，我的第一个ipv6是一个短的没有用，所以这里用@2指定第二个ipv6地址。【上面的ipv4可以关闭】其他保持默认保存就行了。</p>
<p><img src="https://i.111666.best/image/FgtWcmUBTgpqap9vo00C1r.png" alt="image"></p>
<h4 id="ddns-go-自启动"><a href="#ddns-go-自启动" class="headerlink" title="ddns-go 自启动"></a>ddns-go 自启动</h4><p>工具本身支持这个功能</p>
<p>输入命令<code>.\ddns-go.exe -s install</code>就可以了。以后就可以自动更新了，里面也支持配置监听时间之类的，就可以自己去调试了。</p>
<h4 id="外网访问"><a href="#外网访问" class="headerlink" title="外网访问"></a>外网访问</h4><p>虽然已经绑定域名了，但是还是有很多限制。<br>路由器限制，有一些路由器开了ipv6防火墙，这个需要关闭或者开放指定端口，你的服务才可以被外网访问。<br>还有就是windows中的防火墙也需要开启指定端口才行。</p>
<p>大体上还只是说了思路，以及容易遇到的坑，具体操作可能还是需要去搜索才行。</p>
]]></content>
      <categories>
        <category>内网穿透</category>
      </categories>
      <tags>
        <tag>内网穿透</tag>
      </tags>
  </entry>
  <entry>
    <title>learn Websocket</title>
    <url>/post/learn-Websocket.html</url>
    <content><![CDATA[<blockquote>
<p>参考：</p>
<ol>
<li><a href="http://www.ruanyifeng.com/blog/2017/05/websocket.html">http://www.ruanyifeng.com/blog/2017/05/websocket.html</a></li>
<li><a href="https://socket.io/docs/client-api/#IO">https://socket.io/docs/client-api/#IO</a></li>
<li><a href="https://flask-socketio.readthedocs.io/en/latest/">https://flask-socketio.readthedocs.io/en/latest/</a></li>
</ol>
</blockquote>
<h2 id="流程"><a href="#流程" class="headerlink" title="流程"></a>流程</h2><p>客服端发起连接 —–&gt;  服务端 connect事件 连接 —–&gt; 客户端 connect事件 反应。</p>
<p> send 和 emit区别，send只能在 message事件上接受，emit可以发给任何事件接收。</p>
<p>因此emit是首选。</p>
<span id="more"></span>

<h3 id="客户端-socket"><a href="#客户端-socket" class="headerlink" title="客户端  socket"></a>客户端  socket</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">&quot;https://cdn.bootcdn.net/ajax/libs/socket.io/2.3.0/socket.io.js&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">    <span class="keyword">var</span> socket = <span class="title function_">io</span>(<span class="string">&quot;ws://127.0.0.1:5000&quot;</span>);</span></span><br><span class="line"><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">    <span class="variable language_">console</span>.<span class="title function_">log</span>(socket.<span class="property">id</span>); <span class="comment">//undefined</span></span></span><br><span class="line"><span class="language-javascript">    <span class="variable language_">console</span>.<span class="title function_">log</span>(socket.<span class="property">connected</span>); <span class="comment">// false</span></span></span><br><span class="line"><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">    socket.<span class="title function_">on</span>(<span class="string">&#x27;connect&#x27;</span>, <span class="function">(<span class="params">evt</span>) =&gt;</span> &#123;</span></span><br><span class="line"><span class="language-javascript">        <span class="variable language_">console</span>.<span class="title function_">log</span>(socket.<span class="property">id</span>); <span class="comment">//唯一id</span></span></span><br><span class="line"><span class="language-javascript">        <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;connect&quot;</span>, evt);</span></span><br><span class="line"><span class="language-javascript">        <span class="variable language_">console</span>.<span class="title function_">log</span>(socket.<span class="property">connected</span>); <span class="comment">// true</span></span></span><br><span class="line"><span class="language-javascript">        socket.<span class="title function_">emit</span>(<span class="string">&quot;onmessage&quot;</span>, <span class="string">&quot;hello,world&quot;</span>)</span></span><br><span class="line"><span class="language-javascript">    &#125;);</span></span><br><span class="line"><span class="language-javascript"></span></span><br><span class="line"><span class="language-javascript">    socket.<span class="title function_">on</span>(<span class="string">&#x27;disconnect&#x27;</span>, <span class="function">(<span class="params">evt</span>) =&gt;</span> &#123;</span></span><br><span class="line"><span class="language-javascript">        <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;disconnect&quot;</span>, evt);</span></span><br><span class="line"><span class="language-javascript">        <span class="variable language_">console</span>.<span class="title function_">log</span>(socket.<span class="property">connected</span>); <span class="comment">// false</span></span></span><br><span class="line"><span class="language-javascript">        <span class="variable language_">console</span>.<span class="title function_">log</span>(socket.<span class="property">disconnected</span>); <span class="comment">// false</span></span></span><br><span class="line"><span class="language-javascript">    &#125;);</span></span><br><span class="line"><span class="language-javascript">    socket.<span class="title function_">on</span>(<span class="string">&quot;onmessage&quot;</span>, <span class="function">(<span class="params">evt</span>) =&gt;</span> &#123;</span></span><br><span class="line"><span class="language-javascript">        <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;onmessage&quot;</span>, evt);</span></span><br><span class="line"><span class="language-javascript">        socket.<span class="title function_">send</span>(<span class="string">&quot;gjjgjg&quot;</span>);</span></span><br><span class="line"><span class="language-javascript">        socket.<span class="title function_">disconnect</span>()</span></span><br><span class="line"><span class="language-javascript">    &#125;);</span></span><br><span class="line"><span class="language-javascript">    socket.<span class="title function_">on</span>(<span class="string">&quot;message&quot;</span>, <span class="function">(<span class="params">evt</span>) =&gt;</span> &#123;</span></span><br><span class="line"><span class="language-javascript">        <span class="comment">// send 只能用在 message事件上</span></span></span><br><span class="line"><span class="language-javascript">        <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;message&quot;</span>, evt);</span></span><br><span class="line"><span class="language-javascript">    &#125;)</span></span><br><span class="line"><span class="language-javascript">    <span class="comment">// 监听窗口事件，当窗口关闭时，主动断开websocket连接，防止连接没断开就关闭窗口，server端报错</span></span></span><br><span class="line"><span class="language-javascript">    <span class="variable language_">window</span>.<span class="property">onbeforeunload</span> = <span class="function">() =&gt;</span> &#123;</span></span><br><span class="line"><span class="language-javascript">        socket.<span class="title function_">close</span>()</span></span><br><span class="line"><span class="language-javascript">    &#125;</span></span><br><span class="line"><span class="language-javascript">    </span></span><br><span class="line"><span class="language-javascript"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="socket-id"><a href="#socket-id" class="headerlink" title="socket.id"></a>socket.id</h4><p><em>字符串</em></p>
<p>套接字会话的唯一标识符。在<code>connect</code>事件触发后设置，并在<code>reconnect</code>事件后更新。</p>
<h4 id="socket-connected"><a href="#socket-connected" class="headerlink" title="socket.connected"></a>socket.connected</h4><p><em>布尔值</em></p>
<p>套接字是否已连接到服务器。</p>
<h4 id="socket-disconnected"><a href="#socket-disconnected" class="headerlink" title="socket.disconnected"></a>socket.disconnected</h4><p><em>布尔值</em></p>
<p>套接字是否与服务器断开连接。</p>
<h4 id="socket-open"><a href="#socket-open" class="headerlink" title="socket.open()"></a>socket.open()</h4><p>手动重连</p>
<h4 id="socket-connect"><a href="#socket-connect" class="headerlink" title="socket.connect()"></a>socket.connect()</h4><p>同 ocket.open()</p>
<h4 id="socket-on"><a href="#socket-on" class="headerlink" title="socket.on"></a>socket.on</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">socket.on(&#x27;news&#x27;, (data) =&gt; &#123;  console.log(data);&#125;);// wit</span><br></pre></td></tr></table></figure>

<h4 id="socket-emit"><a href="#socket-emit" class="headerlink" title="socket.emit"></a>socket.emit</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">socket.emit(&#x27;hello&#x27;, &#x27;world&#x27;);</span><br></pre></td></tr></table></figure>

<h4 id="socket-send"><a href="#socket-send" class="headerlink" title="socket.send"></a>socket.send</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">socket.send(&#x27;world&#x27;);</span><br></pre></td></tr></table></figure>

<h4 id="socket-close"><a href="#socket-close" class="headerlink" title="socket.close()"></a>socket.close()</h4><p>手动关闭</p>
<h4 id="socket-disconnect"><a href="#socket-disconnect" class="headerlink" title="socket.disconnect()"></a>socket.disconnect()</h4><p>同 socket.close()</p>
<h4 id="socket-compress-value"><a href="#socket-compress-value" class="headerlink" title="socket.compress(value)"></a>socket.compress(value)</h4><p>压缩传输</p>
<ul>
<li><code>value</code> <em>（布尔值）</em></li>
<li><strong>退货</strong> <code>Socket</code></li>
</ul>
<p>设置用于后续事件发射的修饰符，以使事件数据仅在值为时才被<em>压缩</em><code>true</code>。默认为<code>true</code>不调用该方法的时间。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">socket.compress(false).emit(&#x27;an event&#x27;, &#123; some: &#x27;data&#x27; &#125;);</span><br></pre></td></tr></table></figure>

<h4 id="socket-binary-value"><a href="#socket-binary-value" class="headerlink" title="socket.binary(value)"></a>socket.binary(value)</h4><p>指定发出的数据是否包含二进制。指定时提高性能。可以是<code>true</code>或<code>false</code>。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">socket.binary（false）.emit（&#x27;一个事件&#x27;，&#123; 一些：&#x27;数据&#x27; &#125;）;</span><br></pre></td></tr></table></figure>

<h3 id="服务端"><a href="#服务端" class="headerlink" title="服务端"></a>服务端</h3><p><em>用很多没有介绍到的，可以查看文档进行学习。</em></p>
<h4 id="普通socket"><a href="#普通socket" class="headerlink" title="普通socket"></a>普通socket</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python</span></span><br><span class="line"><span class="comment"># -*- coding: utf-8 -*-</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">@Time    : 2020/9/27 10:13</span></span><br><span class="line"><span class="string">@File    : main.py</span></span><br><span class="line"><span class="string">@author  : dfkai</span></span><br><span class="line"><span class="string">@Software: PyCharm</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask</span><br><span class="line"><span class="keyword">from</span> flask_socketio <span class="keyword">import</span> SocketIO, emit, send</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line">socketio = SocketIO(app, cors_allowed_origins=<span class="string">&quot;*&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@socketio.on(<span class="params"><span class="string">&quot;connect&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">connect</span>(<span class="params">msg=<span class="literal">None</span></span>):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;connect&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(msg)</span><br><span class="line">    <span class="comment"># emit(&quot;onmessage&quot;, &#123;&quot;data&quot;: &quot;hell world&quot;&#125;)</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@socketio.on(<span class="params"><span class="string">&quot;disconnect&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">disconnect</span>(<span class="params">msg=<span class="literal">None</span></span>):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;disconnect&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(msg)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@socketio.on(<span class="params"><span class="string">&quot;onmessage&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">onmessage</span>(<span class="params">msg=<span class="literal">None</span></span>):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;onmessage&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(msg)</span><br><span class="line">    send(&#123;<span class="string">&quot;data&quot;</span>: <span class="string">&quot;hell world&quot;</span>&#125;)</span><br><span class="line">    emit(<span class="string">&quot;onmessage&quot;</span>, &#123;<span class="string">&quot;data&quot;</span>: <span class="string">&quot;hell world&quot;</span>&#125;)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@socketio.on(<span class="params"><span class="string">&quot;message&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">message</span>(<span class="params">msg=<span class="literal">None</span></span>):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;message&quot;</span>)</span><br><span class="line">    <span class="built_in">print</span>(msg)</span><br><span class="line">    send(<span class="string">&quot;googogogo&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    socketio.run(app, debug=<span class="literal">True</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="后台轮询任务"><a href="#后台轮询任务" class="headerlink" title="后台轮询任务"></a>后台轮询任务</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python</span></span><br><span class="line"><span class="comment"># -*- coding: utf-8 -*-</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">@Time    : 2020/9/14 11:42</span></span><br><span class="line"><span class="string">@File    : back_socket.py</span></span><br><span class="line"><span class="string">@author  : dfkai</span></span><br><span class="line"><span class="string">@Software: PyCharm</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> datetime <span class="keyword">import</span> datetime</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask</span><br><span class="line"><span class="keyword">from</span> flask_socketio <span class="keyword">import</span> SocketIO, emit</span><br><span class="line"></span><br><span class="line">app = Flask(__name__)</span><br><span class="line">socketio = SocketIO(app)</span><br><span class="line"></span><br><span class="line">thread_inspect = <span class="literal">None</span></span><br><span class="line">thread_json = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@socketio.on(<span class="params"><span class="string">&quot;connect&quot;</span></span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">handle_connect</span>():</span><br><span class="line">    <span class="keyword">global</span> thread_json, thread_inspect</span><br><span class="line">    <span class="keyword">if</span> thread_json <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;ine&quot;</span>)</span><br><span class="line">        thread_json = socketio.start_background_task(target=background_thread_for_json)</span><br><span class="line">    <span class="keyword">if</span> thread_inspect <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        thread_json = socketio.start_background_task(target=background_thread_for_inspect)</span><br><span class="line">    emit(<span class="string">&quot;message&quot;</span>, <span class="string">&quot;hhhe&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">background_thread_for_json</span>():</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        socketio.sleep(<span class="number">10</span>)</span><br><span class="line">        now = datetime.now().strftime(<span class="string">&quot;%Y-%m-%d %H:%M:%S&quot;</span>)</span><br><span class="line">        login_json = <span class="string">f&quot;<span class="subst">&#123;now&#125;</span> models.get_login_for_socket()&quot;</span></span><br><span class="line">        arra_json = <span class="string">f&quot;<span class="subst">&#123;now&#125;</span> models.get_arra_for_socket()&quot;</span></span><br><span class="line">        gather_json = <span class="string">f&quot;<span class="subst">&#123;now&#125;</span> models.get_gather_for_socket()&quot;</span></span><br><span class="line">        re_msg_dict = &#123;</span><br><span class="line">            <span class="string">&quot;login_json&quot;</span>: login_json,</span><br><span class="line">            <span class="string">&quot;arra_json&quot;</span>: arra_json,</span><br><span class="line">            <span class="string">&quot;gather_json&quot;</span>: gather_json</span><br><span class="line">        &#125;</span><br><span class="line">        socketio.emit(<span class="string">&quot;message&quot;</span>, re_msg_dict, namespace=<span class="string">&quot;/abc&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">background_thread_for_inspect</span>():</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        socketio.sleep(<span class="number">10</span>)</span><br><span class="line">        now = datetime.now().strftime(<span class="string">&quot;%Y-%m-%d %H:%M:%S&quot;</span>)</span><br><span class="line">        inspect_json = <span class="string">f&quot;<span class="subst">&#123;now&#125;</span> models.get_inspect_for_socket()&quot;</span></span><br><span class="line">        re_msg_dict = &#123;</span><br><span class="line">            <span class="string">&quot;inspect_json&quot;</span>: inspect_json</span><br><span class="line">        &#125;</span><br><span class="line">        socketio.emit(<span class="string">&quot;inspect&quot;</span>, re_msg_dict, namespace=<span class="string">&quot;/abc&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    socketio.run(app)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p><img src="https://i.loli.net/2020/09/27/gSl8aYbT251swOU.png"></p>
]]></content>
      <categories>
        <category>websocket</category>
      </categories>
      <tags>
        <tag>websocket</tag>
      </tags>
  </entry>
  <entry>
    <title>openwrt 编译k2固件</title>
    <url>/post/openwrt-%E7%BC%96%E8%AF%91k2%E5%9B%BA%E4%BB%B6.html</url>
    <content><![CDATA[<p>​	最近又想玩玩我的k2，别人的编译的固件老是没有我自己想要的插件，所以想自己编译一下。中间也踩了很多坑。</p>
<span id="more"></span>

<h2 id="只记录关键步骤"><a href="#只记录关键步骤" class="headerlink" title="只记录关键步骤"></a>只记录关键步骤</h2><ol>
<li>wsl子系统全局外网问题</li>
</ol>
<p>windows使用Proxifier直接全局掌控，稳如老狗。</p>
<p>网上看了一些linux系统代理设置，但是无法全局，因此还是Proxifier永远的神。</p>
<ol start="2">
<li>添加其他feeds插件库</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 找到 feeds.conf.default ,可以利用everythings搜索</span></span><br><span class="line">src-git kenzo https://github.com/kenzok8/openwrt-packages</span><br><span class="line">src-git small https://github.com/kenzok8/small</span><br><span class="line">src-git lienol https://github.com/Lienol/openwrt-package</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>make menuconfig配置</li>
</ol>
<p>参考上面第三个链接</p>
<ol start="4">
<li>编译前执行go环境设置 避免错误</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> GO111MODULE=on</span><br><span class="line"><span class="built_in">export</span> GOPROXY=https://goproxy.cn,direct</span><br></pre></td></tr></table></figure>

<ol start="5">
<li>wsl系统编译</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin make -j1 V=s</span><br><span class="line"><span class="comment"># 以上代码为大雕大佬推荐的单线程模式，-j1表示1线程 </span></span><br><span class="line"><span class="comment"># 直接上最多 自己cpu线程 +1 就可以了 我是6核12线程</span></span><br><span class="line"><span class="built_in">sudo</span> PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin make -j13 V=s</span><br></pre></td></tr></table></figure>

<ol start="6">
<li>如果上面编码出现一下错误</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">configure: error: you should not run configure as root (<span class="built_in">set</span> FORCE_UNSAFE_CONFIGURE=1 <span class="keyword">in</span> environment to bypass this check)</span><br><span class="line">See `config.log<span class="string">&#x27; for more details</span></span><br><span class="line"><span class="string">make[3]: *** [/home/linux/myopenwrt/openwrt/build_dir/host/tar-1.28/.configured] Error 1</span></span><br><span class="line"><span class="string">make[3]: Leaving directory `/home/linux/myopenwrt/openwrt/tools/tar&#x27;</span></span><br><span class="line">make[2]: *** [tools/tar/compile] Error 2</span><br><span class="line">make[2]: Leaving directory `/home/linux/myopenwrt/openwrt<span class="string">&#x27;</span></span><br><span class="line"><span class="string">make[1]: *** [/home/linux/myopenwrt/openwrt/staging_dir/target-mips_34kc_musl-1.1.14/stamp/.tools_install_yynyynynynyyyyyyynyyyyyyyyyyyyynyyyyynnyynynyyyynnnyy] Error 2</span></span><br><span class="line"><span class="string">make[1]: Leaving directory `/home/linux/myopenwrt/openwrt&#x27;</span></span><br><span class="line">make: *** [world] Error 2</span><br></pre></td></tr></table></figure>

<p><strong>wsl编译命令 就修改为</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin make FORCE_UNSAFE_CONFIGURE=1 -j13 V=s</span><br></pre></td></tr></table></figure>

<ol start="7">
<li>刚开始编译怎么都没有 openwrt-ramips-mt7620-phicomm_psg1218a-squashfs-sysupgrade.bin 文件</li>
</ol>
<p>网上搜索说 固件太大 就没有了，但是我在配置时只做了删除了怎么还更大呢。（未知）</p>
<p>然后我就什么都没有更改 直接编译一次，就成功了，然后再进行删除增加就可以了。</p>
<ol start="8">
<li>copy 配置修改ip时，复制</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">192.168.2.12</span><br><span class="line">24</span><br><span class="line">255.255.255.255</span><br><span class="line">192.168.2.1</span><br><span class="line"></span><br><span class="line">192.168.31.2</span><br><span class="line"></span><br><span class="line">192.168.31.23</span><br></pre></td></tr></table></figure>

<ol start="9">
<li>分享一个 移动wsl子系统的软件 避免wsl系统太占C盘空间</li>
</ol>
<p>下载：<a href="https://github.com/DDoSolitary/LxRunOffline/releases">https://github.com/DDoSolitary/LxRunOffline/releases</a></p>
<p>案例：<a href="https://blog.bccn.net/%E9%9D%99%E5%A4%9C%E6%80%9D/67307">https://blog.bccn.net/%E9%9D%99%E5%A4%9C%E6%80%9D/67307</a></p>
<p>安装wsl子系统 ubuntu-20.04 和 LxRunOffline 软件</p>
<ol>
<li>备份&#x2F;还原WSL</li>
</ol>
<ul>
<li>查看当前系统中存在的WSL</li>
</ul>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">LxRunOffline.exe list or wsl <span class="literal">-l</span></span><br><span class="line">Ubuntu<span class="literal">-20</span>.<span class="number">04</span></span><br></pre></td></tr></table></figure>

<ul>
<li>开始备份</li>
</ul>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">./LxRunOffline.exe export <span class="literal">-n</span> Ubuntu<span class="literal">-20</span>.<span class="number">04</span> <span class="operator">-f</span> E:\wsl_ubuntu_bak.tar.gz</span><br></pre></td></tr></table></figure>

<ul>
<li>还原WSL</li>
</ul>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">./LxRunOffline.exe install <span class="literal">-n</span> Ubuntu <span class="literal">-d</span> D:\wsl_ubuntu <span class="operator">-f</span> E:\wsl_ubuntu_bak.tar.gz</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<ol start="2">
<li>直接移动(新装推荐用这个)</li>
</ol>
<figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 新建文件夹作为移动目标，如：D:\wsl_ubuntu</span></span><br><span class="line">LxRunOffline <span class="built_in">move</span> <span class="literal">-n</span> Ubuntu<span class="literal">-20</span>.<span class="number">04</span> <span class="literal">-d</span> D:\wsl_ubuntu</span><br><span class="line"><span class="comment"># 时间比较久，等待移动完成用下面命令查看</span></span><br><span class="line">LxRunOffline <span class="built_in">get-dir</span> <span class="literal">-n</span> Ubuntu<span class="literal">-20</span>.<span class="number">04</span> </span><br><span class="line"></span><br></pre></td></tr></table></figure>



<p>参考：</p>
<ol>
<li><a href="https://github.com/coolsnowwolf/lede">https://github.com/coolsnowwolf/lede</a></li>
<li><a href="https://www.right.com.cn/forum/thread-5559590-1-1.html">https://www.right.com.cn/forum/thread-5559590-1-1.html</a></li>
<li><a href="https://www.right.com.cn/forum/thread-344825-1-1.html">https://www.right.com.cn/forum/thread-344825-1-1.html</a></li>
<li><a href="https://blog.csdn.net/tiwsonchen/article/details/84924909">https://blog.csdn.net/tiwsonchen/article/details/84924909</a></li>
<li><a href="https://github.com/hanxi/blog/issues/28">https://github.com/hanxi/blog/issues/28</a></li>
</ol>
]]></content>
      <categories>
        <category>路由器</category>
      </categories>
  </entry>
  <entry>
    <title>python 虚拟环境</title>
    <url>/post/python-%E8%99%9A%E6%8B%9F%E7%8E%AF%E5%A2%83.html</url>
    <content><![CDATA[<blockquote>
<p>一直想要一个方便上手的环境配置方案，有时候项目多了管理起来就特别的麻烦。</p>
<p>（以前用的就是virtualenv、virtualenvwrapper-win,后面升级python3后，就用venv，以至于前面的virutalenv和virtualenvwrapper都已经忘了，但是python3 venv，各个版本不好切换。）</p>
<p>因此又在网上搜索查看了一些资料，有推荐Anaconda，安装过后发现太多了，而且导出的requirements 很多引用的本地的，到时候部署到生产环境很麻烦。</p>
<p>又有推荐的miniconda的，但是还没有安装使用，就被另一个方案给安利了。</p>
<p>**pyenv+pyenv-virtualenv ** 可以更好控制版本。</p>
</blockquote>
<span id="more"></span>

<h2 id="pyenv-安装"><a href="#pyenv-安装" class="headerlink" title="pyenv 安装"></a>pyenv 安装</h2><p>pyenv 可以让你更加方便的安装各个python版本，然后利用pyenv-virtualenv就可以直接创建虚拟环境。</p>
<p>但是在windows上只有pyenv-win，没有pyenv-virtualenv，因此也只能使用virtualenv配合virtualenvwrapper-win。也是非常不错的方案。</p>
<h3 id="windows"><a href="#windows" class="headerlink" title="windows"></a>windows</h3><h4 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install pyenv-win --target E:\pyenv\.pyenv <span class="comment">#这里目录可以不存在，会自动创建</span></span><br></pre></td></tr></table></figure>

<h4 id="设置虚拟环境"><a href="#设置虚拟环境" class="headerlink" title="设置虚拟环境"></a>设置虚拟环境</h4><p>在windows中虚拟环境配置 PATH 中添加</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">E:\pyenv\.pyenv\bin</span><br><span class="line">E:\pyenv\.pyenv\shims</span><br></pre></td></tr></table></figure>

<p>可能会发现当前还没有shims文件夹，后面会讲他的作用。</p>
<h2 id="命令"><a href="#命令" class="headerlink" title="命令"></a>命令</h2><h3 id="in·stall安装："><a href="#in·stall安装：" class="headerlink" title="in·stall安装："></a>in·stall安装：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pyenv install --list	<span class="comment">#版本列表</span></span><br><span class="line">pyenv install 3.8.0 -v	<span class="comment">#在线安装指定版本：例如3.8.0</span></span><br></pre></td></tr></table></figure>

<h3 id="版本控制："><a href="#版本控制：" class="headerlink" title="版本控制："></a>版本控制：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pyenv version	<span class="comment">#显示当前版本</span></span><br><span class="line">pyenv versions	<span class="comment">#显示所有可用版本及当前版本</span></span><br><span class="line">pyenv global 3.8.0	<span class="comment">#global为全局设置版本，不建议随意使用</span></span><br><span class="line">pyenv shell 3.8.0	<span class="comment">#shell为当前会话设置版本，关闭失效</span></span><br><span class="line">pyenv <span class="built_in">local</span> 3.8.0	<span class="comment">#local为当前目录设置版本（递归继承）</span></span><br><span class="line">pyenv shims <span class="comment"># 为全局环境设置一个命令环境，因此会创建一个shims文件和上面配合</span></span><br></pre></td></tr></table></figure>

<h3 id="命令大全"><a href="#命令大全" class="headerlink" title="命令大全"></a>命令大全</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pyenv commands：查看所有pyenv命令</span><br><span class="line">pyenv <span class="built_in">exec</span>：使用特定Python版本执行某条命令</span><br><span class="line">pyenv global：设置或查看全局Python版本</span><br><span class="line">pyenv hooks：列出给出的pyenv命令的钩子脚本</span><br><span class="line">pyenv init：配置shell环境</span><br><span class="line">pyenv install：安装某个版本的Python</span><br><span class="line">pyenv <span class="built_in">local</span>：为某个应用设置特定的Python版本</span><br><span class="line">pyenv prefix：显示对应Python版本的路径前缀</span><br><span class="line">pyenv <span class="built_in">rehash</span>：再哈希pyenv shims（不清楚有什么用，欢迎评论补充）</span><br><span class="line">pyenv root：显示pyenv根目录</span><br><span class="line">pyenv shell：为某个shell设置特定的Python版本（与pyenv <span class="built_in">local</span>类似）</span><br><span class="line">pyenv shims：列出当前存在的shims（pyenv的工作原理就是在一个叫shims的目录下创建Python解释器的“假版本”，寻找Python应用时先从该目录查找）</span><br><span class="line">pyenv uninstall：卸载某个版本Python</span><br><span class="line">pyenv --version：显示pyenv版本</span><br><span class="line">pyenv version：显示当前Python版本，等价于pyenv version-name+pyenv version-file</span><br><span class="line">pyenv version-file/version-name/version-origin：当前Python版本对应的文件/名字/位置</span><br><span class="line">pyenv <span class="built_in">whence</span>：列出已安装的一个范围内的Python版本，比如列出本地安装的Python2到Python3的版本，可以使用pyenv <span class="built_in">whence</span> 2to3</span><br><span class="line">pyenv <span class="built_in">which</span>：列出可执行文件的绝对路径，比如pyenv <span class="built_in">which</span> python可以列出python命令的绝对路径</span><br></pre></td></tr></table></figure>

<h3 id="创建虚拟环境"><a href="#创建虚拟环境" class="headerlink" title="创建虚拟环境"></a>创建虚拟环境</h3><p>github：<a href="https://github.com/pyenv/pyenv-virtualenv">https://github.com/pyenv/pyenv-virtualenv</a></p>
<p>利用pyenv-virtualenv快速创建，windows好像不行</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">pyenv virtualenv 3.7.1 TEST</span><br><span class="line">pyenv activate TEST</span><br><span class="line">pyenv deactivate</span><br><span class="line">pyenv uninstall TEST</span><br></pre></td></tr></table></figure>



<h2 id="传统-virtualenvwrapper"><a href="#传统-virtualenvwrapper" class="headerlink" title="传统 virtualenvwrapper"></a>传统 virtualenvwrapper</h2><p>github:<a href="https://github.com/davidmarble/virtualenvwrapper-win/">https://github.com/davidmarble/virtualenvwrapper-win/</a></p>
<h3 id="linux"><a href="#linux" class="headerlink" title="linux"></a>linux</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> apt install virtualenv</span><br><span class="line"><span class="built_in">sudo</span> apt install virtualenvwrapper</span><br><span class="line"><span class="built_in">sudo</span> find / -name <span class="string">&#x27;virtualenvwrapper.sh&#x27;</span></span><br><span class="line"><span class="built_in">mkdir</span> ~/.virtualenvs  or <span class="built_in">mkdir</span> ~/envs</span><br><span class="line"><span class="built_in">export</span> WROKON_HOME=~/.virtualenvs       or  envs</span><br><span class="line"></span><br><span class="line"><span class="comment">###iwnodws 设置的时候 要小写  workon_home </span></span><br><span class="line"><span class="built_in">source</span> /usr/local/bin/virtualenvwrapper.sh  or  使用上马find找到的 virtualenvwrapper.sh</span><br><span class="line"><span class="comment">#### 可以在root 下 .bashrc 最后添加一句export WROKON_HOME=/home/dfk/.virtualenvs 把目录移到外面 方便pycharm导入</span></span><br><span class="line"><span class="built_in">source</span> ~/.bashrc</span><br><span class="line"></span><br><span class="line">mkvirtualenv -p /usr/bin/python3 djangopy3</span><br><span class="line"></span><br><span class="line">workon </span><br><span class="line"></span><br><span class="line">deactivate</span><br><span class="line"></span><br><span class="line">pip freeze &gt; rexx.txt 打印出所有包</span><br><span class="line">pip install -i https://pypi.douban.com/simple -r rexx.txt 安装所有包</span><br></pre></td></tr></table></figure>

<h3 id="windows-1"><a href="#windows-1" class="headerlink" title="windows"></a>windows</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install virtualenvwrapper-win</span><br><span class="line"><span class="comment"># 设置 WORKON_HOME = xxx</span></span><br><span class="line"><span class="comment"># 指定版本 创建虚拟环境</span></span><br><span class="line">mkvirtualenv --python=xxx virtualenv_name</span><br><span class="line">workon <span class="comment"># 查看</span></span><br><span class="line">workon virtualenv_name <span class="comment"># 进入虚拟环境</span></span><br><span class="line">deactivate <span class="comment"># 退出虚拟环境</span></span><br></pre></td></tr></table></figure>



<p>参考：<a href="https://segmentfault.com/a/1190000039173896">https://segmentfault.com/a/1190000039173896</a></p>
<p>参考：<a href="https://blog.csdn.net/OwangxinyangO/article/details/105712185">https://blog.csdn.net/OwangxinyangO/article/details/105712185</a></p>
<p>参考：<a href="https://zhuanlan.zhihu.com/p/27294128">https://zhuanlan.zhihu.com/p/27294128</a></p>
<p>python版本管理 对比：<a href="https://stackoverflow.com/questions/41573587/what-is-the-difference-between-venv-pyvenv-pyenv-virtualenv-virtualenvwrappe#:~:text=virtualenvwrapper%20is%20a%20set%20of,switching%20between%20different%20virtualenv%20directories.&text=This%20is%20different%20from%20virtualenv,in%20the%20current%20working%20directory">https://stackoverflow.com/questions/41573587/what-is-the-difference-between-venv-pyvenv-pyenv-virtualenv-virtualenvwrappe#:~:text=virtualenvwrapper%20is%20a%20set%20of,switching%20between%20different%20virtualenv%20directories.&amp;text=This%20is%20different%20from%20virtualenv,in%20the%20current%20working%20directory</a>.</p>
]]></content>
      <categories>
        <category>python</category>
      </categories>
      <tags>
        <tag>python</tag>
      </tags>
  </entry>
  <entry>
    <title>scrapy + scrapyd + scrapydweb + logparser + docker分布式部署</title>
    <url>/post/scrapy-scrapyd-scrapydweb-logparser-docker%E5%88%86%E5%B8%83%E5%BC%8F%E9%83%A8%E7%BD%B2.html</url>
    <content><![CDATA[<p>在工作中使用，特此在这记录一下，并写成docker文件，方便部署使用。</p>
<span id="more"></span>

<h5 id="1-构建-scrapyd-logparser"><a href="#1-构建-scrapyd-logparser" class="headerlink" title="1.构建 scrapyd_logparser"></a>1.构建 scrapyd_logparser</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cd scrapyd_logparser</span><br><span class="line">docker build -t scrapyd_logparser .</span><br></pre></td></tr></table></figure>

<h5 id="2-运行-scrapyd-logparser"><a href="#2-运行-scrapyd-logparser" class="headerlink" title="2.运行 scrapyd_logparser"></a>2.运行 scrapyd_logparser</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker run -d -p 6800:6800 --name scrapyd_1 scrapyd_logparser</span><br><span class="line"># docker run -d -p 6800:6800 -v /root/scrapyd_logparser:/code --name scrapyd_1 scrapyd_logparser</span><br><span class="line"># 可以外联出文件 可以进行配置修改</span><br></pre></td></tr></table></figure>

<h5 id="3-构建-scrapydweb"><a href="#3-构建-scrapydweb" class="headerlink" title="3.构建 scrapydweb"></a>3.构建 scrapydweb</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cd scrapydweb</span><br><span class="line">docker build -t scrapydweb .</span><br></pre></td></tr></table></figure>

<h5 id="4-运行-scrapydweb"><a href="#4-运行-scrapydweb" class="headerlink" title="4.运行 scrapydweb"></a>4.运行 scrapydweb</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">docker run -d -p 5000:5000 -v /root/scrapydweb:/code --name scrapydweb scrapydweb</span><br><span class="line"># 外链出文件 好修改配置 进行更新部署</span><br><span class="line"># 修改配置</span><br><span class="line">vim scrapydweb_settings_v10.py </span><br><span class="line"># 重启</span><br><span class="line">docker restart scrapydweb</span><br></pre></td></tr></table></figure>

<h5 id="5-多机部署"><a href="#5-多机部署" class="headerlink" title="5.多机部署"></a>5.多机部署</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">在其他机器中 部署 文件 执行 1，2 步</span><br><span class="line">同时在 scrapydweb 的配置文件 添加在 新主机的ip</span><br><span class="line"></span><br><span class="line"># 以下添加</span><br><span class="line">SCRAPYD_SERVERS = [</span><br><span class="line">    &#x27;192.168.5.131:6800&#x27;,</span><br><span class="line">	&#x27;192.168.5.131:6805&#x27;,</span><br><span class="line">    # &#x27;username:password@localhost:6801#group&#x27;,</span><br><span class="line">    # (&#x27;username&#x27;, &#x27;password&#x27;, &#x27;localhost&#x27;, &#x27;6801&#x27;, &#x27;group&#x27;),</span><br><span class="line">]</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p><strong>源码</strong>: <a href="https://github.com/libaibuaidufu/scrapyd_web_log">https://github.com/libaibuaidufu/scrapyd_web_log</a><br><strong>注</strong>： 可以自己查看 一下 scrapydweb 配置文件 修改，和 scrapyd 配置文件修改</p>
<p>参考：</p>
<ol>
<li><a href="https://github.com/my8100/scrapydweb">scrapydweb</a></li>
<li><a href="https://github.com/my8100/logparser">logparser</a></li>
</ol>
]]></content>
      <categories>
        <category>代码来了</category>
      </categories>
      <tags>
        <tag>scrapy</tag>
        <tag>docker</tag>
      </tags>
  </entry>
  <entry>
    <title>scrapy+scrapyd 部署使用</title>
    <url>/post/scrapy-scrapyd-%E9%83%A8%E7%BD%B2%E4%BD%BF%E7%94%A8.html</url>
    <content><![CDATA[<h3 id="scrapyd-部署"><a href="#scrapyd-部署" class="headerlink" title="scrapyd 部署"></a>scrapyd 部署</h3><p><strong>注</strong>：example 为项目名称</p>
<h5 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h5><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install scrapyd <span class="comment"># 快速部署爬虫 服务端</span></span><br><span class="line">pip install scrapyd-client <span class="comment"># 快速部署爬虫 客户端</span></span><br><span class="line">pip install scrapyd-api <span class="comment"># 集成python可调用api</span></span><br><span class="line">pip install scrapydweb <span class="comment"># web页面</span></span><br><span class="line">pip install logparser <span class="comment"># 日志解析</span></span><br></pre></td></tr></table></figure>

<h5 id="启动-scrapyd"><a href="#启动-scrapyd" class="headerlink" title="启动 scrapyd"></a>启动 scrapyd</h5><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scrapyd -p example</span><br></pre></td></tr></table></figure>

<h5 id="部署-爬虫"><a href="#部署-爬虫" class="headerlink" title="部署 爬虫"></a>部署 爬虫</h5><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">scrapyd-deploy -p example</span><br><span class="line"></span><br><span class="line">scrapyd-deploy &lt;target&gt; -p example</span><br></pre></td></tr></table></figure>

<h5 id="添加爬虫"><a href="#添加爬虫" class="headerlink" title="添加爬虫"></a>添加爬虫</h5><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl http://localhost:6800/schedule.json -d project=example -d spider=dongtan</span><br></pre></td></tr></table></figure>

<h5 id="取消爬虫"><a href="#取消爬虫" class="headerlink" title="取消爬虫"></a>取消爬虫</h5><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">curl http://localhost:6800/cancel.json -d project=example -d job=68d25db0506111e9a4c0e2df1c2eb35b</span><br><span class="line"><span class="comment"># job 为可在web查看</span></span><br></pre></td></tr></table></figure>

<h5 id="启动scrapyweb"><a href="#启动scrapyweb" class="headerlink" title="启动scrapyweb"></a>启动scrapyweb</h5><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 第一次运行 一下命令 生成一个配置文件 scrapydweb_settings_v10.py</span></span><br><span class="line">scrapydweb </span><br><span class="line"><span class="comment"># 第二次运行 则进行运行 同目录下</span></span><br><span class="line">scrapydweb </span><br></pre></td></tr></table></figure>

<h5 id="启动logparser"><a href="#启动logparser" class="headerlink" title="启动logparser"></a>启动logparser</h5><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 修改 scrapydweb_settings_v10.py 一下为Ture 会自动生成 stats.json 在日志目录下，可以重启一下scrapydweb</span></span><br><span class="line">ENABLE_LOGPARSER = True</span><br><span class="line"></span><br><span class="line"><span class="comment"># 然后运行 就可以了</span></span><br><span class="line">logparser </span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h5 id="scrapy-cfg"><a href="#scrapy-cfg" class="headerlink" title="scrapy.cfg"></a>scrapy.cfg</h5><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line">[<span class="string">settings</span>]</span><br><span class="line"><span class="string">default</span> <span class="string">=</span> <span class="string">example.settings</span></span><br><span class="line">[<span class="string">deploy</span>]</span><br><span class="line"><span class="string">url</span> <span class="string">=</span> <span class="string">http://localhost:6800/</span></span><br><span class="line"><span class="string">project</span> <span class="string">=</span> <span class="string">example</span></span><br><span class="line">[<span class="string">deploy:djwq</span>]</span><br><span class="line"><span class="string">url</span> <span class="string">=</span> <span class="string">http://localhost:2100/</span></span><br><span class="line"><span class="string">project</span> <span class="string">=</span> <span class="string">example</span></span><br><span class="line"><span class="comment"># scrapyd-deploy -p example</span></span><br><span class="line"><span class="comment"># scrapyd-deploy djwq -p example</span></span><br></pre></td></tr></table></figure>



<p>源码地址：<a href="https://github.com/libaibuaidufu/scrapyd_web_log">https://github.com/libaibuaidufu/scrapyd_web_log</a></p>
]]></content>
      <categories>
        <category>代码来了</category>
      </categories>
      <tags>
        <tag>scrapy</tag>
        <tag>scrapyd</tag>
      </tags>
  </entry>
  <entry>
    <title>selenium 反爬虫识别特征处理</title>
    <url>/post/selenium-%E5%8F%8D%E7%88%AC%E8%99%AB%E8%AF%86%E5%88%AB%E7%89%B9%E5%BE%81%E5%A4%84%E7%90%86.html</url>
    <content><![CDATA[<p><strong>因为业务中发现网站对selenium特征识别为爬虫了，因此在搜索引擎中搜索进行处理</strong></p>
<span id="more"></span>

<h3 id="方式一"><a href="#方式一" class="headerlink" title="方式一"></a>方式一</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 实例化一个浏览器对象</span></span><br><span class="line">options = webdriver.ChromeOptions()</span><br><span class="line">options.add_experimental_option(<span class="string">&#x27;excludeSwitches&#x27;</span>, [<span class="string">&#x27;enable-automation&#x27;</span>])</span><br><span class="line"><span class="keyword">if</span> sys.platform == <span class="string">&quot;win32&quot;</span>:</span><br><span class="line">    browser = webdriver.Chrome(driver_path, options=options)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    browser = webdriver.Chrome(options=options)</span><br><span class="line">browser.implicitly_wait(<span class="number">30</span>)</span><br><span class="line">browser.execute_cdp_cmd(<span class="string">&quot;Page.addScriptToEvaluateOnNewDocument&quot;</span>, &#123;</span><br><span class="line">    <span class="string">&quot;source&quot;</span>: <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">    Object.defineProperty(navigator, &#x27;webdriver&#x27;, &#123;</span></span><br><span class="line"><span class="string">      get: () =&gt; undefined</span></span><br><span class="line"><span class="string">    &#125;)</span></span><br><span class="line"><span class="string">  &quot;&quot;&quot;</span></span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>此方法虽然可以躲避识别，但是在使用过一段时候后，依然出现被屏蔽的问题。</p>
<p>因此只有继续搜索 然后再stackoverflow 中又找到了新的答案。</p>
<h3 id="方式二-（推荐）"><a href="#方式二-（推荐）" class="headerlink" title="方式二 （推荐）"></a>方式二 （推荐）</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 实例化一个浏览器对象</span></span><br><span class="line">options = webdriver.ChromeOptions()</span><br><span class="line">options.add_argument(<span class="string">&quot;--disable-blink-features=AutomationControlled&quot;</span>)</span><br><span class="line"><span class="keyword">if</span> sys.platform == <span class="string">&quot;win32&quot;</span>:</span><br><span class="line">    browser = webdriver.Chrome(driver_path, options=options)</span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    browser = webdriver.Chrome(options=options)</span><br></pre></td></tr></table></figure>

<p>这个方法执行的后，目前运行良好也没有出现被识别的问题。</p>
<h3 id="关闭当前标签-在切换标签前关闭"><a href="#关闭当前标签-在切换标签前关闭" class="headerlink" title="关闭当前标签 在切换标签前关闭"></a>关闭当前标签 在切换标签前关闭</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">driver.close()</span><br><span class="line">windows = driver.window_handles</span><br><span class="line">driver.switch_to.window(windows[-<span class="number">1</span>])</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>代码来了</category>
      </categories>
      <tags>
        <tag>selenium</tag>
      </tags>
  </entry>
  <entry>
    <title>wsl 定时任务 crontab权限</title>
    <url>/post/wsl-%E5%AE%9A%E6%97%B6%E4%BB%BB%E5%8A%A1-crontab%E6%9D%83%E9%99%90.html</url>
    <content><![CDATA[<p>可能有一些乱,但是不影响整体思路。</p>
<h3 id="安装-wsl"><a href="#安装-wsl" class="headerlink" title="安装 wsl"></a>安装 wsl</h3><p>首先在windows上 打开 <strong>启动或关闭windows功能</strong>  选择 <strong>适用于 Linux 的 windows 子系统</strong> 打勾</p>
<p>然后取 windows应用市场 搜索 wsl ，下载你喜欢的一个 linux子系统</p>
<p>我这边安装了一个 debian，一个ubuntu。</p>
<p>如果你没有开启<strong>适用于 Linux 的 windows 子系统</strong> 是无法安装的。</p>
<p><strong>记得一定要安装windows terminal 这个太提升体验了</strong></p>
<span id="more"></span>

<h3 id="配置linux子系统"><a href="#配置linux子系统" class="headerlink" title="配置linux子系统"></a>配置linux子系统</h3><p>进入 debian系统</p>
<h4 id="挂载windows和linux的共享盘"><a href="#挂载windows和linux的共享盘" class="headerlink" title="挂载windows和linux的共享盘"></a>挂载windows和linux的共享盘</h4><p>可以先在linux子系统和windows之间建立一个共享盘</p>
<p>挂载 windows D盘的wsl_linux 到 debian到 win10目录</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">ln</span> -s /mnt/d/wsl_linux ~/win10 </span><br></pre></td></tr></table></figure>

<h4 id="换国内源"><a href="#换国内源" class="headerlink" title="换国内源"></a>换国内源</h4><p>清华源地址:<a href="https://mirrors.tuna.tsinghua.edu.cn/help/debian/">https://mirrors.tuna.tsinghua.edu.cn/help/debian/</a></p>
<p><strong>这是debian10</strong></p>
<p>在windows中 创建一个 sources.list ,同样也可以在linux下创建，看你喜欢</p>
<p>把内容直接复制到sources.list 里面，因为无法拉去 https ，所以可以吧所有的https改为http</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># 默认注释了源码镜像以提高 apt update 速度，如有需要可自行取消注释</span><br><span class="line">deb http://mirrors.tuna.tsinghua.edu.cn/debian/ buster main contrib non-free</span><br><span class="line">deb http://mirrors.tuna.tsinghua.edu.cn/debian/ buster-updates main contrib non-free</span><br><span class="line">deb http://mirrors.tuna.tsinghua.edu.cn/debian/ buster-backports main contrib non-free</span><br><span class="line">deb http://mirrors.tuna.tsinghua.edu.cn/debian-security buster/updates main contrib non-free</span><br></pre></td></tr></table></figure>

<p>然后把 文件复制到 &#x2F;etc&#x2F;apt中</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sudo</span> <span class="built_in">mv</span> /etc/apt/sources.list /etc/apt/sources.list.bak</span><br><span class="line"><span class="built_in">sudo</span> <span class="built_in">cp</span> ~/win10/sources.list /etc/apt/sources.list</span><br><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="comment">#安装 ca证书 就可以拉去 https</span></span><br><span class="line"><span class="built_in">sudo</span> apt install apt-transport-https ca-certificates</span><br><span class="line"><span class="comment">#此时就可以修改所有http为 https了</span></span><br><span class="line"><span class="comment"># 然后在执行一次</span></span><br><span class="line"><span class="built_in">sudo</span> apt update</span><br><span class="line"><span class="comment"># 安装一些我需要的软件</span></span><br><span class="line"><span class="built_in">sudo</span> apt install crul wget vim -y</span><br></pre></td></tr></table></figure>

<p>此时debian的系统就调试的差不多了。</p>
<h3 id="设置定时任务-crontab-及其权限"><a href="#设置定时任务-crontab-及其权限" class="headerlink" title="设置定时任务 crontab 及其权限"></a>设置定时任务 crontab 及其权限</h3><p>设置定时任务 crontab  ，因为crontab定时任务的命令网上太多了一搜一大堆，我这里就不复制，粘贴了。</p>
<h4 id="编辑定时任务"><a href="#编辑定时任务" class="headerlink" title="编辑定时任务"></a>编辑定时任务</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 第一次 他需要让你选择打开使用的编辑器 选择 vim就好了 或者 选择自己熟悉的</span></span><br><span class="line">crontab -e</span><br><span class="line">0 21 * * * <span class="built_in">cd</span> /home/libai/fping-4.2/src/ &amp;&amp; <span class="built_in">sudo</span> /bin/bash /home/libai/fping-4.2/src/cf.sh &gt;&gt; /var/log/cf_log.log</span><br><span class="line"><span class="comment"># 查看</span></span><br><span class="line">crontab -l</span><br><span class="line"><span class="comment"># 重启 这样才会设置任务 ### 认识错误 不用重启  他会自己去读取</span></span><br><span class="line"><span class="built_in">sudo</span> service cron restart <span class="comment"># debian</span></span><br><span class="line"><span class="built_in">sudo</span> systemctl restart crond.service <span class="comment"># centos</span></span><br></pre></td></tr></table></figure>

<p>我的这个任务是每天晚上九点 执行一次。</p>
<p>因为执行命令需要root权限。我们需要设置执行我们这个命令时候，不需要输入密码。</p>
<h4 id="设置-visudo-，定时任务权限"><a href="#设置-visudo-，定时任务权限" class="headerlink" title="设置 visudo ，定时任务权限"></a>设置 visudo ，定时任务权限</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 打开 visudo</span></span><br><span class="line"><span class="built_in">sudo</span> visudo</span><br><span class="line"><span class="comment"># 在末尾添加 这个用的是nano编辑器</span></span><br><span class="line">%<span class="built_in">sudo</span> ALL=NOPASSWD: /etc/init.d/cron start</span><br><span class="line">%<span class="built_in">sudo</span> ALL=NOPASSWD: /bin/bash /home/libai/fping-4.2/src/cf.sh</span><br><span class="line"><span class="comment"># 这里的命令要和上面crontab -e 命令保持一致 路径也一样 不然还是要输入密码的</span></span><br><span class="line"><span class="comment"># 路径 和 目录写清楚 可从下面参考里面看详细的visudo命令解释</span></span><br><span class="line"><span class="comment"># who     where=(whom)    commands</span></span><br><span class="line"><span class="comment"># 用户    主机=（以谁的身份） commands</span></span><br><span class="line"><span class="comment"># nano 命令</span></span><br><span class="line"><span class="comment"># 保存</span></span><br><span class="line">Ctrl+O 然后回车一下就可以了</span><br><span class="line"><span class="comment"># 推出</span></span><br><span class="line">Ctrl+X</span><br></pre></td></tr></table></figure>

<p>此时定时任务就可以起飞了，同时把任务日志保存一下，看看是不是真的运行了。同时运行有什么问题，也可以处理。** &gt;&gt; &#x2F;var&#x2F;log&#x2F;cf_log.log** 注意创建此文件同时注意权限 <strong>chmod 777 cf_log.log</strong></p>
<h3 id="关闭linux子系统，windows开机启动"><a href="#关闭linux子系统，windows开机启动" class="headerlink" title="关闭linux子系统，windows开机启动"></a>关闭linux子系统，windows开机启动</h3><p>但是这样当我们关闭windows子系统的时候，整个就关闭了，就没有什么定时任务了，因此我们要创建一个windows的开启任务。</p>
<p>在windows下：</p>
<p>win+R 输入 shell:startup 打开目录</p>
<p>新建一个 快捷方式</p>
<p>输入 <strong>C:\Windows\System32\wsl.exe sudo &#x2F;etc&#x2F;init.d&#x2F;cron start</strong> </p>
<p>你发现这个命令和我们上面visudo的添加第一个命令一致，但是我开机启动时，还是要让我输入密码。。。</p>
<p><strong>需要输入密码是因为wsl默认启动我的ubuntu，修改默认启动顺序，如果你只有一个系统就不会出现这个情况或者你用的系统是你第一个安装应该也是没有问题的</strong></p>
<h4 id="查看、修改默认系统"><a href="#查看、修改默认系统" class="headerlink" title="查看、修改默认系统"></a>查看、修改默认系统</h4><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="built_in">PS</span> C:\Users\dfk&gt; wslconfig /list</span><br><span class="line">适用于 Linux 的 Windows 子系统:</span><br><span class="line">Ubuntu<span class="literal">-20</span>.<span class="number">04</span> (默认)</span><br><span class="line">Debian</span><br><span class="line"><span class="built_in">PS</span> C:\Users\dfk&gt; wslconfig /s Debian <span class="comment"># 修改默认</span></span><br><span class="line"><span class="built_in">PS</span> C:\Users\dfk&gt; wsl <span class="comment"># 进入系统</span></span><br><span class="line">libai@DESKTOP<span class="literal">-3SJEOBV</span>:/mnt/c/Users/dfk<span class="variable">$</span> uname <span class="literal">-a</span> <span class="comment"># 查看是否是 debian</span></span><br><span class="line">Linux DESKTOP<span class="literal">-3SJEOBV</span> <span class="number">4.4</span>.<span class="number">0</span><span class="literal">-18362-Microsoft</span> <span class="comment">#836-Microsoft Mon May 05 16:04:00 PST 2020 x86_64 GNU/Linux</span></span><br><span class="line">libai@DESKTOP<span class="literal">-3SJEOBV</span>:/mnt/c/Users/dfk<span class="variable">$</span> <span class="built_in">cat</span> /etc/issue</span><br><span class="line">Debian GNU/Linux <span class="number">10</span> \n \l</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>命令帮助：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">对 Windows 上的 Linux 子系统进行操作管理</span><br><span class="line"></span><br><span class="line">用途：</span><br><span class="line">    /l, /list [/all]</span><br><span class="line">        列出已注册的分发版。</span><br><span class="line">        /all - 有选择地列出所有分发版，包括</span><br><span class="line">               当前正在安装或卸载的分发版。</span><br><span class="line"></span><br><span class="line">    /s, /setdefault &lt;DistributionName&gt;</span><br><span class="line">        将该分发版设为默认。</span><br><span class="line"></span><br><span class="line">    /t, /terminate &lt;DistributionName&gt;</span><br><span class="line">        终止分发。</span><br><span class="line"></span><br><span class="line">    /u, /unregister &lt;DistributionName&gt;</span><br><span class="line">        取消分发版注册。</span><br><span class="line"></span><br><span class="line">    /upgrade &lt;DistributionName&gt;</span><br><span class="line">        将分发版升级至 WslFs 文件系统格式。</span><br></pre></td></tr></table></figure>

<h4 id="最后再次执行-快捷方式-看看-要不要密码"><a href="#最后再次执行-快捷方式-看看-要不要密码" class="headerlink" title="最后再次执行 快捷方式 看看 要不要密码"></a>最后再次执行 快捷方式 看看 要不要密码</h4><p>不要了 完美了。。。。。。</p>
<h4 id="查看windows是否开启"><a href="#查看windows是否开启" class="headerlink" title="查看windows是否开启"></a>查看windows是否开启</h4><p>查看 任务管理系统，里面有没有一个cron的进程，有就是成功了的。</p>
<p>参考：</p>
<ol>
<li><a href="https://www.jianshu.com/p/fff3ccc3e04e">sudo和它的编辑工具visudo</a></li>
<li><a href="%5Bhttps://naizi.moe/2020/01/24/wsl-crontab/#%E5%AE%89%E8%A3%85WSL%5D(https://naizi.moe/2020/01/24/wsl-crontab/#%E5%AE%89%E8%A3%85WSL)">Windows使用WSL配置crontab定时任务</a></li>
<li><a href="https://sxi.io/how-to-start-stop-restart-cron-service-in-linux-centos-7/">How To Start&#x2F;Stop&#x2F;Restart Cron Service In Linux Centos 7</a></li>
</ol>
]]></content>
      <categories>
        <category>wsl</category>
      </categories>
      <tags>
        <tag>wsl</tag>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>Android逆向流程</title>
    <url>/post/%E5%AE%89%E5%8D%93%E9%80%86%E5%90%91%E6%B5%81%E7%A8%8B.html</url>
    <content><![CDATA[<p>工具地址：</p>
<ol>
<li>kstools <a href="https://github.com/fourbrother/kstools">https://github.com/fourbrother/kstools</a></li>
<li>52爱盘工具 <a href="https://down.52pojie.cn/Tools/Android_Tools/">https://down.52pojie.cn/Tools/Android_Tools/</a></li>
<li>AndroidKiller (下面同理都在爱盘里面有)</li>
<li>apkIDE</li>
<li>apktools</li>
<li>dex2jar</li>
<li>jadx</li>
<li>jd-gui</li>
</ol>
<span id="more"></span>

<h3 id="kstools-爆破-签名校验"><a href="#kstools-爆破-签名校验" class="headerlink" title="kstools 爆破 签名校验"></a>kstools 爆破 签名校验</h3><p>有一些程序有签名的校验，当你修改完，打包的时候后，可以安装，但是打开闪退，或者弹出提示框，大部分就是有签名校验。</p>
<p>下载kstools工具，没有加固的就使用kstools.bat,直接搞定。注意appt的配置，我是用的apkIDE里面的appt。</p>
<h3 id="AndroidKiller-集成软件"><a href="#AndroidKiller-集成软件" class="headerlink" title="AndroidKiller 集成软件"></a>AndroidKiller 集成软件</h3><p>AndroidKiller  集成了常用的软件，配上可视化操作，十分好上手，就是里面的插件有点久了，需要自己手动替换为爱盘里面的最新版插件。</p>
<ol>
<li>配置java环境，配置最新版的apktools.</li>
<li>把爆破签名后生成的apk，拖进去，可自己动反编译。</li>
<li>如果未修改签名，有一些app无法编译成功，会在编译死，需要关闭再次打开才可以，最好先爆破一下</li>
<li>修改smail文件，保存</li>
<li>编译，然后连接手机，安装</li>
<li>测试是否可用</li>
</ol>
<h3 id="单独使用工具"><a href="#单独使用工具" class="headerlink" title="单独使用工具"></a>单独使用工具</h3><h4 id="apktools-资源编译"><a href="#apktools-资源编译" class="headerlink" title="apktools 资源编译"></a>apktools 资源编译</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">java -jar apktool_2.3.1.jar d -f KeyValueTest.apk -o KeyValueTest</span><br><span class="line">参数说明：</span><br><span class="line">-f 如果目标文件夹已存在，则强制删除现有文件夹（默认如果目标文件夹已存在，则解码失败）。</span><br><span class="line">-o 指定解码目标文件夹的名称（默认使用APK文件的名字来命名目标文件夹）。</span><br><span class="line">-s 不反编译dex文件，也就是说classes.dex文件会被保留（默认会将dex文件解码成smali文件）。</span><br><span class="line">-r 不反编译资源文件，也就是说resources.arsc文件会被保留（默认会将resources.arsc解码成具体的资源文件）</span><br></pre></td></tr></table></figure>

<h4 id="dex2jar-代码编译-或者-jadx-代码编译"><a href="#dex2jar-代码编译-或者-jadx-代码编译" class="headerlink" title="dex2jar 代码编译 或者 jadx 代码编译"></a>dex2jar 代码编译 或者 jadx 代码编译</h4><p><strong>dex2jar</strong></p>
<p>将apk文件修改后缀为zip然后解压或者直接解压，找到<strong>classes.dex</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">d2j-dex2jar.bat classes.dex</span><br></pre></td></tr></table></figure>

<p>生成了classes-dex2jar.jar 一个jar包。</p>
<p><strong>jadx</strong></p>
<p>jadx更加方便快捷，打开bin文件中的jadx-gui.bat然后直接选择APK即可,虽然jadx也可反编译资源文件，但有时候并不准确</p>
<h4 id="jd-gui-查看jar包"><a href="#jd-gui-查看jar包" class="headerlink" title="jd-gui 查看jar包"></a>jd-gui 查看jar包</h4><p><strong>启动 jd-gui</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">java -jar jd-gui.jar</span><br></pre></td></tr></table></figure>

<p><strong>把生成的classes-dex2jar.jar 拖进去，就可以查看源码了，但是不能修改，可以帮助理解</strong></p>
<h4 id="重新打包签名"><a href="#重新打包签名" class="headerlink" title="重新打包签名"></a>重新打包签名</h4><h5 id="打包"><a href="#打包" class="headerlink" title="打包"></a>打包</h5><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">java -jar apktool_2.3.1.jar b KeyValueTest</span><br></pre></td></tr></table></figure>

<p>目录下会生成build和dist目录,dist 就有新的apk</p>
<h5 id="生成keystore证书"><a href="#生成keystore证书" class="headerlink" title="生成keystore证书"></a>生成keystore证书</h5><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">keytool -genkey -v -keystore myApp.keystore -<span class="built_in">alias</span> myApp.keystore -keyalg RSA -validity 30000</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>key</th>
<th>dec</th>
</tr>
</thead>
<tbody><tr>
<td>keytool</td>
<td>工具名称（固定写法）</td>
</tr>
<tr>
<td>)-genkey</td>
<td>执行的是生成数字证书操作（固定写法）</td>
</tr>
<tr>
<td>-v</td>
<td>打印生成证书的详细信息</td>
</tr>
<tr>
<td>-keystore myApp.keystore</td>
<td>生成的证书的文件名为”myApp.keystore”(根据需求，设置你的证书名)</td>
</tr>
<tr>
<td>alias myApp.keystore</td>
<td>证书的别名为”myApp.keystore”。(一般和上面的文件名相同，可以不同，但要记好，签名时会用**(A)**)</td>
</tr>
<tr>
<td>-keyalg RSA</td>
<td>生成密钥文件采用的算法为RSA(固定写法)</td>
</tr>
<tr>
<td>-validity 3000</td>
<td>该数字证书的有效期为30000天，30000天之后该证书将失效</td>
</tr>
</tbody></table>
<h5 id="签名"><a href="#签名" class="headerlink" title="签名"></a>签名</h5><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">jarsigner -verbose -keystore myApp.keystore -signedjar android-release-signed.apk android-release-unsigned.apk myApp.keystore</span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>key</th>
<th>dec</th>
</tr>
</thead>
<tbody><tr>
<td>jarsigner</td>
<td>工具称名（固定写法）</td>
</tr>
<tr>
<td>-verbose</td>
<td>显示出签名详细信息</td>
</tr>
<tr>
<td>-keystore myApp.keystore myApp.keystore</td>
<td>要使用的签名证书文件</td>
</tr>
<tr>
<td>-signedjar android-release-signed.apk</td>
<td>签名后的文件名</td>
</tr>
<tr>
<td>android-release-unsigned.apk</td>
<td>要签名的文件</td>
</tr>
<tr>
<td>myApp.keystore</td>
<td>别名，即(A)步中的别名</td>
</tr>
</tbody></table>
<p>我这个还是比较简单的流程，还有深入的smail学习，因为逆向主要还是修改smail语句。或者大佬的修改so文件，还有hook，去签名校验，还有去壳等。</p>
<p>路还是很长的。</p>
<p>例如：smail，baksmail这个软件我还没有使用到。</p>
<p>参考：</p>
<ol>
<li><a href="https://www.cnblogs.com/xiaoshen666/p/11008255.html">https://www.cnblogs.com/xiaoshen666/p/11008255.html</a></li>
<li><a href="https://blog.csdn.net/weitao_666/article/details/97615961">https://blog.csdn.net/weitao_666/article/details/97615961</a></li>
</ol>
]]></content>
      <categories>
        <category>Android逆向</category>
      </categories>
      <tags>
        <tag>Android</tag>
      </tags>
  </entry>
  <entry>
    <title>转载-flask源码分析</title>
    <url>/post/%E8%BD%AC%E8%BD%BD-flask%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90.html</url>
    <content><![CDATA[<p>本来想着写第二篇的，但是一直没有什么头绪，就把以前自己看过的文章转载一下，同时在温故而知新。然后在根据理解在进行写。我要转载也是因为别人写的好很详细。</p>
<p>同时还有更详细的 就是此作者在后面的参考中的。当然我也是看过的，别人写的更加详细。以模块分类进行深度讲解。</p>
<p>看了下面两篇可以让你对Flask有着更深的认识。</p>
<p>后续我也会写一些自己的认识。</p>
<span id="more"></span>

<p>转载：<a href="https://juejin.im/post/6844903533238566925">flask 源码解析</a></p>
<p>文章系列：<a href="https://cizixs.com/2017/01/10/flask-insight-introduction/">flask 源码解析：简介</a></p>
<h1 id="flask-源码解析"><a href="#flask-源码解析" class="headerlink" title="flask 源码解析"></a>flask 源码解析</h1><p>本文简单的分析了 Flask 的源码，主要关注 WSGI、Flask 对象的数据结构、Flask 应用启动过程、请求处理过程、视图函数、URL 的映射、应用上下文和请求上下文。讲解这些主题时也不会面面俱到，请按照你阅读源码的需要自行探索。要读懂本文，你需要较为熟悉 Flask，比如已经用 Flask 写过一个小项目，并且有一定的阅读代码的能力，并对 web 框架的功能有基本了解。</p>
<p>本文会不时更新，最近更新日期：2017年9月10日。</p>
<p>这是 Flask 官方钦定的 Demo 代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> flask <span class="keyword">import</span> Flask</span><br><span class="line">app = Flask(__name__)</span><br><span class="line"></span><br><span class="line"><span class="meta">@app.route(<span class="params">‘/‘</span>)</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">index</span>():</span><br><span class="line">    <span class="keyword">return</span> ‘Hello, world!’</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == ‘__main__’:</span><br><span class="line">    app.run()</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>这篇文章从这个简单的代码开始，简要介绍了 WSGI、Flask 对象的数据结构、Flask 应用启动过程、请求处理过程、视图函数、URL 的映射、request 和 response 类（应用上下文和请求上下文），这些主题涵盖了一个 web 框架的核心。</p>
<h2 id="WSGI"><a href="#WSGI" class="headerlink" title="WSGI"></a>WSGI</h2><p>在用户发起的请求到达服务器之后，会被一个 HTTP 服务器所接收，然后交给 web 应用程序做业务处理，这样 HTTP 服务器和 web 应用之间就需要一个接口，在 Python web 开发的世界里，Python 官方<strong>钦定</strong>了这个接口并命名为 WSGI，由 PEP333 所规定。只要服务器和框架都遵守这个约定，那么就能实现服务器和框架的任意组合。按照这个规定，一个面向 WSGI 的框架必须要实现这个方法：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">application</span>(<span class="params">environ, start_response</span>)</span><br></pre></td></tr></table></figure>

<p>在工作过程中，HTTP 服务器会调用上面这个方法，传入请求信息，即名为 <code>environ</code> 的字典和 <code>start_response</code> 函数，应用从 <code>environ</code> 中获取请求信息，在进行业务处理后调用 <code>start_response</code> 设置响应头，并返回响应体（必须是一个可遍历的对象，比如列表、字典）给 HTTP 服务器，HTTP 服务器再返回响应给用户。</p>
<p>所以 Flask 作为一个开发 web 应用的 web 框架，负责解决的问题就是：</p>
<ol>
<li>作为一个应用，能够被 HTTP 服务器所调用，必须要有 <code>__call__</code> 方法</li>
<li>通过传入的请求信息（URL、HTTP 方法等），找到正确的业务处理逻辑，即正确的视图函数</li>
<li>处理业务逻辑，这些逻辑可能包括表单检查、数据库 CRUD 等（这个在这篇文章里不会涉及）</li>
<li>返回正确的响应</li>
<li>在同时处理多个请求时，还需要保护这些请求，知道应该用哪个响应去匹配哪个请求，即线程保护</li>
</ol>
<p>下面就来看看 Flask 是如何解决这些问题的。</p>
<blockquote>
<p>参考阅读：<a href="http://python.jobbole.com/81524/">一起写一个 web 服务器</a>，该系列文章能够让你基本理解 web 服务器和框架是如何通过 WSGI 协同工作的。</p>
</blockquote>
<h2 id="应用的创建"><a href="#应用的创建" class="headerlink" title="应用的创建"></a>应用的创建</h2><blockquote>
<p>源码阅读：<code>app.py</code> 中 <code>Flask</code> 类的代码。</p>
</blockquote>
<p>Demo 代码的第二行创建了一个 Flask 类的实例，传入的参数是当前模块的名字。我们先来看看 Flask 应用到底是什么，它的数据结构是怎样的。</p>
<p><code>Flask</code> 是这样一个类：</p>
<blockquote>
<p>The flask object implements a WSGI application and acts as the central object. It is passed the name of the module or package of the application. Once it is created it will act as a central registry for the view functions, the URL rules, template configuration and much more.</p>
</blockquote>
<blockquote>
<p>The name of the package is used to resolve resources from inside the package or the folder the module is contained in depending on if the package parameter resolves to an actual python package (a folder with an <code>__init__.py</code> file inside) or a standard module (just a <code>.py</code> file).</p>
</blockquote>
<blockquote>
<p>一个 Flask 对象实际上是一个 WSGI 应用。它接收一个模块或包的名字作为参数。它被创建之后，所有的视图函数、URL 规则、模板设置等都会被注册到它上面。之所以要传入模块或包的名字，是为了定位一些资源。</p>
</blockquote>
<p>Flask 类有这样一些属性：</p>
<ul>
<li><code>request_class = Request</code> 设置请求的类型</li>
<li><code>response_class = Response</code> 设置响应的类型</li>
</ul>
<p>这两个类型都来源于它的依赖库 <code>werkzeug</code> 并做了简单的拓展。</p>
<p>Flask 对象的 <code>__init__</code> 方法如下：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, package_name</span>):</span><br><span class="line">    <span class="comment">#: Flask 对象有这样一个字典来保存所有的视图函数</span></span><br><span class="line">    <span class="variable language_">self</span>.view_functions = &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">#: 这个字典用来保存所有的错误处理视图函数</span></span><br><span class="line">    <span class="comment">#: 字典的 key 是错误类型码</span></span><br><span class="line">    <span class="variable language_">self</span>.error_handlers = &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">#: 这个列表用来保存在请求被分派之前应当执行的函数</span></span><br><span class="line">    <span class="variable language_">self</span>.before_request_funcs = []</span><br><span class="line"></span><br><span class="line">    <span class="comment">#: 在接收到第一个请求的时候应当执行的函数</span></span><br><span class="line">    <span class="variable language_">self</span>.before_first_request_funcs = []</span><br><span class="line"></span><br><span class="line">    <span class="comment">#: 这个列表中的函数在请求完成之后被调用，响应对象会被传给这些函数</span></span><br><span class="line">    <span class="variable language_">self</span>.after_request_funcs = []</span><br><span class="line"></span><br><span class="line">    <span class="comment">#: 这里设置了一个 url_map 属性，并把它设置为一个 Map 对象</span></span><br><span class="line">    <span class="variable language_">self</span>.url_map = Map()</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>到这里一个 Flask 对象创建完毕并被变量 <code>app</code> 所指向，其实它就是一个保存了一些配置信息，绑定了一些视图函数并且有个 URL 映射对象（<code>url_map</code>）的对象。但我们还不知道这个 Map 对象是什么，有什么作用，从名字上看，似乎其作用是映射 URL 到视图函数。源代码第 21 行有 <code>from werkzeug.routing import Map, Rule</code>，那我们就来看看 <code>werkzeug</code> 这个库中对 Map 的定义：</p>
<blockquote>
<p>The map class stores all the URL rules and some configuration parameters. Some of the configuration values are only stored on the <code>Map</code> instance since those affect all rules, others are just defaults and can be overridden for each rule. Note that you have to specify all arguments besides the <code>rules</code> as keyword arguments!</p>
</blockquote>
<p>可以看到这个类的对象储存了所有的 URL 规则和一些配置信息。由于 <code>werkzeug</code> 的映射机制比较复杂，我们下文中讲到映射机制的时候再深入了解，现在只要记住 Flask 应用（即一个 <code>Flask</code> 类的实例）存储了视图函数，并通过 <code>url_map</code> 这个变量存储了一个 URL 映射机构就可以了。</p>
<h2 id="应用启动过程"><a href="#应用启动过程" class="headerlink" title="应用启动过程"></a>应用启动过程</h2><blockquote>
<p>源码阅读：<code>app.py</code> 中 <code>Flask</code> 类的代码和 <code>werkzeug.serving</code> 的代码，特别注意 <code>run_simple</code> <code>BaseWSGIServer</code> <code>WSGIRequestHandler</code>。</p>
</blockquote>
<p>Demo 代码的第 6 行是一个限制，表示如果 Python 解释器是直接运行该文件或包的，则运行 Flask 程序：在 Python 中，如果直接执行一个模块或包，那么解释器就会把当前模块或包的 <code>__name__</code> 设置为为 <code>__main_</code>。</p>
<p>第 7 行中的 <code>run</code> 方法启动了 Flask 应用：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">run</span>(<span class="params">self, host=<span class="literal">None</span>, port=<span class="literal">None</span>, debug=<span class="literal">None</span>, **options</span>):</span><br><span class="line">    <span class="keyword">from</span> werkzeug.serving <span class="keyword">import</span> run_simple</span><br><span class="line">    <span class="keyword">if</span> host <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        host = <span class="string">&#x27;127.0.0.1&#x27;</span></span><br><span class="line">    <span class="keyword">if</span> port <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        server_name = <span class="variable language_">self</span>.config[<span class="string">&#x27;SERVER_NAME&#x27;</span>]</span><br><span class="line">        <span class="keyword">if</span> server_name <span class="keyword">and</span> <span class="string">&#x27;:&#x27;</span> <span class="keyword">in</span> server_name:</span><br><span class="line">            port = <span class="built_in">int</span>(server_name.rsplit(<span class="string">&#x27;:&#x27;</span>, <span class="number">1</span>)[<span class="number">1</span>])</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            port = <span class="number">5000</span></span><br><span class="line">    <span class="keyword">if</span> debug <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="variable language_">self</span>.debug = <span class="built_in">bool</span>(debug)</span><br><span class="line">    options.setdefault(<span class="string">&#x27;use_reloader&#x27;</span>, <span class="variable language_">self</span>.debug)</span><br><span class="line">    options.setdefault(<span class="string">&#x27;use_debugger&#x27;</span>, <span class="variable language_">self</span>.debug)</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        run_simple(host, port, <span class="variable language_">self</span>, **options)</span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        <span class="comment"># reset the first request information if the development server</span></span><br><span class="line">        <span class="comment"># reset normally.  This makes it possible to restart the server</span></span><br><span class="line">        <span class="comment"># without reloader and that stuff from an interactive shell.</span></span><br><span class="line">        <span class="variable language_">self</span>._got_first_request = <span class="literal">False</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>可以看到这个方法基本上是在配置参数，实际上启动服务器的是 <code>werkzeug</code> 的 <code>run_simple</code> 方法，该方法在默认情况下启动了服务器 <code>BaseWSGIServer</code>，继承自 Python 标准库中的 <code>HTTPServer.TCPServer</code>。注意在调用 <code>run_simple</code> 时，Flask 对象把自己 <code>self</code> 作为参数传进去了，这是正确的，因为服务器在收到请求的时候，必须要知道应该去调用谁的 <code>__call__</code> 方法。</p>
<p>按照标准库中 <code>HTTPServer.TCPServer</code> 的模式，服务器必须有一个类来作为 request handler 来处理收到的请求，而不是由 <code>HTTPServer.TCPServer</code> 本身的实例来处理，<code>werkzeug</code> 提供了 <code>WSGIRequestHandler</code> 类来作为 request handler，这个类在被 <code>BaseWSGIServer</code> 调用时，会执行这个函数：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">execute</span>(<span class="params">app</span>):</span><br><span class="line">    application_iter = app(environ, start_response)</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">for</span> data <span class="keyword">in</span> application_iter:</span><br><span class="line">            write(data)</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> headers_sent:</span><br><span class="line">            write(<span class="string">b&#x27;&#x27;</span>)</span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">hasattr</span>(application_iter, <span class="string">&#x27;close&#x27;</span>):</span><br><span class="line">            application_iter.close()</span><br><span class="line">        application_iter = <span class="literal">None</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>函数的第一行就是按照 WSGI 要求的，调用了 app 并把 <code>environ</code> 和 <code>start_response</code> 传入。我们再看看 flask 中是如何按照 WSGI 要求对服务器的调用进行呼应的。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">__call__</span>(<span class="params">self, environ, start_response</span>):</span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">self</span>.wsgi_app(environ, start_response)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">wsgi_app</span>(<span class="params">self, environ, start_response</span>):</span><br><span class="line">    ctx = <span class="variable language_">self</span>.request_context(environ)</span><br><span class="line">    ctx.push()</span><br><span class="line">    error = <span class="literal">None</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            response = <span class="variable language_">self</span>.full_dispatch_request()</span><br><span class="line">        <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">            error = e</span><br><span class="line">            response = <span class="variable language_">self</span>.handle_exception(e)</span><br><span class="line">        <span class="keyword">return</span> response(environ, start_response)</span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>.should_ignore_error(error):</span><br><span class="line">            error = <span class="literal">None</span></span><br><span class="line">        ctx.auto_pop(error)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>可以看到 Flask 按照 WSGI 的要求实现了 <code>__call__</code> 方法，因此成为了一个可调用的对象。但它不是在直接在 <code>__call__</code> 里写逻辑的，而是调用了 <code>wsgi_app</code> 方法，这是为了中间件的考虑，不展开谈了。这个方法返回的 <code>response(environ, start_response)</code> 中，<code>response</code> 是 <code>werkzueg.response</code> 类的一个实例，它也是个可以调用的对象，这个对象会负责生成最终的可遍历的响应体，并调用 <code>start_response</code> 形成响应头。</p>
<h2 id="请求处理过程"><a href="#请求处理过程" class="headerlink" title="请求处理过程"></a>请求处理过程</h2><blockquote>
<p>源码阅读：<code>app.Flask</code> 的代码。</p>
</blockquote>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">wsgi_app</span>(<span class="params">self, environ, start_response</span>):</span><br><span class="line">    ctx = <span class="variable language_">self</span>.request_context(environ)</span><br><span class="line">    ctx.push()</span><br><span class="line">    error = <span class="literal">None</span></span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            response = <span class="variable language_">self</span>.full_dispatch_request()</span><br><span class="line">        <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">            error = e</span><br><span class="line">            response = <span class="variable language_">self</span>.handle_exception(e)</span><br><span class="line">        <span class="keyword">return</span> response(environ, start_response)</span><br><span class="line">    <span class="keyword">finally</span>:</span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>.should_ignore_error(error):</span><br><span class="line">            error = <span class="literal">None</span></span><br><span class="line">        ctx.auto_pop(error)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p><code>wsgi_app</code> 方法中里面的内容就是对请求处理过程的一个高度抽象。</p>
<p>首先，在接收到服务器传过来的请求时，Flask 调用 <code>request_context</code> 函数建立了一个 <code>RequestContext</code> 请求上下文对象，并把它压入 <code>_request_ctx_stack</code> 栈。关于上下文和栈的内容下文会再讲到，你现在需要知道的是，这些操作是为了 flask 在处理多个请求的时候不会混淆。之后，Flask 会调用 <code>full_dispatch_request</code> 方法对这个请求进行分发，开始实际的请求处理过程，这个过程中会生成一个响应对象并最终通过调用 <code>response</code> 对象来返回给服务器。如果当中出错，就声称相应的错误信息。不管是否出错，最终 Flask 都会把请求上下文推出栈。</p>
<p><code>full_dispatch_request</code> 是请求分发的入口，我们再来看它的实现：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">full_dispatch_request</span>(<span class="params">self</span>):</span><br><span class="line">    <span class="variable language_">self</span>.try_trigger_before_first_request_functions()</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        rv = <span class="variable language_">self</span>.preprocess_request()</span><br><span class="line">        <span class="keyword">if</span> rv <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">            rv = <span class="variable language_">self</span>.dispatch_request()</span><br><span class="line">    <span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">        rv = <span class="variable language_">self</span>.handle_user_exception(e)</span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">self</span>.finalize_request(rv)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>首先调用 <code>try_trigger_before_first_request_functions</code> 方法来尝试调用 <code>before_first_request</code> 列表中的函数，如果 <code>Flask</code> 的 <code>_got_first_request</code> 属性为 <code>False</code>，<code>before_first_request</code> 中的函数就会被执行，执行一次之后，<code>_got_first_request</code> 就会被设置为 <code>True</code> 从而不再执行这些函数。</p>
<p>然后调用 <code>preprocess_request</code> 方法，这个方法调用 <code>before_request_funcs</code> 列表中所有的方法，如果这些 <code>before_request_funcs</code> 方法中返回了某种东西，那么就不会真的去分发这个请求。比如说，一个 <code>before_request_funcs</code> 方法是用来检测用户是否登录的，如果用户没有登录，那么这个方法就会调用 <code>abort</code> 方法从而返回一个错误，Flask 就不会分发这个请求而是直接报 401 错误。</p>
<p>如果 <code>before_request_funcs</code> 中的函数没有返回，那么再调用 <code>dispatch_request</code> 方法进行请求分发。这个方法首先会查看 URL 规则中有没有相应的 <code>endpoint</code> 和 <code>value</code> 值，如果有，那么就调用 <code>view_functions</code> 中相应的视图函数（<code>endpoint</code> 作为键值）并把参数值传入（<code>**req.view_args</code>），如果没有就由 <code>raise_routing_exception</code> 进行处理。视图函数的返回值或者错误处理视图函数的返回值会返回给 <code>wsgi_app</code> 方法中的 <code>rv</code> 变量。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">dispatch_request</span>(<span class="params">self</span>):</span><br><span class="line">        req = _request_ctx_stack.top.request</span><br><span class="line">        <span class="keyword">if</span> req.routing_exception <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            <span class="variable language_">self</span>.raise_routing_exception(req)</span><br><span class="line">        rule = req.url_rule</span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">getattr</span>(rule, <span class="string">&#x27;provide_automatic_options&#x27;</span>, <span class="literal">False</span>) \</span><br><span class="line">           <span class="keyword">and</span> req.method == <span class="string">&#x27;OPTIONS&#x27;</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="variable language_">self</span>.make_default_options_response()</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.view_functions[rule.endpoint](**req.view_args)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">finalize_request</span>(<span class="params">self, rv, from_error_handler=<span class="literal">False</span></span>):</span><br><span class="line">    response = <span class="variable language_">self</span>.make_response(rv)</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        response = <span class="variable language_">self</span>.process_response(response)</span><br><span class="line">        request_finished.send(<span class="variable language_">self</span>, response=response)</span><br><span class="line">    <span class="keyword">except</span> Exception:</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> from_error_handler:</span><br><span class="line">            <span class="keyword">raise</span></span><br><span class="line">        <span class="variable language_">self</span>.logger.exception(<span class="string">&#x27;Request finalizing failed with an &#x27;</span></span><br><span class="line">                              <span class="string">&#x27;error while handling an error&#x27;</span>)</span><br><span class="line">    <span class="keyword">return</span> response</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">make_response</span>(<span class="params">self, rv</span>):</span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">isinstance</span>(rv, <span class="variable language_">self</span>.response_class):</span><br><span class="line">        <span class="keyword">return</span> rv</span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">isinstance</span>(rv, basestring):</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.response_class(rv)</span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">isinstance</span>(rv, <span class="built_in">tuple</span>):</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.response_class(*rv)</span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">self</span>.response_class.force_type(rv, request.environ)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>然后 Flask 就会根据 <code>rv</code> 生成响应，这个 <code>make_response</code> 方法会查看 rv 是否是要求的返回值类型，否则生成正确的返回类型。比如 Demo 中返回值是字符串，就会满足 <code>isinstance(rv, basestring)</code> 判断并从字符串生成响应。这一步完成之后，Flask 查看是否有后处理视图函数需要执行（在 <code>process_response</code> 方法中），并最终返回一个完全处理好的 <code>response</code> 对象。</p>
<h2 id="视图函数注册"><a href="#视图函数注册" class="headerlink" title="视图函数注册"></a>视图函数注册</h2><p>在请求处理过程一节中，我们已经看到了 Flask 是如何调用试图函数的，这一节我们要关注 Flask 如何构建和请求分派相关的数据结构。我们将主要关注 <code>view_functions</code>，因为其他的数据结构如 <code>before_request_funcs</code> 的构建过程大同小异，甚至更为简单。我们也将仔细讲解在应用的创建一节中遗留的问题，即 <code>url_map</code> 到底是什么。</p>
<p>Demo 代码的第 4 行用修饰器 <code>route</code> 注册一个视图函数，这是 Flask 中受到广泛称赞的一个设计。在 Flask 类的 <code>route</code> 方法中，可以看到它调用了 <code>add_url_rule</code> 方法。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">route</span>(<span class="params">self, rule, **options</span>):</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">decorator</span>(<span class="params">f</span>):</span><br><span class="line">        endpoint = options.pop(<span class="string">&#x27;endpoint&#x27;</span>, <span class="literal">None</span>)</span><br><span class="line">        <span class="variable language_">self</span>.add_url_rule(rule, endpoint, f, **options)</span><br><span class="line">        <span class="keyword">return</span> f</span><br><span class="line">    <span class="keyword">return</span> decorator</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">add_url_rule</span>(<span class="params">self, rule, endpoint, **options</span>):</span><br><span class="line">    <span class="keyword">if</span> endpoint <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        endpoint = _endpoint_from_view_func(view_func)</span><br><span class="line">    options[<span class="string">&#x27;endpoint&#x27;</span>] = endpoint</span><br><span class="line">    methods = options.pop(<span class="string">&#x27;methods&#x27;</span>, <span class="literal">None</span>)</span><br><span class="line">    <span class="keyword">if</span> methods <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        methods = <span class="built_in">getattr</span>(view_func, <span class="string">&#x27;methods&#x27;</span>, <span class="literal">None</span>) <span class="keyword">or</span> (<span class="string">&#x27;GET&#x27;</span>,)</span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">isinstance</span>(methods, string_types):</span><br><span class="line">        <span class="keyword">raise</span> TypeError(<span class="string">&#x27;Allowed methods have to be iterables of strings, &#x27;</span></span><br><span class="line">                        <span class="string">&#x27;for example: @app.route(..., methods=[&quot;POST&quot;])&#x27;</span>)</span><br><span class="line">    methods = <span class="built_in">set</span>(item.upper() <span class="keyword">for</span> item <span class="keyword">in</span> methods)</span><br><span class="line"></span><br><span class="line">    required_methods = <span class="built_in">set</span>(<span class="built_in">getattr</span>(view_func, <span class="string">&#x27;required_methods&#x27;</span>, ()))</span><br><span class="line"></span><br><span class="line">    provide_automatic_options = <span class="built_in">getattr</span>(view_func,</span><br><span class="line">        <span class="string">&#x27;provide_automatic_options&#x27;</span>, <span class="literal">None</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> provide_automatic_options <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="keyword">if</span> <span class="string">&#x27;OPTIONS&#x27;</span> <span class="keyword">not</span> <span class="keyword">in</span> methods:</span><br><span class="line">            provide_automatic_options = <span class="literal">True</span></span><br><span class="line">            required_methods.add(<span class="string">&#x27;OPTIONS&#x27;</span>)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            provide_automatic_options = <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    methods |= required_methods</span><br><span class="line"></span><br><span class="line">    rule = <span class="variable language_">self</span>.url_rule_class(rule, methods=methods, **options)</span><br><span class="line">    rule.provide_automatic_options = provide_automatic_options</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">self</span>.url_map.add(rule)</span><br><span class="line">    <span class="keyword">if</span> view_func <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">        old_func = <span class="variable language_">self</span>.view_functions.get(endpoint)</span><br><span class="line">        <span class="keyword">if</span> old_func <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span> <span class="keyword">and</span> old_func != view_func:</span><br><span class="line">            <span class="keyword">raise</span> AssertionError(<span class="string">&#x27;View function mapping is overwriting an &#x27;</span></span><br><span class="line">                                 <span class="string">&#x27;existing endpoint function: %s&#x27;</span> % endpoint)</span><br><span class="line">        <span class="variable language_">self</span>.view_functions[endpoint] = view_func</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>这个方法负责注册视图函数，并实现 URL 到视图函数的映射。首先，它要准备好一个视图函数所支持的 HTTP 方法（基本上一半多的代码都是在做这个），然后通过 <code>url_rule_class</code> 创建一个 <code>rule</code> 对象，并把这个对象添加到自己的 <code>url_map</code> 里。我们那个遗留问题在这里就得到解答了：<code>rule</code> 对象是一个保存合法的（Flask 应用所支持的） URL、方法、<code>endpoint</code>（在 <code>**options</code> 中） 及它们的对应关系的数据结构，而 <code>url_map</code> 是保存这些对象的集合。然后，这个方法将视图函数添加到 <code>view_functions</code> 当中，<code>endpoint</code> 作为它的键，其值默认是函数名。</p>
<p>我们再来深入了解一下 <code>rule</code> ，它被定义在 <code>werkzeug.routing.Rule</code> 中：</p>
<blockquote>
<p>A Rule represents one URL pattern. There are some options for <code>Rule</code> that change the way it behaves and are passed to the <code>Rule</code> constructor. 一个 Rule 对象代表了一种 URL 模式，可以通过传入参数来改变它的许多行为。</p>
</blockquote>
<p>Rule 的 <code>__init__</code> 方法为：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, string, defaults=<span class="literal">None</span>, subdomain=<span class="literal">None</span>, methods=<span class="literal">None</span>,</span></span><br><span class="line"><span class="params">                 build_only=<span class="literal">False</span>, endpoint=<span class="literal">None</span>, strict_slashes=<span class="literal">None</span>,</span></span><br><span class="line"><span class="params">                 redirect_to=<span class="literal">None</span>, alias=<span class="literal">False</span>, host=<span class="literal">None</span></span>):</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> string.startswith(<span class="string">&#x27;/&#x27;</span>):</span><br><span class="line">        <span class="keyword">raise</span> ValueError(<span class="string">&#x27;urls must start with a leading slash&#x27;</span>)</span><br><span class="line">    <span class="variable language_">self</span>.rule = string</span><br><span class="line">    <span class="variable language_">self</span>.is_leaf = <span class="keyword">not</span> string.endswith(<span class="string">&#x27;/&#x27;</span>)</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">self</span>.<span class="built_in">map</span> = <span class="literal">None</span></span><br><span class="line">    <span class="variable language_">self</span>.strict_slashes = strict_slashes</span><br><span class="line">    <span class="variable language_">self</span>.subdomain = subdomain</span><br><span class="line">    <span class="variable language_">self</span>.host = host</span><br><span class="line">    <span class="variable language_">self</span>.defaults = defaults</span><br><span class="line">    <span class="variable language_">self</span>.build_only = build_only</span><br><span class="line">    <span class="variable language_">self</span>.alias = alias</span><br><span class="line">    <span class="keyword">if</span> methods <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="variable language_">self</span>.methods = <span class="literal">None</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">isinstance</span>(methods, <span class="built_in">str</span>):</span><br><span class="line">            <span class="keyword">raise</span> TypeError(<span class="string">&#x27;param `methods` should be `Iterable[str]`, not `str`&#x27;</span>)</span><br><span class="line">        <span class="variable language_">self</span>.methods = <span class="built_in">set</span>([x.upper() <span class="keyword">for</span> x <span class="keyword">in</span> methods])</span><br><span class="line">        <span class="keyword">if</span> <span class="string">&#x27;HEAD&#x27;</span> <span class="keyword">not</span> <span class="keyword">in</span> <span class="variable language_">self</span>.methods <span class="keyword">and</span> <span class="string">&#x27;GET&#x27;</span> <span class="keyword">in</span> <span class="variable language_">self</span>.methods:</span><br><span class="line">            <span class="variable language_">self</span>.methods.add(<span class="string">&#x27;HEAD&#x27;</span>)</span><br><span class="line">    <span class="variable language_">self</span>.endpoint = endpoint</span><br><span class="line">    <span class="variable language_">self</span>.redirect_to = redirect_to</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> defaults:</span><br><span class="line">        <span class="variable language_">self</span>.arguments = <span class="built_in">set</span>(<span class="built_in">map</span>(<span class="built_in">str</span>, defaults))</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="variable language_">self</span>.arguments = <span class="built_in">set</span>()</span><br><span class="line">    <span class="variable language_">self</span>._trace = <span class="variable language_">self</span>._converters = <span class="variable language_">self</span>._regex = <span class="variable language_">self</span>._weights = <span class="literal">None</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>一个 Rule 被创建后，通过 <code>Map</code> 的 <code>add</code> 方法被绑定到 <code>Map</code> 对象上，我们之前说过 <code>flask.url_map</code> 就是一个 <code>Map</code> 对象。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">add</span>(<span class="params">self, rulefactory</span>):</span><br><span class="line">    <span class="keyword">for</span> rule <span class="keyword">in</span> rulefactory.get_rules(<span class="variable language_">self</span>):</span><br><span class="line">        rule.bind(<span class="variable language_">self</span>)</span><br><span class="line">        <span class="variable language_">self</span>._rules.append(rule)</span><br><span class="line">        <span class="variable language_">self</span>._rules_by_endpoint.setdefault(rule.endpoint, []).append(rule)</span><br><span class="line">    <span class="variable language_">self</span>._remap = <span class="literal">True</span></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>而 <code>Rule</code> 的 <code>bind</code> 方法的内容，就是添加 <code>Rule</code> 对应的 <code>Map</code>，然后调用 <code>compile</code> 方法生成一个正则表达式，<code>compile</code> 方法比较复杂，就不展开了。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">bind</span>(<span class="params">self, <span class="built_in">map</span>, rebind=<span class="literal">False</span></span>):</span><br><span class="line">    <span class="string">&quot;&quot;&quot;Bind the url to a map and create a regular expression based on</span></span><br><span class="line"><span class="string">    the information from the rule itself and the defaults from the map.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">    :internal:</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="keyword">if</span> <span class="variable language_">self</span>.<span class="built_in">map</span> <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span> <span class="keyword">and</span> <span class="keyword">not</span> rebind:</span><br><span class="line">        <span class="keyword">raise</span> RuntimeError(<span class="string">&#x27;url rule %r already bound to map %r&#x27;</span> %</span><br><span class="line">                           (<span class="variable language_">self</span>, <span class="variable language_">self</span>.<span class="built_in">map</span>))</span><br><span class="line">    <span class="variable language_">self</span>.<span class="built_in">map</span> = <span class="built_in">map</span></span><br><span class="line">    <span class="keyword">if</span> <span class="variable language_">self</span>.strict_slashes <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="variable language_">self</span>.strict_slashes = <span class="built_in">map</span>.strict_slashes</span><br><span class="line">    <span class="keyword">if</span> <span class="variable language_">self</span>.subdomain <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="variable language_">self</span>.subdomain = <span class="built_in">map</span>.default_subdomain</span><br><span class="line">    <span class="variable language_">self</span>.<span class="built_in">compile</span>()</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>在 Flask 应用收到请求时，这些被绑定到 <code>url_map</code> 上的 <code>Rule</code> 会被查看，来找到它们对应的视图函数。这是在请求上下文中实现的，我们先前在 <code>dispatch_request</code> 方法中就见过——我们是从 <code>_request_ctx_stack.top.request</code> 得到 <code>rule</code> 并从这个 <code>rule</code> 找到 <code>endpoint</code>，最终找到用来处理该请求的正确的视图函数的。所以，接下来我们需要看请求上下的具体实现，并且看一看 Flask 是如何从 <code>url_map</code> 中找到这个 <code>rule</code> 的。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">dispatch_request</span>(<span class="params">self</span>):</span><br><span class="line">    req = _request_ctx_stack.top.request</span><br><span class="line">    <span class="keyword">if</span> req.routing_exception <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="variable language_">self</span>.raise_routing_exception(req)</span><br><span class="line">    rule = req.url_rule</span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">getattr</span>(rule, <span class="string">&#x27;provide_automatic_options&#x27;</span>, <span class="literal">False</span>) \</span><br><span class="line">       <span class="keyword">and</span> req.method == <span class="string">&#x27;OPTIONS&#x27;</span>:</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.make_default_options_response()</span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">self</span>.view_functions[rule.endpoint](**req.view_args)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h2 id="请求上下文"><a href="#请求上下文" class="headerlink" title="请求上下文"></a>请求上下文</h2><blockquote>
<p>源码阅读：<code>ctx.RequestContext</code> 的代码。</p>
</blockquote>
<p>请求上下文是如何、在何时被创建的呢？我们先前也见过，在服务器调用应用的时候，Flask 的 <code>wsgi_app</code> 中有这样的语句，就是创建了请求上下文并压栈。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">wsgi_app</span>(<span class="params">self, environ, start_response</span>):</span><br><span class="line">    ctx = <span class="variable language_">self</span>.request_context(environ)</span><br><span class="line">    ctx.push()</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p><code>request_context</code> 方法非常简单，就是创建了 <code>RequestContext</code> 类的一个实例，这个类被定义在 <code>flask.ctx</code> 文件中，它包含了一系列关于请求的信息，<strong>最重要的是</strong>它自身的 <code>request</code> 属性指向了一个 <code>Request</code> 类的实例，这个类继承自 <code>werkzeug.Request</code>，在 <code>RequestContext</code> 的创建过程中，它会根据传入的 <code>environ</code> 创建一个 <code>werkzeug.Request</code> 的实例。</p>
<p>接着 <code>RequestContext</code> 的 <code>push</code> 方法被调用，这个方法将自己推到 <code>_request_ctx_stack</code> 的栈顶。</p>
<p><code>_request_ctx_stack</code> 被定义在 <code>flask.global</code> 文件中，它是一个 <code>LocalStack</code> 类的实例，是 <code>werkzeug.local</code> 所实现的，如果你对 Python 的 threading 熟悉的话，就会发现这里实现了线程隔离，就是说，在 Python 解释器运行到 <code>_request_ctx_stack</code> 相关代码的时候，解释器会根据当前进程来选择正确的实例。</p>
<p>但是，在整个分析 Flask 源码的过程中，我们也没发现 Flask 在被调用之后创建过线程啊，那么为什么要做线程隔离呢？看我们开头提到的 <code>run</code> 函数，其实它可以传一个 <code>threaded</code> 参数。当不传这个函数的时候，我们启动的是 <code>BasicWSGIServer</code>，这个服务器是单线程单进程的，Flask 的线程安全自然没有意义，但是当我们传入这个参数的时候，我们启动的是 <code>ThreadedWSGIServer</code>，这时 Flask 的线程安全就是有意义的了，在其他多线程的服务器中也是一样。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><h3 id="一个请求的旅程"><a href="#一个请求的旅程" class="headerlink" title="一个请求的旅程"></a>一个请求的旅程</h3><p>这里，我们通过追踪一个请求到达服务器并返回（当然是通过“变成”一个相应）的旅程，串讲本文的内容。</p>
<ol>
<li>在请求发出之前，Flask 注册好了所有的视图函数和 URL 映射，服务器在自己身上注册了 Flask 应用。</li>
<li>请求到达服务器，服务器准备好 <code>environ</code> 和 <code>make_response</code> 函数，然后调用了自己身上注册的 Flask 应用。</li>
<li>应用实现了 WSGI 要求的 <code>application(environ, make_response)</code> 方法。在 Flask 中，这个方法是个被 <code>__call__</code> 中转的叫做 <code>wsgi_app</code> 的方法。它首先通过 <code>environ</code> 创建了请求上下文，并将它推入栈，使得 flask 在处理当前请求的过程中都可以访问到这个请求上下文。</li>
<li>然后 Flask 开始处理这个请求，依次调用 <code>before_first_request_funcs</code> <code>before_request_funcs</code> <code>view_functions</code> 中的函数，并最终通过 <code>finalize_request</code> 生成一个 <code>response</code> 对象，当中只要有函数返回值，后面的函数组就不会再执行，<code>after_request_funcs</code> 进行 <code>response</code> 生成后的后处理。</li>
<li>Flask 调用这个 <code>response</code> 对象，最终调用了 <code>make_response</code> 函数，并返回了一个可遍历的响应内容。</li>
<li>服务器发送响应。</li>
</ol>
<h3 id="Flask-和-werkzeug"><a href="#Flask-和-werkzeug" class="headerlink" title="Flask 和 werkzeug"></a>Flask 和 werkzeug</h3><p>在分析过程中，可以很明显地看出 Flask 和 <code>werkzeug</code> 是强耦合的，实际上 <code>werkzeug</code> 是 Flask 唯一不可或缺的依赖，一些非常细节的工作，其实都是 <code>werkzeug</code> 库完成的，在本文的例子中，它至少做了这些事情：</p>
<ol>
<li>封装 <code>Response</code> 和 <code>Request</code> 类型供 Flask 使用，在实际开发中，我们在请求和响应对象上的操作，调用的其实是 <code>werkzeug</code> 的方法。</li>
<li>实现 URL 到视图函数的映射，并且能把 URL 中的参数传给该视图函数。我们看到了 Flask 的 url_map 属性并且看到了它如何绑定视图函数和错误处理函数，但是具体的映射规则的实践，和在响应过程中的 URL 解析，都是由 werkzeug 完成的。</li>
<li>通过 LocalProxy 类生成的 <code>_request_ctx_stack</code> 对 Flask 实现线程保护。</li>
</ol>
<p>对于 Flask 的源码分析先暂时到这里。有时间的话，我会分析 Flask 中的模板渲染、<code>import request</code>、蓝图和一些好用的变量及函数，或者深入分析 <code>werkzeug</code> 库。</p>
<h3 id="参考阅读"><a href="#参考阅读" class="headerlink" title="参考阅读"></a>参考阅读</h3><ol>
<li><a href="http://cizixs.com/2017/01/10/flask-insight-introduction">flask 源码解析系列文章</a>，你可以在读完本文了解主线之后，再看这系列文章了解更加细节的东西。</li>
<li><a href="http://python.jobbole.com/81524/">一起写一个 web 服务器</a>。</li>
</ol>
<h2 id="文章更新记录"><a href="#文章更新记录" class="headerlink" title="文章更新记录"></a>文章更新记录</h2><ul>
<li>2017年9月10日：利用 0.12.0 版本进行分析，重新调整了结构和行文顺序，增加了许多内容。</li>
</ul>
]]></content>
      <categories>
        <category>转载</category>
      </categories>
      <tags>
        <tag>flask</tag>
      </tags>
  </entry>
  <entry>
    <title>转载整理-Python-ElasticSearch，python对ES进行写入、更新、删除、搜索</title>
    <url>/post/%E8%BD%AC%E8%BD%BD%E6%95%B4%E7%90%86-Python-ElasticSearch%EF%BC%8Cpython%E5%AF%B9ES%E8%BF%9B%E8%A1%8C%E5%86%99%E5%85%A5%E3%80%81%E6%9B%B4%E6%96%B0%E3%80%81%E5%88%A0%E9%99%A4%E3%80%81%E6%90%9C%E7%B4%A2.html</url>
    <content><![CDATA[<blockquote>
<p>py-es官方文档：<a href="https://elasticsearch-py.readthedocs.io/en/master/api.html">https://elasticsearch-py.readthedocs.io/en/master/api.html</a></p>
<p>es官方中文：<a href="http://doc.codingdict.com/elasticsearch/1/">http://doc.codingdict.com/elasticsearch/1/</a></p>
<p>es官方英文：<a href="https://www.elastic.co/guide/cn/elasticsearch/guide/current/_most_important_queries.html">https://www.elastic.co/guide/cn/elasticsearch/guide/current/_most_important_queries.html</a></p>
<p>转载：</p>
<ol>
<li><p><a href="https://blog.csdn.net/u013429010/article/details/81746179">Python-ElasticSearch，python对ES进行写入、更新、删除、搜索</a></p>
</li>
<li><p><a href="https://blog.csdn.net/qq_42513284/article/details/90678516">Elasticsearch学习（三）————元数据（_index、_type、_id、_score、_source）</a></p>
</li>
<li><p><a href="https://blog.csdn.net/ZYC88888/article/details/82964474">ES基本查询总结</a></p>
</li>
</ol>
<p>参考：</p>
<ol>
<li><a href="https://www.cnblogs.com/shoufeng/p/11266136.html">ES 22 - Elasticsearch中如何进行日期(数值)范围查询</a></li>
<li><a href="https://stackoverflow.com/questions/48749364/how-to-fix-the-error-failed-to-parse-date-field-in-elasticsearch">上面那个format 有问题 </a></li>
</ol>
</blockquote>
<span id="more"></span>

<p>[TOC]</p>
<h2 id="数据理解"><a href="#数据理解" class="headerlink" title="数据理解"></a>数据理解</h2><h3 id="元数据"><a href="#元数据" class="headerlink" title="元数据"></a>元数据</h3><p>&#x3D;&#x3D;概念：&#x3D;&#x3D;元数据(Metadata)，又称中介数据、中继数据，为描述数据的数据(data about data)，主要是描述数据属性(property)的信息，用来支持如指示存储位置、历史数据、资源查找、文件记录等功能。元数据算是一种电子式目录，为了达到编制目录的目的，必须在描述并收藏数据的内容或特色，进而达成协助数据检索的目的。元数据是关于数据的组织、数据域及其关系的信息，简言之，元数据就是关于数据的数据。</p>
<h4 id="1-1-index"><a href="#1-1-index" class="headerlink" title="1.1. _index"></a>1.1. _index</h4><ol>
<li>代表一个document存储在哪个index中。</li>
<li>类似的数据（document）放在同一个index，不同类型的数据放在不同的index。</li>
<li>索引名必须是小写，不能用下划线（_）开头，不能包含逗号。</li>
</ol>
<h4 id="1-2-type"><a href="#1-2-type" class="headerlink" title="1.2. _type"></a>1.2. _type</h4><ol>
<li>代表document属于index下 的哪个类别（type）。</li>
<li>一个index通常包含多个type。</li>
<li>type名称可以大写或小写，不能用下划线（_）开头，不能包含逗号。</li>
</ol>
<h4 id="1-3-id"><a href="#1-3-id" class="headerlink" title="1.3. _id"></a>1.3. _id</h4><ol>
<li>代表document的唯一标识，与index和type一起确定一个唯一的document。</li>
<li>可以手动指定，也可es自动生成。手动指定：PUT &#x2F;index&#x2F;type&#x2F;id 自动生成： PUT &#x2F;index&#x2F;type&#x2F; 生成base64编码的20长度的字符串ID，分布式集群下不可能重复。</li>
</ol>
<h4 id="1-4-score"><a href="#1-4-score" class="headerlink" title="1.4. _score"></a>1.4. _score</h4><ol>
<li>相关度分数：匹配程度，分数越高越相关。</li>
</ol>
<h4 id="1-5-source"><a href="#1-5-source" class="headerlink" title="1.5. _source"></a>1.5. _source</h4><ol>
<li>代表一个document，是一个json对象（{json}）,包含一个实例对象</li>
</ol>
<h2 id="python-常用es方法"><a href="#python-常用es方法" class="headerlink" title="python 常用es方法"></a>python 常用es方法</h2><h3 id="python-es-常用-增删改-方法"><a href="#python-es-常用-增删改-方法" class="headerlink" title="python-es 常用  增删改  方法"></a>python-es 常用  增删改  方法</h3><h4 id="创建-删除-index索引-（类似于数据库）"><a href="#创建-删除-index索引-（类似于数据库）" class="headerlink" title="创建,删除 index索引 （类似于数据库）"></a>创建,删除 index索引 （类似于数据库）</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#创建索引，索引的名字是my-index,如果已经存在了，就返回个400，</span></span><br><span class="line"><span class="comment">#这个索引可以现在创建，也可以在后面插入数据的时候再临时创建</span></span><br><span class="line">es.indices.create(index=<span class="string">&#x27;my-index&#x27;</span>,ignore)</span><br><span class="line"><span class="comment">#但是我在使用中发现好像没有ignore这个参数了</span></span><br><span class="line"><span class="comment"># ignore 400 cause by IndexAlreadyExistsException when creating an index</span></span><br><span class="line">es.indices.create(index=<span class="string">&#x27;my-index&#x27;</span>, ignore=<span class="number">400</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># ignore 404 and 400</span></span><br><span class="line">es.indices.delete(index=<span class="string">&#x27;my-index&#x27;</span>, ignore=[<span class="number">400</span>, <span class="number">404</span>])</span><br></pre></td></tr></table></figure>

<h4 id="插入数据"><a href="#插入数据" class="headerlink" title="插入数据"></a>插入数据</h4><p>index 为 my-index, doc_type 为 test-type，id 为 01，其他内容用body</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#插入数据,(这里省略插入其他两条数据，后面用)</span></span><br><span class="line">es.index(index=<span class="string">&quot;my-index&quot;</span>,doc_type=<span class="string">&quot;test-type&quot;</span>,<span class="built_in">id</span>=<span class="string">&#x27;01&#x27;</span>,body=&#123;<span class="string">&quot;any&quot;</span>:<span class="string">&quot;data01&quot;</span>,<span class="string">&quot;timestamp&quot;</span>:datetime.now()&#125;)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="get获取数据"><a href="#get获取数据" class="headerlink" title="get获取数据"></a>get获取数据</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#查询数据，两种get and search</span></span><br><span class="line"><span class="comment">#get获取</span></span><br><span class="line">res = es.get(index=<span class="string">&quot;my-index&quot;</span>, doc_type=<span class="string">&quot;test-type&quot;</span>, <span class="built_in">id</span>=01)</span><br><span class="line">es.get(index=<span class="string">&#x27;indexName&#x27;</span>, doc_type=<span class="string">&#x27;typeName&#x27;</span>, <span class="built_in">id</span>=<span class="string">&#x27;idValue&#x27;</span>)</span><br></pre></td></tr></table></figure>

<h4 id="删除数据"><a href="#删除数据" class="headerlink" title="删除数据"></a>删除数据</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">delete：删除指定index、<span class="built_in">type</span>、<span class="built_in">id</span>的文档</span><br><span class="line"></span><br><span class="line">es.delete(index=<span class="string">&#x27;indexName&#x27;</span>, doc_type=<span class="string">&#x27;typeName&#x27;</span>, <span class="built_in">id</span>=<span class="string">&#x27;idValue&#x27;</span>)</span><br></pre></td></tr></table></figure>

<h4 id="条件删除"><a href="#条件删除" class="headerlink" title="条件删除"></a>条件删除</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">delete_by_query：删除满足条件的所有数据，查询条件必须符合DLS格式</span><br><span class="line"></span><br><span class="line">query = &#123;<span class="string">&#x27;query&#x27;</span>: &#123;<span class="string">&#x27;match&#x27;</span>: &#123;<span class="string">&#x27;sex&#x27;</span>: <span class="string">&#x27;famale&#x27;</span>&#125;&#125;&#125;<span class="comment"># 删除性别为女性的所有文档</span></span><br><span class="line"></span><br><span class="line">query = &#123;<span class="string">&#x27;query&#x27;</span>: &#123;<span class="string">&#x27;range&#x27;</span>: &#123;<span class="string">&#x27;age&#x27;</span>: &#123;<span class="string">&#x27;lt&#x27;</span>: <span class="number">11</span>&#125;&#125;&#125;&#125;<span class="comment"># 删除年龄小于11的所有文档</span></span><br><span class="line"></span><br><span class="line">es.delete_by_query(index=<span class="string">&#x27;indexName&#x27;</span>, body=query, doc_type=<span class="string">&#x27;typeName&#x27;</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="条件更新"><a href="#条件更新" class="headerlink" title="条件更新"></a>条件更新</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">update_by_query：更新满足条件的所有数据，写法同上删除和查询</span><br></pre></td></tr></table></figure>

<h4 id="批量写入、删除、更新"><a href="#批量写入、删除、更新" class="headerlink" title="批量写入、删除、更新"></a>批量写入、删除、更新</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">doc = [</span><br><span class="line">     &#123;<span class="string">&quot;index&quot;</span>: &#123;&#125;&#125;,</span><br><span class="line">     &#123;<span class="string">&#x27;name&#x27;</span>: <span class="string">&#x27;jackaaa&#x27;</span>, <span class="string">&#x27;age&#x27;</span>: <span class="number">2000</span>, <span class="string">&#x27;sex&#x27;</span>: <span class="string">&#x27;female&#x27;</span>, <span class="string">&#x27;address&#x27;</span>: <span class="string">u&#x27;北京&#x27;</span>&#125;,</span><br><span class="line">     &#123;<span class="string">&quot;index&quot;</span>: &#123;&#125;&#125;,</span><br><span class="line">     &#123;<span class="string">&#x27;name&#x27;</span>: <span class="string">&#x27;jackbbb&#x27;</span>, <span class="string">&#x27;age&#x27;</span>: <span class="number">3000</span>, <span class="string">&#x27;sex&#x27;</span>: <span class="string">&#x27;male&#x27;</span>, <span class="string">&#x27;address&#x27;</span>: <span class="string">u&#x27;上海&#x27;</span>&#125;,</span><br><span class="line">     &#123;<span class="string">&quot;index&quot;</span>: &#123;&#125;&#125;,</span><br><span class="line">     &#123;<span class="string">&#x27;name&#x27;</span>: <span class="string">&#x27;jackccc&#x27;</span>, <span class="string">&#x27;age&#x27;</span>: <span class="number">4000</span>, <span class="string">&#x27;sex&#x27;</span>: <span class="string">&#x27;female&#x27;</span>, <span class="string">&#x27;address&#x27;</span>: <span class="string">u&#x27;广州&#x27;</span>&#125;,</span><br><span class="line">     &#123;<span class="string">&quot;index&quot;</span>: &#123;&#125;&#125;,</span><br><span class="line">     &#123;<span class="string">&#x27;name&#x27;</span>: <span class="string">&#x27;jackddd&#x27;</span>, <span class="string">&#x27;age&#x27;</span>: <span class="number">1000</span>, <span class="string">&#x27;sex&#x27;</span>: <span class="string">&#x27;male&#x27;</span>, <span class="string">&#x27;address&#x27;</span>: <span class="string">u&#x27;深圳&#x27;</span>&#125;,</span><br><span class="line"> ]</span><br><span class="line">doc = [</span><br><span class="line">    &#123;<span class="string">&#x27;index&#x27;</span>: &#123;<span class="string">&#x27;_index&#x27;</span>: <span class="string">&#x27;indexName&#x27;</span>, <span class="string">&#x27;_type&#x27;</span>: <span class="string">&#x27;typeName&#x27;</span>, <span class="string">&#x27;_id&#x27;</span>: <span class="string">&#x27;idValue&#x27;</span>&#125;&#125;</span><br><span class="line">    &#123;<span class="string">&#x27;name&#x27;</span>: <span class="string">&#x27;jack&#x27;</span>, <span class="string">&#x27;sex&#x27;</span>: <span class="string">&#x27;male&#x27;</span>, <span class="string">&#x27;age&#x27;</span>: <span class="number">10</span> &#125;</span><br><span class="line">    &#123;<span class="string">&#x27;delete&#x27;</span>: &#123;<span class="string">&#x27;_index&#x27;</span>: <span class="string">&#x27;indexName&#x27;</span>, <span class="string">&#x27;_type&#x27;</span>: <span class="string">&#x27;typeName&#x27;</span>, <span class="string">&#x27;_id&#x27;</span>: <span class="string">&#x27;idValue&#x27;</span>&#125;&#125;</span><br><span class="line">    &#123;<span class="string">&quot;create&quot;</span>: &#123;<span class="string">&#x27;_index&#x27;</span> : <span class="string">&#x27;indexName&#x27;</span>, <span class="string">&quot;_type&quot;</span> : <span class="string">&#x27;typeName&#x27;</span>, <span class="string">&#x27;_id&#x27;</span>: <span class="string">&#x27;idValue&#x27;</span>&#125;&#125;</span><br><span class="line">    &#123;<span class="string">&#x27;name&#x27;</span>: <span class="string">&#x27;lucy&#x27;</span>, <span class="string">&#x27;sex&#x27;</span>: <span class="string">&#x27;female&#x27;</span>, <span class="string">&#x27;age&#x27;</span>: <span class="number">20</span> &#125;</span><br><span class="line">    &#123;<span class="string">&#x27;update&#x27;</span>: &#123;<span class="string">&#x27;_index&#x27;</span>: <span class="string">&#x27;indexName&#x27;</span>, <span class="string">&#x27;_type&#x27;</span>: <span class="string">&#x27;typeName&#x27;</span>, <span class="string">&#x27;_id&#x27;</span>: <span class="string">&#x27;idValue&#x27;</span>&#125;&#125;</span><br><span class="line">    &#123;<span class="string">&#x27;doc&#x27;</span>: &#123;<span class="string">&#x27;age&#x27;</span>: <span class="string">&#x27;100&#x27;</span>&#125;&#125;</span><br><span class="line"> ]</span><br><span class="line"> es.bulk(index=<span class="string">&#x27;indexName&#x27;</span>，  doc_type=<span class="string">&#x27;typeName&#x27;</span>, body=doc)</span><br><span class="line"> </span><br><span class="line"> <span class="comment">#批量更新也可以采用如下的方式进行json拼装，最后写入</span></span><br><span class="line"> <span class="keyword">for</span> line <span class="keyword">in</span> <span class="built_in">list</span>:</span><br><span class="line">            action = &#123;</span><br><span class="line">                <span class="string">&quot;_index&quot;</span>: <span class="variable language_">self</span>.index_name,</span><br><span class="line">                <span class="string">&quot;_type&quot;</span>: <span class="variable language_">self</span>.index_type,</span><br><span class="line">                <span class="string">&quot;_id&quot;</span>: i, <span class="comment">#_id 也可以默认生成，不赋值</span></span><br><span class="line">                <span class="string">&quot;_source&quot;</span>: &#123;</span><br><span class="line">                    <span class="string">&quot;date&quot;</span>: line[<span class="string">&#x27;date&#x27;</span>],</span><br><span class="line">                    <span class="string">&quot;source&quot;</span>: line[<span class="string">&#x27;source&#x27;</span>].decode(<span class="string">&#x27;utf8&#x27;</span>),</span><br><span class="line">                    <span class="string">&quot;link&quot;</span>: line[<span class="string">&#x27;link&#x27;</span>],</span><br><span class="line">                    <span class="string">&quot;keyword&quot;</span>: line[<span class="string">&#x27;keyword&#x27;</span>].decode(<span class="string">&#x27;utf8&#x27;</span>),</span><br><span class="line">                    <span class="string">&quot;title&quot;</span>: line[<span class="string">&#x27;title&#x27;</span>].decode(<span class="string">&#x27;utf8&#x27;</span>)&#125;</span><br><span class="line">            &#125;</span><br><span class="line">            i += <span class="number">1</span></span><br><span class="line">            ACTIONS.append(action)</span><br><span class="line">success, _ = bulk(<span class="variable language_">self</span>.es, ACTIONS, index=<span class="variable language_">self</span>.index_name, raise_on_error=<span class="literal">True</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure>



<h3 id="python-es-常用查询方法"><a href="#python-es-常用查询方法" class="headerlink" title="python-es 常用查询方法"></a>python-es 常用查询方法</h3><h4 id="match-all-查询所有"><a href="#match-all-查询所有" class="headerlink" title="match_all 查询所有"></a>match_all 查询所有</h4><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 或者</span></span><br><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;match_all&quot;</span>:&#123;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h4 id="term与terms-等于-匹配"><a href="#term与terms-等于-匹配" class="headerlink" title="term与terms 等于 匹配"></a>term与terms 等于 匹配</h4><h5 id="term-单一匹配关键词"><a href="#term-单一匹配关键词" class="headerlink" title="term 单一匹配关键词"></a>term 单一匹配关键词</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;term&quot;</span>:&#123;</span><br><span class="line">            <span class="string">&quot;name&quot;</span>:<span class="string">&quot;python&quot;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 查询name=&quot;python&quot;的所有数据</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h5 id="terms-匹配多个关键词"><a href="#terms-匹配多个关键词" class="headerlink" title="terms 匹配多个关键词"></a>terms 匹配多个关键词</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;terms&quot;</span>:&#123;</span><br><span class="line">            <span class="string">&quot;name&quot;</span>:[</span><br><span class="line">                <span class="string">&quot;python&quot;</span>,<span class="string">&quot;android&quot;</span></span><br><span class="line">            ]</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 搜索出name=&quot;python&quot;或name=&quot;android&quot;的所有数据</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h4 id="match与multi-match-包含匹配"><a href="#match与multi-match-包含匹配" class="headerlink" title="match与multi_match 包含匹配"></a>match与multi_match 包含匹配</h4><h5 id="match-包含-类似于-contain"><a href="#match-包含-类似于-contain" class="headerlink" title="match 包含 类似于 contain"></a>match 包含 类似于 contain</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># match:匹配name包含python关键字的数据</span></span><br><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;match&quot;</span>:&#123;</span><br><span class="line">            <span class="string">&quot;name&quot;</span>:<span class="string">&quot;python&quot;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 查询name包含python关键字的数据</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h5 id="multi-match-多个字段包含-类似于多个contain"><a href="#multi-match-多个字段包含-类似于多个contain" class="headerlink" title="multi_match  多个字段包含 类似于多个contain"></a>multi_match  多个字段包含 类似于多个contain</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># multi_match:在name和addr里匹配包含深圳关键字的数据</span></span><br><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;multi_match&quot;</span>:&#123;</span><br><span class="line">            <span class="string">&quot;query&quot;</span>:<span class="string">&quot;深圳&quot;</span>,</span><br><span class="line">            <span class="string">&quot;fields&quot;</span>:[<span class="string">&quot;name&quot;</span>,<span class="string">&quot;addr&quot;</span>]</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 查询name和addr包含&quot;深圳&quot;关键字的数据</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h4 id="ids"><a href="#ids" class="headerlink" title="ids"></a>ids</h4><h5 id="ids-搜索指定id的数据"><a href="#ids-搜索指定id的数据" class="headerlink" title="ids 搜索指定id的数据"></a>ids 搜索指定id的数据</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;ids&quot;</span>:&#123;</span><br><span class="line">            <span class="string">&quot;type&quot;</span>:<span class="string">&quot;test_type&quot;</span>,</span><br><span class="line">            <span class="string">&quot;values&quot;</span>:[</span><br><span class="line">                <span class="string">&quot;1&quot;</span>,<span class="string">&quot;2&quot;</span></span><br><span class="line">            ]</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 搜索出id为1或2d的所有数据</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h4 id="复合查询bool"><a href="#复合查询bool" class="headerlink" title="复合查询bool"></a>复合查询bool</h4><p>bool有3类查询关系，must(都满足),should(其中一个满足),must_not(都不满足)</p>
<h5 id="must-都满足"><a href="#must-都满足" class="headerlink" title="must(都满足)"></a>must(都满足)</h5><h5 id="should-其中一个满足"><a href="#should-其中一个满足" class="headerlink" title="should(其中一个满足)"></a>should(其中一个满足)</h5><h5 id="must-not-都不满足"><a href="#must-not-都不满足" class="headerlink" title="must_not(都不满足)"></a>must_not(都不满足)</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;bool&quot;</span>:&#123;</span><br><span class="line">            <span class="string">&quot;must&quot;</span>:[</span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="string">&quot;term&quot;</span>:&#123;</span><br><span class="line">                        <span class="string">&quot;name&quot;</span>:<span class="string">&quot;python&quot;</span></span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;,</span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="string">&quot;term&quot;</span>:&#123;</span><br><span class="line">                        <span class="string">&quot;age&quot;</span>:<span class="number">18</span></span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            ]</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 获取name=&quot;python&quot;并且age=18的所有数据</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h4 id="切片式查询"><a href="#切片式查询" class="headerlink" title="切片式查询"></a>切片式查询</h4><h5 id="可以用来分页"><a href="#可以用来分页" class="headerlink" title="可以用来分页"></a>可以用来分页</h5><p>利用 from 和 size 来   控制数据</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;match_all&quot;</span>:&#123;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="string">&quot;from&quot;</span>:<span class="number">2</span>    <span class="comment"># 从第二条数据开始</span></span><br><span class="line">    <span class="string">&quot;size&quot;</span>:<span class="number">4</span>    <span class="comment"># 获取4条数据</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 从第2条数据开始，获取4条数据</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h4 id="范围查询"><a href="#范围查询" class="headerlink" title="范围查询"></a>范围查询</h4><h5 id="range-利用-gte或者lte-查询范围"><a href="#range-利用-gte或者lte-查询范围" class="headerlink" title="range 利用 gte或者lte 查询范围"></a>range 利用 gte或者lte 查询范围</h5><p>如果是查询时间 ，还要用到format ，对日期进行格式化 </p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;range&quot;</span>:&#123;</span><br><span class="line">            <span class="string">&quot;age&quot;</span>:&#123;</span><br><span class="line">                <span class="string">&quot;gte&quot;</span>:<span class="number">18</span>,       <span class="comment"># &gt;=18</span></span><br><span class="line">                <span class="string">&quot;lte&quot;</span>:<span class="number">30</span>        <span class="comment"># &lt;=30</span></span><br><span class="line">            &#125;,</span><br><span class="line">            <span class="string">&quot;post_date&quot;</span>: &#123;</span><br><span class="line">                <span class="string">&quot;gte&quot;</span>: <span class="string">&quot;2/1/2018&quot;</span>, </span><br><span class="line">                <span class="string">&quot;lte&quot;</span>: <span class="string">&quot;2019&quot;</span>,</span><br><span class="line">                <span class="string">&quot;format&quot;</span>: <span class="string">&quot;dd/MM/yyyy||yyyy&quot;</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 查询18&lt;=age&lt;=30的所有数据</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h4 id="前缀查询"><a href="#前缀查询" class="headerlink" title="前缀查询"></a>前缀查询</h4><h5 id="prefix-（查询前缀为xx的数据）"><a href="#prefix-（查询前缀为xx的数据）" class="headerlink" title="prefix （查询前缀为xx的数据）"></a>prefix （查询前缀为xx的数据）</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;prefix&quot;</span>:&#123;</span><br><span class="line">            <span class="string">&quot;name&quot;</span>:<span class="string">&quot;p&quot;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 查询前缀为&quot;赵&quot;的所有数据</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h4 id="通配符查询-类似like"><a href="#通配符查询-类似like" class="headerlink" title="通配符查询 类似like"></a>通配符查询 类似like</h4><h5 id="wildcard-（查询name以-id为后缀的所有数据）"><a href="#wildcard-（查询name以-id为后缀的所有数据）" class="headerlink" title="wildcard （查询name以*id为后缀的所有数据）"></a>wildcard （查询name以*id为后缀的所有数据）</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;wildcard&quot;</span>:&#123;</span><br><span class="line">            <span class="string">&quot;name&quot;</span>:<span class="string">&quot;*id&quot;</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 查询name以id为后缀的所有数据</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h4 id="排序-对结果进行排序"><a href="#排序-对结果进行排序" class="headerlink" title="排序 对结果进行排序"></a>排序 对结果进行排序</h4><h5 id="sort"><a href="#sort" class="headerlink" title="sort"></a>sort</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;match_all&quot;</span>:&#123;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="string">&quot;sort&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;age&quot;</span>:&#123;                 <span class="comment"># 根据age字段升序排序</span></span><br><span class="line">            <span class="string">&quot;order&quot;</span>:<span class="string">&quot;asc&quot;</span>       <span class="comment"># asc升序，desc降序</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="响应过滤-filter-path-获取指定字段"><a href="#响应过滤-filter-path-获取指定字段" class="headerlink" title="响应过滤 filter_path (获取指定字段)"></a>响应过滤 filter_path (获取指定字段)</h4><h5 id="filter-path"><a href="#filter-path" class="headerlink" title="filter_path"></a>filter_path</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 只需要获取_id数据,多个条件用逗号隔开</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,filter_path=[<span class="string">&quot;hits.hits._id&quot;</span>])</span><br><span class="line"></span><br><span class="line"><span class="comment"># 获取所有数据</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,filter_path=[<span class="string">&quot;hits.hits._*&quot;</span>])</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="执行查询并获取该查询的匹配数-count"><a href="#执行查询并获取该查询的匹配数-count" class="headerlink" title="执行查询并获取该查询的匹配数 count"></a>执行查询并获取该查询的匹配数 count</h4><h5 id="count"><a href="#count" class="headerlink" title="count"></a>count</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 获取数据量</span></span><br><span class="line">es.count(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>)</span><br></pre></td></tr></table></figure>

<h4 id="度量类聚合-最小值-最大值"><a href="#度量类聚合-最小值-最大值" class="headerlink" title="度量类聚合 (最小值 最大值)"></a>度量类聚合 (最小值 最大值)</h4><h5 id="获取最小值-min"><a href="#获取最小值-min" class="headerlink" title="获取最小值 min"></a>获取最小值 min</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;match_all&quot;</span>:&#123;&#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">&quot;aggs&quot;</span>:&#123;                        <span class="comment"># 聚合查询</span></span><br><span class="line">        <span class="string">&quot;min_age&quot;</span>:&#123;                 <span class="comment"># 最小值的key</span></span><br><span class="line">            <span class="string">&quot;min&quot;</span>:&#123;                 <span class="comment"># 最小</span></span><br><span class="line">                <span class="string">&quot;field&quot;</span>:<span class="string">&quot;age&quot;</span>       <span class="comment"># 查询&quot;age&quot;的最小值</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 搜索所有数据，并获取age最小的值</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h5 id="获取最大值-max"><a href="#获取最大值-max" class="headerlink" title="获取最大值 max"></a>获取最大值 max</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;match_all&quot;</span>:&#123;&#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">&quot;aggs&quot;</span>:&#123;                        <span class="comment"># 聚合查询</span></span><br><span class="line">        <span class="string">&quot;max_age&quot;</span>:&#123;                 <span class="comment"># 最大值的key</span></span><br><span class="line">            <span class="string">&quot;max&quot;</span>:&#123;                 <span class="comment"># 最大</span></span><br><span class="line">                <span class="string">&quot;field&quot;</span>:<span class="string">&quot;age&quot;</span>       <span class="comment"># 查询&quot;age&quot;的最大值</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 搜索所有数据，并获取age最大的值</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h5 id="获取和-sum"><a href="#获取和-sum" class="headerlink" title="获取和 sum"></a>获取和 sum</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;match_all&quot;</span>:&#123;&#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">&quot;aggs&quot;</span>:&#123;                        <span class="comment"># 聚合查询</span></span><br><span class="line">        <span class="string">&quot;sum_age&quot;</span>:&#123;                 <span class="comment"># 和的key</span></span><br><span class="line">            <span class="string">&quot;sum&quot;</span>:&#123;                 <span class="comment"># 和</span></span><br><span class="line">                <span class="string">&quot;field&quot;</span>:<span class="string">&quot;age&quot;</span>       <span class="comment"># 获取所有age的和</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 搜索所有数据，并获取所有age的和</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h5 id="获取平均值-sum"><a href="#获取平均值-sum" class="headerlink" title="获取平均值 sum"></a>获取平均值 sum</h5><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">body = &#123;</span><br><span class="line">    <span class="string">&quot;query&quot;</span>:&#123;</span><br><span class="line">        <span class="string">&quot;match_all&quot;</span>:&#123;&#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">&quot;aggs&quot;</span>:&#123;                        <span class="comment"># 聚合查询</span></span><br><span class="line">        <span class="string">&quot;avg_age&quot;</span>:&#123;                 <span class="comment"># 平均值的key</span></span><br><span class="line">            <span class="string">&quot;sum&quot;</span>:&#123;                 <span class="comment"># 平均值</span></span><br><span class="line">                <span class="string">&quot;field&quot;</span>:<span class="string">&quot;age&quot;</span>       <span class="comment"># 获取所有age的平均值</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment"># 搜索所有数据，获取所有age的平均值</span></span><br><span class="line">es.search(index=<span class="string">&quot;my_index&quot;</span>,doc_type=<span class="string">&quot;test_type&quot;</span>,body=body)</span><br></pre></td></tr></table></figure>

<h3 id="es-和-mysql数据对比结构图"><a href="#es-和-mysql数据对比结构图" class="headerlink" title="es 和 mysql数据对比结构图"></a>es 和 mysql数据对比结构图</h3><p><img src="https://i.loli.net/2020/09/11/B2hLPtKwJFjd3ID.png"><br><img src="https://i.loli.net/2020/09/11/95HXcybCIilqNan.png"><br><img src="https://i.loli.net/2020/09/11/Eg7UOTf5mt4NLKs.png"></p>
<p>Elasticsearch中当我们设置Mapping（分词器、字段类型）完毕后，就可以按照设定的方式导入数据。</p>
<p>有了数据后，我们就需要对数据进行检索操作。根据实际开发需要，往往我们需要支持包含但不限于以下类型的检索：<br>1）精确匹配，类似mysql中的 “&#x3D;”操作；<br>2）模糊匹配，类似mysql中的”like %关键词% “查询操作；<br>3）前缀匹配；<br>4）通配符匹配；<br>5）正则表达式匹配；<br>6）跨索引匹配；<br>7）提升精读匹配。</p>
<p>细数一下，我们的痛点在于：<br>1）ES究竟支持哪些检索操作？<br>2）如何实现ES精确值检索、指定索引检索、全文检索？</p>
<p>这些就是本文着重参考ES最新官方文档，针对ES5.X版本探讨的内容。</p>
<h2 id="0、检索概览"><a href="#0、检索概览" class="headerlink" title="0、检索概览"></a>0、检索概览</h2><p>检索子句的行为取决于查询应用于过滤(filter)上下文还是查询&#x2F;分析(query)上下文。</p>
<p>过滤上下文——对应于结构化检索</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1）核心回答的问题是：“这个文档是否符合这个查询条款？”  2）答案是简单的是或否，不计算分数。  3）过滤器上下文主要用于过滤结构化数据。类似于Mysql中判定某个字段是否存在： </span><br></pre></td></tr></table></figure>

<p>例如：<br>a. 时间戳字段：是否属于2015年或2016年？<br>b. 状态字段：是否设置为“已发布”？</p>
<p>经常使用的过滤器将被Elasticsearch<strong>自动缓存</strong>，以<strong>加快性能</strong>。</p>
<p>分析上下文——对应于全文检索<br>1）核心回答了“本文档与此查询子句是否匹配？”的问题。</p>
<p>2）除了决定文档是否匹配之外，查询子句还会计算一个_score，表示文档与其他文档的匹配程度。</p>
<p>综合应用场景如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /_search&#123;  &quot;query&quot;: &#123;  &quot;bool&quot;: &#123;  &quot;must&quot;: [  &#123; &quot;match&quot;: &#123; &quot;title&quot;: &quot;Search&quot; &#125;&#125;,  &#123; &quot;match&quot;: &#123; &quot;content&quot;: &quot;Elasticsearch&quot; &#125;&#125;  ],  &quot;filter&quot;: [  &#123; &quot;term&quot;: &#123; &quot;status&quot;: &quot;published&quot; &#125;&#125;,  &#123; &quot;range&quot;: &#123; &quot;publish_date&quot;: &#123; &quot;gte&quot;: &quot;2015-01-01&quot; &#125;&#125;&#125;  ]  &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<p>以上检索，title中包含”Search”并且content中包含 “Elasticsearch”，status中精确匹配”published”，并且publish_date 大于“2015-01-01”的全部信息。</p>
<p>以下，以“<strong>脑图</strong>”的形式直观展示检索分类。</p>
<p><img src="https://i.loli.net/2020/09/11/BCOKp6EDrGYZ5eM.png"><br>其中，3-7根据我随着我开发深入再做更新。</p>
<p>以下内容的原文需要参考ES官方文档（随着版本变化，后续会有更新）</p>
<h2 id="1、结构化检索"><a href="#1、结构化检索" class="headerlink" title="1、结构化检索"></a>1、结构化检索</h2><p>针对字段类型： 日期、时间、数字类型，以及精确的文本匹配。<br>结构化检索特点：<br>* 1）结构化查询，我们得到的结果 总是 非是即否，要么存于集合之中，要么存在集合之外。<br>* 2）结构化查询不关心文件的相关度或评分；它简单的对文档包括或排除处理。</p>
<h3 id="1-1-精确值查找"><a href="#1-1-精确值查找" class="headerlink" title="1.1 精确值查找"></a>1.1 精确值查找</h3><h4 id="1-1-1-单个精确值查找（term-query）"><a href="#1-1-1-单个精确值查找（term-query）" class="headerlink" title="1.1.1 单个精确值查找（term query）"></a>1.1.1 单个精确值查找（term query）</h4><p>term 查询会查找我们指定的精确值。term 查询是简单的，它接受一个字段名以及我们希望查找的数值。</p>
<p>想要类似mysql中如下sql语句的查询操作：</p>
<p>SELECT document FROM products WHERE price &#x3D; 20;<br>DSL写法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /my_store/products/_search&#123;  &quot;query&quot; : &#123;  &quot;term&quot; : &#123;  &quot;price&quot; : 20  &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<p>当进行精确值查找时， 我们会使用过滤器（filters）。过滤器很重要，因为它们执行速度非常快，不会计算相关度（直接跳过了整个评分阶段）而且很容易被缓存。如下： 使用 constant_score 查询以非评分模式来执行 term 查询并以一作为统一评分。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /my_store/products/_search&#123;  &quot;query&quot; : &#123;  &quot;constant_score&quot; : &#123;  &quot;filter&quot; : &#123;  &quot;term&quot; : &#123;  &quot;price&quot; : 20  &#125;  &#125;  &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<p>注意：5.xES中，对于字符串类型，要进行精确值匹配。需要讲类型设置为text和keyword两种类型。mapping设置如下:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">POST testindex/testtype/_mapping&#123;   &quot;testtype &quot;:&#123;  &quot;properties&quot;:&#123;  &quot;title&quot;:&#123;  &quot;type&quot;:&quot;text&quot;,  &quot;analyzer&quot;:&quot;ik_max_word&quot;,  &quot;search_analyzer&quot;:&quot;ik_max_word&quot;,  &quot;fields&quot;:&#123;  &quot;keyword&quot;:&#123;  &quot;type&quot;:&quot;keyword&quot;  &#125;  &#125;  &#125;&#125;&#125;</span><br></pre></td></tr></table></figure>

<p>精确值java api jest使用方法：<br>searchSourceBuilder.query(QueryBuilders.termQuery(“text.keyword”, “来自新华社的报道”));</p>
<h4 id="1-1-2-布尔过滤器"><a href="#1-1-2-布尔过滤器" class="headerlink" title="1.1.2 布尔过滤器"></a>1.1.2 布尔过滤器</h4><p>一个 bool 过滤器由三部分组成：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;   &quot;bool&quot; : &#123;      &quot;must&quot; :     [],      &quot;should&quot; :   [],      &quot;must_not&quot; : [],      &quot;filter&quot;:    []   &#125;&#125;</span><br></pre></td></tr></table></figure>

<p>must ——所有的语句都 必须（must） 匹配，与 AND 等价。<br>must_not ——所有的语句都 不能（must not） 匹配，与 NOT 等价。<br>should ——至少有一个语句要匹配，与 OR 等价。<br>filter——必须匹配，运行在非评分&amp;过滤模式。<br>就这么简单！ 当我们需要多个过滤器时，只须将它们置入 bool 过滤器的不同部分即可。</p>
<p>举例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /my_store/products/_search&#123;  &quot;query&quot; : &#123;  &quot;filtered&quot; : &#123;  &quot;filter&quot; : &#123;  &quot;bool&quot; : &#123;  &quot;should&quot; : [  &#123; &quot;term&quot; : &#123;&quot;price&quot; : 20&#125;&#125;,   &#123; &quot;term&quot; : &#123;&quot;productID&quot; : &quot;XHDK-A-1293-#fJ3&quot;&#125;&#125;   ],  &quot;must_not&quot; : &#123;  &quot;term&quot; : &#123;&quot;price&quot; : 30&#125;   &#125;  &#125;  &#125;  &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<h4 id="1-1-3-多个值精确查找（terms-query）"><a href="#1-1-3-多个值精确查找（terms-query）" class="headerlink" title="1.1.3 多个值精确查找（terms query）"></a>1.1.3 多个值精确查找（terms query）</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;  &quot;terms&quot; : &#123;  &quot;price&quot; : [20, 30]  &#125;&#125;</span><br></pre></td></tr></table></figure>

<p>如上，terms是包含的意思，包含20或者包含30。<br>如下实现严格意义的精确值检索， tag_count代表必须匹配的次数为1。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /my_index/my_type/_search&#123;  &quot;query&quot;: &#123;  &quot;constant_score&quot; : &#123;  &quot;filter&quot; : &#123;  &quot;bool&quot; : &#123;  &quot;must&quot; : [  &#123; &quot;term&quot; : &#123; &quot;tags&quot; : &quot;search&quot; &#125; &#125;,  &#123; &quot;term&quot; : &#123; &quot;tag_count&quot; : 1 &#125; &#125;  ]  &#125;  &#125;  &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="1-2-范围检索（range-query）"><a href="#1-2-范围检索（range-query）" class="headerlink" title="1.2 范围检索（range query）"></a>1.2 范围检索（range query）</h3><p>range 查询可同时提供包含（inclusive）和不包含（exclusive）这两种范围表达式，可供组合的选项如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">gt: &gt; 大于（greater than）lt: &lt; 小于（less than）gte: &gt;= 大于或等于（greater than or equal to）lte: &lt;= 小于或等于（less than or equal to）</span><br></pre></td></tr></table></figure>

<p>类似Mysql中的范围查询：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">SELECT documentFROM   productsWHERE  price BETWEEN 20 AND 40</span><br></pre></td></tr></table></figure>

<p>ES中对应的DSL如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /my_store/products/_search&#123;  &quot;query&quot; : &#123;  &quot;constant_score&quot; : &#123;  &quot;filter&quot; : &#123;  &quot;range&quot; : &#123;  &quot;price&quot; : &#123;  &quot;gte&quot; : 20,  &quot;lt&quot; : 40  &#125;  &#125;  &#125;  &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="1-3-存在与否检索（exist-query）"><a href="#1-3-存在与否检索（exist-query）" class="headerlink" title="1.3 存在与否检索（exist query）"></a>1.3 存在与否检索（exist query）</h3><p>mysql中，有如下sql：<br>SELECT tags FROM posts WHERE tags IS NOT NULL;</p>
<p>ES中，exist查询某个字段是否存在：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /my_index/posts/_search&#123;    &quot;query&quot; : &#123;        &quot;constant_score&quot; : &#123;            &quot;filter&quot; : &#123;                &quot;exists&quot; : &#123; &quot;field&quot; : &quot;tags&quot; &#125;            &#125;        &#125;    &#125;&#125;</span><br></pre></td></tr></table></figure>

<p>若想要exist查询能匹配null类型，需要设置mapping：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&quot;user&quot;: &#123;  &quot;type&quot;: &quot;keyword&quot;,  &quot;null_value&quot;: &quot;_null_&quot;  &#125;</span><br></pre></td></tr></table></figure>

<p>missing查询在5.x版本已经不存在，改成如下的判定形式：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /_search&#123;    &quot;query&quot;: &#123;        &quot;bool&quot;: &#123;            &quot;must_not&quot;: &#123;                &quot;exists&quot;: &#123;                    &quot;field&quot;: &quot;user&quot;                &#125;            &#125;        &#125;    &#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="1-4-前缀检索（-Prefix-Query-）"><a href="#1-4-前缀检索（-Prefix-Query-）" class="headerlink" title="1.4 前缀检索（ Prefix Query ）"></a>1.4 前缀检索（ Prefix Query ）</h3><p>匹配包含 not analyzed 的前缀字符：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /_search&#123; &quot;query&quot;: &#123;  &quot;prefix&quot; : &#123; &quot;user&quot; : &quot;ki&quot; &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="1-5-通配符检索-wildcard-query"><a href="#1-5-通配符检索-wildcard-query" class="headerlink" title="1.5 通配符检索( wildcard query)"></a>1.5 通配符检索( wildcard query)</h3><p>匹配具有匹配通配符表达式（ (not analyzed ）的字段的文档。 支持的通配符：<br>1）<em>，它匹配任何字符序列（包括空字符序列）；<br>2）？，它匹配任何单个字符。<br>请注意，此查询可能很慢，因为它需要遍历多个术语。<br>为了防止非常慢的通配符查询，通配符不能以任何一个通配符</em>或？开头。<br>举例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /_search&#123;    &quot;query&quot;: &#123;        &quot;wildcard&quot; : &#123; &quot;user&quot; : &quot;ki*y&quot; &#125;    &#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="1-6-正则表达式检索（regexp-query）"><a href="#1-6-正则表达式检索（regexp-query）" class="headerlink" title="1.6 正则表达式检索（regexp query）"></a>1.6 正则表达式检索（regexp query）</h3><p>正则表达式查询允许您使用正则表达式术语查询。<br>举例如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /_search&#123;  &quot;query&quot;: &#123;  &quot;regexp&quot;:&#123;  &quot;name.first&quot;: &quot;s.*y&quot;  &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<p>注意： <em>的匹配会非常慢，你需要使用一个长的前缀，<br>通常类似.</em>?+通配符查询的正则检索性能会非常低。</p>
<h3 id="1-7-模糊检索-fuzzy-query"><a href="#1-7-模糊检索-fuzzy-query" class="headerlink" title="1.7 模糊检索(fuzzy query)"></a>1.7 模糊检索(fuzzy query)</h3><p>模糊查询查找在模糊度中指定的最大编辑距离内的所有可能的匹配项，然后检查术语字典，以找出在索引中实际存在待检索的关键词。<br>举例如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /_search&#123;  &quot;query&quot;: &#123;  &quot;fuzzy&quot; : &#123; &quot;user&quot; : &quot;ki&quot; &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="1-8-类型检索（type-query）"><a href="#1-8-类型检索（type-query）" class="headerlink" title="1.8 类型检索（type query）"></a>1.8 类型检索（type query）</h3><p>举例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /my_index/_search&#123;  &quot;query&quot;: &#123;  &quot;type&quot; : &#123;  &quot;value&quot; : &quot;xext&quot;  &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<p>已验证，检索索引my_index中，type为xext的全部信息。</p>
<h3 id="1-9-Ids检索（ids-query）"><a href="#1-9-Ids检索（ids-query）" class="headerlink" title="1.9 Ids检索（ids query）"></a>1.9 Ids检索（ids query）</h3><p>返回指定id的全部信息。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /my_index/_search&#123;  &quot;query&quot;: &#123;  &quot;ids&quot; : &#123;  &quot;type&quot; : &quot;xext&quot;,  &quot;values&quot; : [&quot;2&quot;, &quot;4&quot;, &quot;100&quot;]  &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2、全文检索"><a href="#2、全文检索" class="headerlink" title="2、全文检索"></a>2、全文检索</h2><p>高级全文查询通常用于在全文本字段（如电子邮件正文）上运行全文查询。他们了解如何对被查询的字段进行分析，并在执行前将每个字段的分析器（或search_analyzer）应用于查询字符串。</p>
<h3 id="2-1-匹配检索（match-query）"><a href="#2-1-匹配检索（match-query）" class="headerlink" title="2.1 匹配检索（match query）"></a>2.1 匹配检索（match query）</h3><p>匹配查询接受文本&#x2F;数字&#x2F;日期类型，分析它们，并构造查询。<br>1）匹配查询的类型为boolean。 这意味着分析所提供的文本，并且分析过程从提供的文本构造一个布尔查询，<br>可以将运算符标志设置为或以控制布尔子句（默认为或）；<br>2）文本分析取决于mapping中设定的analyzer（中文分词，我们默认选择ik分词器）；<br>3） fuzziness——模糊性允许基于被查询的字段的类型进行模糊匹配；<br>4）”operator”: “and”——匹配与操作（默认或操作）；<br>5） “minimum_should_match”: “75%”——这让我们可以指定必须匹配的词项数用来表示一个文档是否相关。<br>举例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /_search&#123;    &quot;query&quot;: &#123;        &quot;match&quot; : &#123;            &quot;message&quot; : &#123;                &quot;query&quot; : &quot;this is a test&quot;,                &quot;operator&quot; : &quot;and&quot;            &#125;        &#125;    &#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-2-匹配解析检索-match-phrase-query"><a href="#2-2-匹配解析检索-match-phrase-query" class="headerlink" title="2.2 匹配解析检索 match_phrase query"></a>2.2 匹配解析检索 match_phrase query</h3><p>match_phrase查询分析文本，并从分析文本中创建短语查询。<br>类似 match 查询， match_phrase 查询首先将查询字符串解析成一个词项列表，然后对这些词项进行搜索，但只保留那些包含 全部 搜索词项，且 位置 与搜索词项相同的文档。<br>举例如下：对于 quick fox 的短语搜索可能不会匹配到任何文档，因为没有文档包含的 quick 词之后紧跟着 fox 。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /my_index/my_type/_search&#123;  &quot;query&quot;: &#123;  &quot;match_phrase&quot;: &#123;  &quot;title&quot;: &quot;quick brown fox&quot;  &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-3-匹配解析前缀检索（match-phrase-prefix）"><a href="#2-3-匹配解析前缀检索（match-phrase-prefix）" class="headerlink" title="2.3 匹配解析前缀检索（match_phrase_prefix）"></a>2.3 匹配解析前缀检索（match_phrase_prefix）</h3><p>用户已经渐渐习惯在输完查询内容之前，就能为他们展现搜索结果，这就是所谓的 即时搜索（instant search） 或 输入即搜索（search-as-you-type） 。<br>不仅用户能在更短的时间内得到搜索结果，我们也能引导用户搜索索引中真实存在的结果。<br>例如，如果用户输入 johnnie walker bl ，我们希望在它们完成输入搜索条件前就能得到：<br>Johnnie Walker Black Label 和 Johnnie Walker Blue Label 。</p>
<p>match_phrase_prefix与match_phrase相同，除了它允许文本中最后一个术语的前缀匹配。<br>举例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET / _search&#123;    “query”：&#123;        “match_phrase_prefix”：&#123;            “message”：“quick brown f”        &#125;    &#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-4-多字段匹配检索（-multi-match-query）"><a href="#2-4-多字段匹配检索（-multi-match-query）" class="headerlink" title="2.4 多字段匹配检索（ multi_match query）"></a>2.4 多字段匹配检索（ multi_match query）</h3><p>multi_match 查询为能在多个字段上反复执行相同查询提供了一种便捷方式。<br>默认情况下，查询的类型是 best_fields， 这表示它会为每个字段生成一个 match 查询。<br>举例1：”fields”: “*_title”<br>——任何与模糊模式正则匹配的字段都会被包括在搜索条件中， 例如可以左侧的方式同时匹配 book_title 、 chapter_title 和 section_title （书名、章名、节名）这三个字段。<br>举例2： “fields”: [ “*_title”, “chapter_title^2” ]<br>——可以使用 ^ 字符语法为单个字段提升权重，在字段名称的末尾添加 ^boost ， 其中 boost 是一个浮点数。<br>举例3：”fields”: [ “first_name”, “last_name” ],<br>“operator”: “and”<br>——两个字段必须都包含。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /_search&#123;  &quot;query&quot;: &#123;  &quot;multi_match&quot; : &#123;  &quot;query&quot;: &quot;this is a test&quot;,  &quot;fields&quot;: [ &quot;subject&quot;, &quot;message&quot; ]  &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-5-字符串检索-query-string）"><a href="#2-5-字符串检索-query-string）" class="headerlink" title="2.5 字符串检索(query_string）"></a>2.5 字符串检索(query_string）</h3><p>一个使用查询解析器解析其内容的查询。<br>query_string查询提供了以简明的简写语法执行多匹配查询 multi_match queries ，布尔查询 bool queries ，提升得分 boosting ，模糊匹配 fuzzy matching ，通配符 wildcards ，正则表达式 regexp 和范围查询 range queries 的方式。<br>支持参数达10几种。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /_search&#123;  &quot;query&quot;: &#123;  &quot;query_string&quot; : &#123;  &quot;default_field&quot; : &quot;content&quot;,  &quot;query&quot; : &quot;this AND that OR thus&quot;  &#125;  &#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-6-简化字符串检索（simple-query-string）"><a href="#2-6-简化字符串检索（simple-query-string）" class="headerlink" title="2.6 简化字符串检索（simple_query_string）"></a>2.6 简化字符串检索（simple_query_string）</h3><p>一个使用SimpleQueryParser解析其上下文的查询。 与常规query_string查询不同，simple_query_string查询永远不会抛出异常，并丢弃查询的无效部分。<br>举例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">GET /_search&#123;    &quot;query&quot;: &#123;        &quot;simple_query_string&quot; : &#123;            &quot;fields&quot; : [&quot;content&quot;],            &quot;query&quot; : &quot;foo bar -baz&quot;        &#125;    &#125;&#125;</span><br></pre></td></tr></table></figure>

<p>支持的操作如下：<br>1）+表示AND操作<br>2）| 表示OR操作<br>3）- 否定操作<br>4）*在术语结束时表示前缀查询<br>5）（和）表示优先</p>
<h2 id="3、小结"><a href="#3、小结" class="headerlink" title="3、小结"></a>3、小结</h2><p>有的博友可能会问，这和ES官网API有什么区别。<br>仔细对比你会发现，ES的中文文档是根据2.X版本翻译的，ES的英文文档一个版本是没有更新到5.X版本，另一个已经更新</p>
]]></content>
      <categories>
        <category>转载</category>
        <category>ElasticSearch</category>
      </categories>
      <tags>
        <tag>ElasticSearch</tag>
      </tags>
  </entry>
</search>
